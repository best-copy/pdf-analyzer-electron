# 2026-08-31 작업내역

> 머리글·바닥글 확장, 임포징 재단선·블리드 정리, 배포용 임포징 도구 재정비,
> 그리고 **실사용에서 나온 사고 3건**(gs 회전·평탄화 과부하·합치기 시 삭제 페이지 부활) 수정.
> (2026-08-28 2차 작업분 `작업내역_2026-08-28b.md`도 이 커밋에 함께 들어간다)

---

# 1. 실사용 사고 3건 — 원인과 수정

## 1-1. 다운로드본의 페이지가 180° 뒤집힘 ← Ghostscript

사용자 파일 3개를 대조해 범인을 특정했다.

| 파일 | 회전 | Producer |
|---|---|---|
| 작업 파일 안 원본(10쪽) | 전부 0° | 원 문서 |
| 작업 파일 안 적용본(12쪽) | **전부 0°** | pdf-lib(우리 조판) |
| 다운로드본(12쪽) | **2·11쪽 180°** | **GPL Ghostscript** |

**원인**: `pdfwrite`의 기본값 `-dAutoRotatePages=/PageByPage`. 페이지 안의 글자 방향을 보고
`/Rotate`를 붙인다. 세로 제목·뒤집힌 라벨이 있는 쪽, 글자가 없는 빈 페이지에서 오판한다.

**수정**: `main.js`의 gs 호출에 **`-dAutoRotatePages=/None`** 추가(곡선화·완전 임베드 공통).
재현/수정 확인: 회전 텍스트가 섞인 문서 → 옵션 없이 `0,0,180,90,180,0` / `/None` 지정 시 `0,0,0,0,0,0`.

## 1-2. Acrobat이 버벅임 ← 투명도 평탄화

표지 시트 한 장이 **콘텐츠 4.4MB · 이미지 273개 · 그리기 2,583회 · 클립 1,509개 · 경로 11,358개**.
본문 쪽(17~21KB)과 비교해 200배. gs 렌더도 598ms vs 237ms.

파일 헤더의 `%%Invocation`에 `-dCompatibilityLevel=1.4`가 남아 있었다 = **평탄화 옵션이 켜져 있었다**.
반투명 그라데이션 삼각형 표지를 평탄화하면서 이미지 타일로 잘게 쪼갠 것(같은 타일을 18~54회씩
서로 다른 클립 아래 반복 그리기 — 전형적인 플래트너 출력).

**수정**: 평탄화를 켤 때 경고 안내를 띄운다. 툴팁도 같은 취지로 교체.
(기능 자체는 구형 RIP 대응이라 유지 — 기본값은 계속 1.6)

## 1-3. 프린터로 문서를 받으면 지운 페이지가 되살아남

`appendImportedFiles()`가 합치기 기준으로 **`baseTab.originalPdfBytes`(맨 처음 원본)** 를 썼다.
그래서 삭제·순서·회전·빈 페이지·내부편집이 전부 무시됐다. 재현: 6쪽 → 3·4쪽 삭제(4쪽) → 2쪽 접수
→ **8쪽**(A1~A6 부활). 수정 후 **6쪽**(A1,A2,A5,A6,B1,B2).

**수정 3가지**
- 활성 문서면 `buildBaseOptimized(null, { skipBw: true })`로 **지금 화면 그대로**를 base로 굽는다.
  `skipBw` 신설 — 흑백을 굽지 않는다(아직 '적용' 전인 선택을 확정해 버리면 되돌릴 수 없다).
  이 결과는 `baseSignature` 캐시에 **넣지 않는다**(같은 서명의 흑백본과 섞이면 사고).
- 챕터 경계를 현재 페이지 기준으로 재계산(`chapterRunsOf`).
- 합치는 동안 `📐 자동 세로 맞춤` 차단(`_appendingDocs`) — base에 이미 구워진 사용자 회전을
  자동 맞춤이 되돌려 회전이 사라지던 문제가 함께 있었다.

# 2. 머리글·바닥글

- **적용 범위**: 전체 / 이 쪽부터 / **체크 선택**(챕터·페이지 2층 체크 목록, 썸네일 선택 담기).
- **시작 번호**(`numFrom`): 시작 쪽에 찍힐 첫 숫자. 예) 5쪽부터 7 → 7,8,9…
  ⚠ 함정: 예전 '번호 생략'은 *계산값이 음수*인 것에 기대고 있어, 시작 번호를 크게 주면 앞쪽에도
  번호가 찍혔다. 시작 쪽 이전은 명시적으로 비우도록 고침.
- **내부 편집기에서 그 페이지의 문구 확인·수정**(페이지별 덮어쓰기 `hf.perPage`).
  편집기에 보이는 문구와 인쇄물이 갈라지지 않도록 해석 로직을 **`src/hf-core.js` 한 벌**로 분리해
  워커(`importScripts`)와 렌더러가 공유한다.
  한글 덮어쓰기가 폰트 없이 이미지로 그려지던 것도 함께 수정(폰트 임베드 대상에 perPage 포함).

# 3. 임포징 — 재단선·블리드 정리

- **트림 인식 배치**: 원고에 블리드가 있으면(TrimBox) **트림을 칸에 맞추고** 블리드는 칸 밖으로.
  실측: 200pt 칸에서 170.9pt(−14.5%) → **200.0pt**.
- **원고 블리드(mm)** 입력 신설 — TrimBox가 없는 파일(디자인 단계에서 블리드를 그냥 포함한 파일)도
  실제 재단 크기로 잡는다. 사용자 사례 303×426(3mm 포함) → **297×420**.
- **재단선 모양 2종**: `┐ 모서리(기본)` / `┐ 모서리(겹침 없음)`(이웃과 마주 보는 쪽을 짧게 잘라 겹침 방지).
  ⚠ 모양을 바꿔도 반영되지 않던 원인: **재단선 스타일이 임포징 옵션에 없어 결과 캐시가 갈리지 않았다**
  → `cropStyle`을 옵션에 포함. 설정(모양·간격·길이·굵기·중앙마크)은 localStorage에 기억.
- **📏 재단 치수(시험)**: 재단기에 넣는 **구간 거리**를 재단선 옆에 인쇄(`12 · 90 · 6 · 90 · 12`).
  절대 좌표는 재단기 입력값이 아니라 쓰지 않는다. 기본 꺼짐이며 기억하지 않는다(보완 예정).
- 블리드 생성의 재단선이 임포징 설정을 공유(하드코딩 제거), 기본 체크 해제.

# 4. 화면·워크플로

- 좌측 '분석·처리옵션' 패널 **1332 → 497px**(섹션 접기/펴기 + 2열 배치 + 압축), 상단 스티키 줄 282 → 142px.
  패널 하단 손잡이로 세로 크기 조절(내부 스크롤·기억, 더블클릭 자동 복귀).
- 사이드바 **색의 의미 분리**: 노랑 fill = 실행 버튼 / 노란 테두리(2px inset) = 켜진 옵션.
- 챕터별 편집: 챕터 제목의 `✏ 편집`·우클릭 메뉴(편집·전체 선택·방향 맞춤·이동·삭제).
- 닫기 = **작업 저장하고 닫기 / 저장 안 하고 닫기 / 취소**(저장 완료 후에만 닫힘).
- 작업 저장 기본 폴더 = 문서 폴더(`pickSaveDir` 순수 함수로 분리).
- 흰 화면(렌더러 종료) 시 원인 안내 + 다시 열기.
- 파일명: `_중철북클릿` → `_중철`, 도구의 `_복제2부` → `_2up`.

# 5. 배포용 임포징 도구 (`dist/임포징도구.html`)

- 최신 빌더로 재생성 — 추출 목록에 `drawTrimMarks·drawCutDims·trimSizeMm`과 헬퍼
  (`uiYield·ensurePageContents·pageTrimInset`)가 빠져 있어 **그대로 다시 구웠다면 죽었을 상태**였다.
- pdf.js 인라인 → **원본 썸네일 레일 + 본문 미리보기**(원본/결과 탭), **드래그&드롭**,
  **프로파일 다중 선택 삭제**, **생성 진행 바**, 같은 설정 재생성 캐시(200쪽 336ms → 재클릭 1ms).

# 검증

| 대상 | 테스트 | 결과 |
|---|---|---|
| gs 자동회전 | `gs-autorotate.test.js` | 4/4 |
| 합치기(편집 상태 유지) | `append-merge.e2e.js` | 9/9 |
| 머리글 범위·시작번호 | `hf-scope.e2e.js` | 14/14 |
| 편집기 페이지별 문구 | `hf-editor.e2e.js` | 11/11 |
| 재단선·블리드·치수 | `imposition-marks.e2e.js` | 16/16 |
| 배포용 도구 | `imposition-standalone.e2e.js` | 68/68 |
| 사이드바 버튼·패널 | `sidebar-buttons.e2e.js` 31 · `sidebar-compact.e2e.js` 8 | 통과 |
| 챕터 편집·합본 방향 | `chapter-edit.e2e.js` 14 · `merge-orient.e2e.js` 7 | 통과 |
| 작업 파일·저장 | `workcache.e2e.js` 15 · `close-save.e2e.js` 8 · `savedir.e2e.js` 9 | 통과 |
| 프리셋 덮어쓰기 | `imp-profile.e2e.js` | 12/12 |
| 회귀 | smoke · workfile 17 · order-dialog 19 · auto-orient 19 · docstate 18 | 통과 |

## 이번에 배운 함정

- **테스트 하네스의 저장소는 실제 앱과 분리**돼 있다(`AppData\Roaming\Electron` vs `pdf-analyzer`).
  단, 하네스끼리는 공유하므로 기본값을 검사하는 테스트는 시작할 때 키를 지운다.
- **오프스크린 창은 프레임이 돌지 않아 CSS transition이 진행되지 않는다** — 색을 계산값으로
  검증할 땐 측정 전에 `transition: none`을 주입해야 한다.
- E2E 스크립트가 던지면(구문 오류 등) Electron이 종료되지 않고 매달린다 → `executeJavaScript`를
  try/catch로 감싸 실패로 끝낸다.
