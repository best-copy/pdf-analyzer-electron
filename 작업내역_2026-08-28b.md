# 2026-08-28 (2차) 작업내역

> ① 작업 파일(.pdfw) 열 때 재분석 제거 ② 합본 시 한 원고가 눕던 버그 ③ 챕터별 편집
> ④ 좌측 '분석·처리옵션' 패널 절반으로 + 버튼 재배치·높이 조절

---

# 1. 💾 작업 파일을 분석 없이 열기 (하이브리드 안)

## 무엇이 시간을 먹고 있었나
`.pdfw`는 이미 **적용본 PDF**를 담고 있어 미리보기·다운로드는 재계산이 없었다.
다시 돌던 것은 **왼쪽 페이지 그리드용 분석**뿐이고, 페이지당 산출물은
`{isColor, 크기}` + 썸네일 JPEG 하나 — 전부 저장 가능한 값이다.

## 방식 (사용자와 상의해 C안 채택)
- 저장: `captureAnalysisCache()` — 판정값 + **폭 300px·q0.72로 줄인 썸네일**을 한 덩어리로.
- 열기: `analyzePDF(file, tab, {cache})`가 **페이지 렌더 0회**로 pageResults 복원.
- 줌이 300px를 넘을 때만 **보이는 페이지**를 원해상도로 다시 렌더(`upgradeThumb`).
  🔍 크게 보기는 원래 pdf.js에서 새로 렌더하므로 영향 없음.
- **`ANALYSIS_CACHE_V`는 판정·썸네일 규칙이 바뀌면 반드시 올린다.**
  안 올리면 옛 판정이 되살아나 컬러 장수(=프린터 과금)가 틀린다.

## 함정 — "예전 파일은 재저장해도 계속 분석되던" 진짜 원인
저장 당시 **페이지를 지웠거나 빈 페이지를 넣었으면** 캐시가 원본 8쪽 중 7쪽만 담긴다.
처음 구현은 `캐시 쪽수 == PDF 쪽수`를 요구해 **통째로 폐기** → 재저장해도 계속 재분석.
→ 검증은 **버전·원본 바이트 길이**만 보고, **빠진 쪽만 그 자리에서 분석**하도록 바꿨다.
(계측으로 재현: 캐시 7 / 원본 8 → 캐시 미사용 → 수정 후 사용됨)

## 실측
| | |
|---|---|
| 12쪽 문서 분석 → 캐시 열기 | **2061ms → 69ms** |
| 캐시 용량 | 시험 원고 쪽당 ~3KB (밀도 높은 실무 원고는 10~25KB 예상) |
| 판정·쪽수·컬러 장수 | 저장 전과 완전 동일 |

구버전 `.pdfw`는 종전대로 분석하고, 안내에 "지금 [💼 작업 저장]으로 덮어 저장하면
다음부터 즉시 열립니다"를 덧붙인다(자동 덮어쓰기 없음 — 사용자 파일을 말없이 고치지 않는다).

# 2. 📐 파일을 합치면 한쪽이 왼쪽 90° 눕던 증상

**원인**: `자동 세로 맞춤`이 **문서 전체 다수결**로 기준 방향을 잡았다.
세로 6쪽 + 가로 4쪽을 합치면 가로 원고 4쪽 전부가 270°(왼쪽 90°) 회전 —
방향이 같은 파일끼리는 아무 일도 없어서 "될 때도 있고 안 될 때도" 있었다.

**수정**: 기준을 **챕터(파일)별**로. `orientGroups()`로 챕터별 다수 방향을 각각 구해
그 안에서 어긋난 쪽만 돌린다. 챕터가 없는 보통 문서는 그룹이 하나라 종전과 동일.
캐시 경로에서도 챕터 태깅이 빠지지 않도록 `tagChapters()`로 묶어 두 경로가 공유한다.

# 3. 📄 챕터별 편집

- 챕터 제목(그리드 헤더·사이드바)에 **`✏ 편집` 버튼**, 제목 **우클릭 메뉴** 6종:
  이 챕터만 편집 / 이 챕터 전체 선택 / 이 챕터 방향 맞춤 / 위·아래 이동 / 삭제.
- `editChapter(name)` = 편집 모드 진입 + `editSettings.scope = {mode:'chapter', chapter:name}`.
  기존 `byChapter` 구조를 그대로 쓰므로 크기·조판·여백·머리글이 그 파일에만 적용된다.

# 4. 좌측 '분석·처리옵션' 패널 정리

- **1332px → 497px** (썸네일이 y=517부터 화면 안에 보임). 상단 스티키 줄도 282 → 142.
- 방법: 섹션 제목 클릭 **접기/펴기**(기본: 분석결과·처리옵션만 펼침, 상태 기억)
  + **버튼 2열 배치** + 치수·간격 압축.
- 한 줄 2버튼 정리: `안전화 켜기·평탄화`, `워터마크·재단선`, `작업 열기·작업 저장`,
  E-book `표준·고화질`. **인쇄 대수 담기·E-book 가벼움(100dpi)은 삭제.**
  체크박스를 버튼으로 바꾸면서 **값은 숨은 체크박스가 계속 보관**한다
  (프리셋 저장/복원·`buildOutlinedBytes`가 그 체크박스를 읽으므로 형태만 바꾼 것).
- `.sbp-row2 > button { flex:1 1 0 }` — 버튼 종류와 무관하게 폭을 반씩(전엔 `.sbp-opt`만
  늘어나 작업 저장/열기가 84px로 쪼그라들었다).
- 안전화가 꺼져 있으면 방식 버튼(곡선화·완전 임베드)의 선택 색을 **무채색(dim)** 으로.
  켜면 **폰트 완전 임베드가 우선 선택**된다(곡선화는 용량 수십~수백 배·되돌릴 수 없음).
- 패널 하단 **손잡이로 세로 크기 조절**(내부 스크롤·높이 기억, 내용 높이까지 내리면 자동 복귀).

# 검증

| 대상 | 테스트 | 결과 |
|---|---|---|
| 분석 캐시(저장·복원·부분 캐시·버전 무효화·썸네일 보정) | `workcache.e2e.js` | 15/15 |
| 합본 자동 방향(챕터별 기준) | `merge-orient.e2e.js` | 7/7 |
| 챕터별 편집(버튼·우클릭 메뉴·범위 마스크) | `chapter-edit.e2e.js` | 14/14 |
| 패널 높이·접기/펴기 | `sidebar-compact.e2e.js` | 8/8 |
| 버튼 2열 배치·토글 동기·폭·높이 조절 | `sidebar-buttons.e2e.js` | 24/24 |
| 기존 | order-resize 14 · savedir 4 · order-dialog 19 · workfile 17 · auto-orient 19 · smoke | 전부 통과 |

E2E는 오프스크린 BrowserWindow에 **실제 index.html + preload**를 띄우고 포인터·클릭
이벤트를 주입해 확인한다(레이아웃은 좌표로, 상태는 전역 변수로 검증).

## 메모
- 테스트가 `localStorage`(접힘 상태 등)를 공유하므로, 기본값을 검사하는 테스트는
  시작할 때 키를 지우고 `initSbSections()`로 초기화한다(한 번 물렸던 함정).
