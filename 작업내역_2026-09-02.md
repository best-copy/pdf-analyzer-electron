# 2026-09-02 작업내역

> **런타임 성능 최적화**(분석 −18% · 적용 −59%)와 **E-book 시안 대폭 개편**
> (단면 인쇄 형식 · 트윈링 제본 · 넘김 깜박임 완전 제거 · 눕혀 보기),
> 그리고 **폰트 출력 안전화 기본 켜짐**.
>
> 이번 작업의 원칙: **눈이 아니라 수치로 확정한다.** 새 하네스 10종을 만들어
> 프레임 단위 밝기·구간별 시간·컬러 판정을 실측했고, 실패한 접근도 함께 남긴다.

---

# 1. 런타임 성능

프로파일 하네스 `scripts/test/perf-profile.e2e.js`(실제 문서를 인자로 받아 구간별 시간 측정) 신규.
기준 문서: RICOH 서비스 매뉴얼 **2403쪽 / 87.4MB**. PC = 9800X3D 16스레드 / 62GB / RX7800XT / NVMe.

## 1-1. 병목 위치 (실측)

| 구간 | 이전 |
|---|---|
| 기동 — `npm start` 0.45초 / `dist/win-unpacked` exe **0.26초** | 문제 아님 |
| 기동 — 포터블 exe | **5.7~6.7초** (매 실행 73MB→326MB LZMA 해제, 캐시 안 함) |
| 분석 | **23.7초** (쪽당 10ms, 메인스레드 여유 18% = 포화) |
| 적용 | **11.4초** — 그중 `outDoc.save()`가 **7.3초(64%)** |

`node_modules/pdf-lib` 22MB는 main·preload 어디에서도 require하지 않는 **사장품**
(렌더러는 vendored 사본을 쓴다). 포터블 배포 문제는 사용자가 win-unpacked 폴더로 쓰므로 보류.

## 1-2. 실패한 최적화 — 다시 시도하지 말 것

**동시성 8→16 + pdf.js 보조 문서 3→7 = 23.7초 → 25.4초로 악화.**
CPU 코어가 병목이 아니다. 보조 문서는 각자 바이트 사본(87MB)을 갖고 문서를 통째로 다시 파싱한다.

## 1-3. 적용한 것

### ① 2단계 썸네일

예전엔 전 쪽을 최대 줌 화질(477px)로 미리 구웠다. 이제 `thumbDisplayPx()`(지금 줌 × DPR)로만 굽고,
줌을 키우면 보이는 쪽만 `upgradeThumb`이 `{full:true}`로 다시 굽는다(작업파일 저해상 보정 장치를 재사용).
→ **23.7초 → 19.8초**

- ⚠ **함정 1 — 너무 줄이면 오히려 느려진다.** 240px까지 줄이면 A는 16.3초로 빨라지지만,
  **이미지가 많은 원고(436쪽·98MB 법령)는 83초 → 97초로 18% 악화**(순서를 바꿔도 재현).
  원인은 전수 검사가 아니다(그건 1초) — 큰 그림을 많이 축소할 때 브라우저가 더 비싼 리샘플링
  경로를 탄다. → **하한 `fullScale*0.75`**(A4에서 358px)면 이미지 문서는 84초로 원래와 같다.
- ⚠ **함정 2 — 해상도가 컬러 판정을 바꾼다(과금!).** 성긴 표본(15k)은 작은 컬러 요소를 놓친다.
  → 저해상 경로는 `sampleStep=1`로 **전 픽셀을 훑는다**(비용 1초).

| 방식 | 컬러 판정 | 정답 대비 | 분석(A) | 분석(B) |
|---|---|---|---|---|
| 정답 기준 (477px + 전 픽셀) | 1932 | — | 26.7초 | — |
| 예전 배포본 (477px + 15k 표본) | 1929 | **−3쪽** | 23.7초 | 83초 |
| 240px + 전수 | 1929 | −3쪽 | 16.3초 | 97초 ✗ |
| **채택: 358px + 전수** | **1931** | **−1쪽** | **19.8초** | **84초** |

즉 새 방식이 **예전보다 빠르면서 더 정확**하다.

### ② toBlob 동시 인코딩 상한

toBlob은 끝날 때까지 원본 캔버스(장당 1.3MB)를 붙잡는다. 상한이 없어 2000장 넘게 쌓이고
인코딩 대기가 장당 1.1초까지 밀렸다. `thumbSlot`/`thumbRelease`(상한 = 코어×2, 최대 32)
→ **메인스레드 여유 18% → 47~56%** (총 시간은 그대로, 분석 중 UI 반응이 살아난다).

### ③ 워커 지연 생성

예전엔 생성자에서 코어 수만큼 즉시 생성 = 문서를 열기도 전에 gray 16 + assemble 16 = **32개**.
assemble 워커는 하나가 pdf-lib+fontkit(1.28MB)을 importScripts 한다.
이제 `_spawn()`으로 첫 작업 때 하나씩, 상한까지만 늘어난다.

### ④ pdf-lib `save()`의 tick 대기 제거 — 가장 큰 단일 개선

`save()`는 `objectsPerTick`(기본 **50**)개마다 `setTimeout(…,0)`으로 양보한다. 브라우저가 중첩
setTimeout을 최소 4ms로 조이고 큰 버퍼 GC까지 겹쳐 **tick 하나당 실측 25~36ms**.

| 설정 | 시간 | 결과물 |
|---|---|---|
| 기본(50) | **10,148 ms** | 117.1MB |
| objectsPerTick 500 | 788 ms | 117.1MB |
| objectsPerTick 5000 | 240 ms | 117.1MB |
| tick 없음 | **126 ms** | 117.1MB |

**실제 직렬화는 126ms, 나머지 99%가 대기.** 결과 바이트는 tick 수와 무관하게 동일하다.
→ 공용 `savePdfDoc(doc, extra)` / `pdfSaveOpts(doc)`(app-core.js)로 **37곳 전부** 통일.
문서의 간접객체 수(`indirectObjects.size`, O(1))를 보고 "한 번 멈춤 ≤ 200ms"가 되게 나눈다.

- ⚠ 각 호출부의 **기존 옵션 의미를 보존**했다 — `{useObjectStreams:false}`만 있던 3곳은
  `updateFieldAppearances`가 pdf-lib 기본값 **true**였고, 인자 없는 `.save()`는 **둘 다 true**였다.
- ⚠ 워커(worker-assemble.js)·편집기 창(editor.html)은 별개 스코프라 **같은 헬퍼를 복제**해 뒀다.
  한쪽만 고치면 저장 성능이 갈라진다.
- ⚠ 캔버스의 `ctx.save()`는 이름이 같아 일괄 치환에서 제외.

진단 하네스: `scripts/test/save-tick.e2e.js`.

## 1-4. 결과

| | 이전 | 이후 |
|---|---|---|
| `save()` (2403쪽·117MB) | 10,148 ms | **162 ms** (63배) |
| 분석 (2403쪽·87MB) | 23.7초 | **19.5초** |
| **적용 — A** | 11.4초 | **4.7초** |
| **적용 — B** (436쪽·98MB) | — | **2.8초** |
| 메인스레드 여유(분석 중) | 18% | **50%** |

남은 적용 4.7초는 대부분 **잉크 정규화 프리웜 완료 대기(약 3.9초)** — 저장이 아니라 실제 변환 작업이다.

**검증**: 기존 E2E 16종 + node 테스트 8종 전부 통과
(imposition-perchapter 26 · imposition-standalone 68 · chapter-edit 14 · workfile 17 · ebook-proof 84 …).

---

# 2. E-book 시안 — 인쇄 형식·제본

## 2-1. 📄 단면(한 면 인쇄) 형식

생성 옵션 `_ebOpts.view` = spread(기본) | single → 페이로드 `meta.view`.

**단면은 "한 쪽만 크게"가 아니다** — 실제 단면 인쇄물처럼 **펼치면 왼쪽이 백지, 오른쪽이 인쇄면**이다.
구현은 펼침면 목록만 바꾼다: `ebookSoloSpreads(n,bind)` → `[[null,0],[null,1]…]`(우철은 반전).
뷰어의 `single`(모바일 세로 = 한 상자)과는 **다른 개념** — SOLO는 화면 방향을 강제하지 않는다.

- **넘길 때 종이 뒷면이 백지** — 예전에는 뒷면에 다음 쪽을 그려 아직 오지 않은 페이지가 비쳐 보였다.
  `backImg=single?(SOLO?null:to[0]):(to[leafR?0:1])` — 단면은 `to[0]=null`이라 저절로 백지가 된다.
- **백지 면 스타일**: `.pg.blank`는 양면 책의 '표지 맞은편 빈 자리'용이라 거의 투명하다.
  단면에서는 그 면이 **앞장의 뒷면 = 실제 흰 종이**이므로 `.solo .pg.blank{background:#fff}`.
  안 하면 넘김이 끝나는 순간 왼쪽 종이가 사라져 보인다.
- **첫 장·마지막 장은 한 면만** — 첫 장 앞에는 넘긴 장이 없고, 마지막 뒤에는 남은 장이 없다.
  가운데 쪽들은 앞장의 뒷면이 실제로 있으므로 흰 종이가 그대로 보인다.

## 2-2. 🌀 트윈링(더블와이어) 제본

`_ebOpts.bindStyle` = book(기본) | twinring → `meta.bindStyle`.

트윈링은 낱장을 타공해 철사로 엮으므로 **책등에 골이 없다**
→ `.twinring .gut,.twinring .gtop{display:none}`, `addSpineShade`도 건너뛴다.
대신 `addRings(sp)`가 철사 고리(`.ring`)와 타공(`.punch`)을 그린다.

- **타공 수는 실제 mm로 센다** — 3:1 피치 실무 기준 **A4(297mm) 34홀**, 판형은 비례 환산
  (A5 24 / A3 48). 화면 픽셀 기준이면 확대·축소에 따라 홀 수가 흔들린다.
- 타공은 z-index 1(종이 구멍), 고리는 z-index 2 — 철사가 구멍을 지나가는 것처럼 보인다.
- 넘김 중에는 `.ringwrap`을 맨 뒤로 옮겨 **낱장보다 앞**에 둔다(철사가 종이를 꿰고 있으므로).
- ⚠ `addRings`는 **무대에 붙인 뒤**에 불러야 한다 — `stage.appendChild(d)` 앞에서 부르면
  `offsetLeft/offsetWidth`가 전부 0이라 고리가 왼쪽 위 한 점에 뭉친다(실제로 밟았다).

## 2-3. 닫힌 책 모양 — 첫 장은 앞표지만, 마지막은 뒤표지만

빈 면을 **투명하게만** 하면 자리가 남아 여전히 펼쳐 놓은 모양으로 보인다(사용자 재신고).
그렇다고 **자리를 지우면** 표지↔펼침면 전환에서 페이지가 순간 이동한다(고정 좌표 밝기 **218/255 급변**).

→ 종이가 없는 면도 **자리는 그대로 두고 `visibility:hidden`**(`.pg.nosheet`)으로 감추고,
`.spread`에 `translateX(±면폭/2)`를 걸어 **보이는 면을 가운데로** 옮긴다.
`.spread{transition:transform 260ms}` 덕분에 표지가 펼침면으로 **미끄러지듯** 열린다.

| 화면 | 보이는 면 | 가운데 어긋남 |
|---|---|---|
| 첫 장 (앞표지) | 1면 | −1px |
| 가운데 펼침면 | 2면 | 0px |
| 마지막 장 (뒤표지) | 1면 | 0px |

- **빈 면이 "실제 종이"인지 판정**: 왼쪽 빈 면은 맞은편 쪽 **앞**에, 오른쪽 빈 면은 맞은편 쪽 **뒤**에
  종이가 있어야 실물이다. 이 한 규칙으로 양면(첫·마지막만)과 단면(첫·마지막만, 가운데는 유지)이
  함께 처리된다.
- ⚠ 부호: 왼쪽이 비면 왼쪽으로(−), 오른쪽이 비면 오른쪽으로(+). 반대로 넣으면 어긋남이 두 배가 된다(밟음).
- ⚠ 자리가 사라지면 **자식 인덱스로 면을 찾던 코드가 깨진다** → 각 면에 `data-side`를 달고
  `beginTurn`은 그 값으로 찾는다. 그쪽에 종이가 없으면 맞은편에서 자리를 옮겨 잡는다.
- ⚠ 타공은 `.nosheet`인 면에는 그리지 않는다 — 안 그러면 없는 종이에 구멍이 뜬다.
- ⚠ `.pg.nosheet{visibility:hidden}`을 `.twinring` 아래에만 두면 **단면+무선책자에서 안 먹는다**(밟음).

---

# 3. E-book 넘김 깜박임 — 완전 제거

프레임 단위로 재야 보였다. `scripts/test/ebook-turn-frames.e2e.js`(`beginFrameSubscription`으로
합성 프레임 수신)와 `ebook-turn-pixels.e2e.js`(영역 캡처) 신규.

**원인은 넷이었다.**

1. 낱장의 들리는 그늘이 **책등 쪽에도 바닥값(0.26)** 을 깔아, 안 움직이는 부분까지 어두워졌다가
   종이가 지나가는 순간 튀었다 → 그늘 모양을 `grad(u²)`로 바꿔 **책등에서 0**.
2. **뒷면 그늘**(`bb=0.30*(1-p)`)이 접힌 종이가 책등을 스칠 때 **−18/255**로 툭 어둡게 했다 →
   뒷면은 반대편에 평평하게 내려앉는 면이라 그늘이 없어야 아래 페이지와 밝기가 같다 → `bb=0`.
3. **착지 교체**: 원근 때문에 착지 직전 낱장은 실제 페이지보다 조금 크고 기울어 있다. 그대로
   `render()`로 갈아치우면 크기·그늘이 함께 튄다 → `landLeaf(T)`가 **최종 펼침면을 먼저 깔고**
   낱장만 그 위에서 140ms 녹여 없앤다(`.leaf.land{transition:opacity 140ms}`).
4. **트윈링**: 페이지의 골은 CSS로 숨겼는데 **넘어가는 낱장에는 골 그늘을 그대로 그리고 있었다**
   (실측 opacity 0.25가 생겼다 사라짐) → `stripFace`에서 `!TWIN`일 때만 만들고,
   `poseLeaf`도 `noGut=(T.spineTop||TWIN)`로 막는다.

**결과 — 책등 밝기 프레임 간 최대 급변**

| | 이전 | 이후 |
|---|---|---|
| 책자·펼침 | 48 | **0** |
| 트윈링 | 깜박임 | **0** |
| 책자·단면 | — | 4 |

- ✗ **실패한 시도**: 그늘 농도를 **조각 각도**(cos)로 주기 — 접힌 바깥 조각이 짙어져 **+53으로 악화**.
- ⚠ 표지에서 넘길 때 나오는 큰 값은 **책이 미끄러져 열리는 정상 동작**이다.
  순수한 넘김만 보려면 `--nocover`로 표지 단독 없이 시작할 것(안쪽 펼침면 실측 0).

---

# 4. E-book 눕혀 보기 (⟳)

펼침면 전체를 **오른쪽으로 90°** — `body.lay90` + `drawSpread`가 `rotate(90deg) translateX(...)`를 넣는다.
그림을 다시 굽지 않아 즉시 전환되고 넘김·확대·목록이 그대로 동작한다. 손봐야 할 곳은 두 군데뿐:

1. `fit()`에서 화면의 가로세로를 맞바꿔 계산
2. 끌기 축 — 페이지의 오른쪽이 화면 **아래**가 되므로 `dragProgress`는 `+(clientY-y0)`,
   잡은 면 판정(`grabR`)과 튕김 판정도 세로로

세로 화면(휴대폰)에서 가로 원고가 실제로 커진다(820×1000 창 실측: 페이지 폭 272 → 411px).
가로 모니터에서 작아지는 것은 기하학적으로 당연하다.

**넘김 표시와 버튼 배치**

- 화살표는 띠의 **정확한 가운데**(오차 ≤3px). 눕혀 보기 버튼은 흐름에서 빼서(absolute)
  중심에서 **104px** 떨어뜨린다 — 붙어 있으면 넘기려다 잘못 눌린다.
- 정상(가로 화면) = 화살표 **아래**, 눕혀 보기 = 화살표 **오른쪽**.
- 눕혀 보기에서는 띠가 페이지 위에 겹쳐 버튼이 내용을 가린다 →
  `.lay90 .layfab{opacity:0}` + `.lay90 .nav:hover .layfab{opacity:1}`로 **근처에 가야 나타난다**.
  투명해도 클릭은 되고, hover가 없는 기기는 상단 툴바 버튼으로 되돌린다.
- ⚠ 화살표를 `<i>`로 감싸면서 `e.target.classList.contains("nav")`가 실패 → `closest(".nav")`로 교체.
  안 고쳤으면 넘김 표시를 눌러도 끌기로 처리됐다.

`↺ 90° 회전`(쪽 그림 자체를 돌리던 기능)은 사용자 요청으로 **삭제** — 눕혀 보기로 대체했다.

---

# 5. ✒ 폰트 출력 안전화 — 기본 켜짐

`_outlineEnabled`를 `localStorage 'outlineOn'`에서 읽고 **기본 true**, 방식 기본은 `embed`(완전 임베드).
폰트가 빠진 채 출력이 나가는 사고가 가장 비싸고, 완전 임베드는 실측 0.3초·+35KB(한글 40쪽)로
부담이 거의 없다. 사용자가 끄면 그 선택을 기억한다.

⚠ 부팅 시 버튼 표시를 맞추는 초기화는 **app-ui.js**에 둬야 한다 — `activateChip`이 그 파일에 있어
app-process.js의 최상위 즉시 실행문에서 부르면 ReferenceError로 부팅이 깨진다(실제로 밟았다).
`setOutlineEnabled`를 부르면 안내 문구·프리웜까지 돌아가므로 초기화는 **표시만** 맞춘다.

---

# 6. 새 검증 하네스

| 파일 | 무엇을 |
|---|---|
| `perf-profile.e2e.js` | 실제 문서로 분석·사이드바·미리보기·적용 구간별 시간 |
| `save-tick.e2e.js` | pdf-lib `save()`의 objectsPerTick별 시간·결과 동일성 |
| `ebook-turn-frames.e2e.js` | 합성 프레임 단위 책등 밝기 (한 프레임짜리 튐 검출) |
| `ebook-turn-pixels.e2e.js` | 넘김 중 가로선 밝기 (`--wait`/`--step`/`--nocover`) |
| `ebook-turn-flicker.e2e.js` | 넘김 중 DOM 상태·그늘 농도 흐름 |
| `ebook-single.e2e.js` | 단면 구성(첫·가운데·마지막) |
| `ebook-solo-back.e2e.js` | 단면 낱장 뒷면이 백지인지 |
| `ebook-solo-blank.e2e.js` | 단면 왼쪽 흰 종이가 유지되는지 (픽셀) |
| `ebook-twinring.e2e.js` | 고리·타공 수·닫힌 책·골 없음 |
| `ebook-lay90.e2e.js` | 눕혀 보기 회전·크기·넘김 표시 위치·버튼 |
| `outline-default.e2e.js` | 폰트 안전화 기본값·기억·재시작 유지 |

**하네스 함정**

- 뷰어 스크립트는 IIFE 안이라 `go()`가 전역이 아니다 — 넘김은 keydown(ArrowRight) 디스패치로.
- `capturePage`를 루프로 돌리면 너무 느려 매달린다 — 좁은 rect로 한 줄만 캡처할 것.
- `beginFrameSubscription` 프레임은 **기기 픽셀** 크기 — CSS 좌표에 배율을 곱해야 한다.
  그래도 13fps 정도라 한 프레임짜리 튐은 놓칠 수 있다.
- 첫 캡처는 합성 전이라 비어 나온다 — 워밍업으로 한 번 버릴 것.
- 진짜 `:hover`는 `webContents.sendInputEvent({type:"mouseMove"})`로만 켜진다.
- 쪽 이동(`goPage`)은 **애니메이션 넘김**이라 고정 대기로는 중간 상태를 읽는다 —
  `.leaf`가 사라질 때까지 폴링할 것.

---

# 7. 검증 요약

```
npm run smoke                       통과
ebook-proof.test.js                 84 통과
ebook-single.e2e.js                 16 통과
ebook-solo-back.e2e.js               5 통과
ebook-solo-blank.e2e.js              5 통과
ebook-twinring.e2e.js               25 통과
ebook-lay90.e2e.js                  30 통과
outline-default.e2e.js              14 통과
imposition-perchapter.e2e.js        26 통과
imposition-standalone.e2e.js        68 통과
chapter-edit / workcache            14 / 15 통과
inknorm-default / sidebar-buttons   10 / 31 통과
close-save / workfile / docstate    13 / 17 / 18 통과
넘김 급변(안쪽 펼침면)               책자 0 · 트윈링 0
```

포터블 앱 동기화(`dist/win-unpacked/resources/app/`) 체크섬 대조 완료,
독립 도구 `dist/E북시안도구.html` 재빌드 완료.
