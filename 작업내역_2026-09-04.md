# 2026-09-04 작업내역

> **🔤 폰트 완전 임베드 — 느림의 원인을 다시 찾고, 조용한 사고 두 건을 막았다.**
>
> 출발점은 "완전 임베드가 너무 오래 걸린다"였다. 파 보니 병목은 이미지가 아니라
> **불필요한 gs 실행**이었고, 그 과정에서 **한글 폰트가 설치돼 있는데도 대체되던 문제**와
> **그 대체를 아무도 감지하지 못하던 문제**가 함께 드러났다.
>
> 기준 문서: `수학_2up.pdf`(40쪽 2.5MB, 임포징 2up 원고) · Ghostscript 10.07.1.

---

# 1. 개선안 B는 **기각** — 실측이 반대였다

첫 진단은 "gs가 모든 JPEG를 풀어 Flate로 다시 굽고 있어 느리다"였다.
근거는 main.js 주석의 기존 실측 기록:

> `-dPassThroughJPEGImages`는 필터를 명시하면 gs가 무시한다

**gs 10.07.1에서 재실측한 결과 이 주석은 사실이 아니다.**

| 원고 | 현재(무손실 강제) | 필터 지정 제거 |
|---|---|---|
| 수학_2up.pdf | 4.64초 / 2.91MB | 4.96초 / 2.14MB — **무손실 이미지 7개가 JPEG로 재압축** |
| JPEG 24장(4.31MB) | 1.21초 / 4.32MB (`/DCTDecode` 4개 유지) | 1.21초 / 4.32MB |

원본 JPEG는 지금도 그대로 통과한다. 필터 지정을 빼면 **속도 이득 0 · 화질 손실만** 생긴다.
→ 이미지 관련 gs 플래그는 **손대지 않았다.** main.js 주석과 메모리를 실측치로 정정.

---

# 2. 폰트 스캔이 Form XObject를 못 보고 있었다

`수학_2up.pdf`는 임포징을 거쳐 내용이 **Form XObject 안**에 들어 있다.
페이지 `Resources`만 훑던 기존 코드는 이 문서에서 폰트를 **0종**으로 봤다.

| 스캔 방식 | 임베드 | 미임베드 |
|---|---|---|
| 페이지 Resources만 (기존) | 0종 | 0종 |
| **재귀 (페이지 + XObject + Pattern + 주석 /AP)** | **6종** | **1종 (KoPubWorldDotum,Bold — 40쪽 전부)** |

**영향 두 곳:**
- 프리플라이트가 이 원고에 "폰트 이상 없음"이라고 답했다(미임베드 폰트가 있는데도).
- 완전 임베드가 대체된 폰트의 사용 페이지를 못 찾아 이미지화를 건너뛰었다.

→ 공용 `scanDocFonts(doc)` 신설(순환 참조 방지·깊이 상한 8). 프리플라이트와 완전 임베드가 함께 쓴다.

---

# 3. 대체 폰트 감지 정규식이 한글 폰트를 통째로 놓치고 있었다

gs 10의 CID 폰트 메시지에는 **괄호가 없다**:

```
Loading CIDFont KoPubWorldDotum,Bold substitute from %rom%…/DroidSansFallback.ttf
```

기존 정규식은 `\(or substitute\)`만 받았다 → **감지 0건.**
즉 이 원고는 지금까지 **쪽번호 글꼴이 조용히 바뀐 채로** 저장되고 있었고, 경고조차 뜨지 않았다.

→ `(?:\(or substitute\)|substitute)` 로 수정.

---

# 4. gs는 CID(한글) 폰트를 `-sFONTPATH`로 찾지 않는다

**"KoPubWorld돋움체 Bold는 설치돼 있는데 왜 임베드가 안 되나?"** — 실제로 설치돼 있었다.

`C:\Windows\Fonts\KoPubWorld Dotum Bold.ttf` 존재. 그런데도 대체됐다.
원인은 gs의 폰트 탐색 구조다:

- `-sFONTPATH` → **일반 폰트용 Fontmap만** 만든다.
- **CID 폰트는 `cidfmap` 표에서만** 찾는다. 앱은 그 표를 준 적이 없다.

여기에 함정이 하나 더 있다 — **레지스트리 표시 이름은 한글이다.**

```
레지스트리:  KoPubWorld돋움체 Bold  →  KoPubWorld Dotum Bold.ttf
PDF 요구:    KoPubWorldDotum,Bold
```

`fonts:list`(레지스트리) 기반 매칭으로는 영원히 안 맞는다.
**폰트 파일의 name 테이블**을 읽으면 Family `KoPubWorldDotum` + Subfamily `Bold`,
PostScript `KoPubWorldDotumBold` — 정확히 맞는다.

## 4-1. 처리

미임베드 CID 폰트를 이 PC의 설치 파일로 잇는 **임시 cidfmap**을 만들어 `-I<dir>`로 넘긴다.

```
/KoPubWorldDotum,Bold << /FileType /TrueType /Path (C:/Windows/Fonts/KoPubWorld Dotum Bold.ttf)
                         /SubfontID 0 /CSI [(Korea1) 1] >> ;
```

- 폰트 색인은 **name 테이블 직접 읽기**(헤더 + name 테이블만 → 파일당 수 KB, TTC는 SubfontID까지).
  시스템 폰트 폴더 + 사용자 계정 폰트 폴더 모두.
- `/CSI`는 PDF의 DescendantFont **CIDSystemInfo**를 그대로 쓴다.
- ⚠ **Identity 정렬은 제외** — CID가 원본 글리프 번호라 다른 폰트로 이으면 오히려 엉뚱한 글자가 된다.
- 프로브도 **같은 조건**으로 돌린다(안 그러면 판정이 어긋난다).

## 4-2. 안전성 실측

| 상황 | 결과 |
|---|---|
| 우리 cidfmap이 gs 기본표를 가림 + 매핑 없는 CID 폰트 | 종전대로 DroidSansFallback 폴백 (정상) |
| 매핑 경로가 틀림(오타·삭제) | 오류 없이 폴백, 종료코드 0 |

## 4-3. 결과 검증

- 합성 Adobe-Korea1 원고(`UniKS-UTF16-H`)로 **한글 글리프 매핑 확인** — "한글 임베드 시험 韓 1234" 정상.
- 결과물 vs 원본(진짜 폰트로 렌더) 200dpi 대조: **평균 오차 0.006/255** (사실상 동일).
- 대체본(v0) vs 임베드본(v3) 차이 영역은 정확히 **쪽번호 "- 1 -"** 하나 — 글꼴이 실제로 달랐다.

---

# 5. 속도 — A(건너뛰기) · C(사전 확정)

## 5-1. A. 이미 전부 임베드된 문서는 굽지 않는다

완전 임베드의 목적(다른 PC·출력기에서 글꼴이 안 바뀜)은 **서브셋 임베드로도 이미 충족**된다.
비서브셋이 의미 있는 건 출력소가 그 파일에서 글자를 직접 고칠 때뿐이다.

→ 미임베드 폰트 0종 **+ 평탄화 꺼짐**이면 입력 바이트를 그대로 돌려준다.

| | 이전 | 이후 |
|---|---|---|
| 이미 전부 임베드된 문서 | 4.9초 (gs 1회) | **0.07초 (gs 0회)** |

평탄화가 켜져 있으면 gs가 할 일이 남으므로 건너뛰지 않는다.

## 5-2. C. 대체될 폰트를 gs 실행 **전에** 확정

예전엔 pdfwrite 1차 실행 로그를 보고서야 알 수 있어 **gs를 두 번** 돌렸다.
`-sDEVICE=nullpage`는 PDF를 쓰지 않고 해석만 하므로 훨씬 싸다:

| 방식 | 시간 |
|---|---|
| pdfwrite (본 실행) | 4.6초 |
| nullpage 전체 40쪽 | 1.7초 |
| **nullpage 1쪽** | **0.21초** |

→ 미임베드 폰트가 **처음 쓰이는 쪽만**(최대 6장) 훑는다. IPC `gs:probeFonts` 신설.
프로브를 못 쓰면(구버전 preload·gs 오류) 종전 사후 감지로 폴백한다.
**pdfwrite는 이제 항상 1회.**

## 5-3. 이미지화 상한 (신규 가드)

대체 폰트 페이지를 300DPI로 굳히는 처리에 상한을 뒀다: `min(20, max(3, 전체의 25%))`쪽 이하.

이 원고는 쪽번호 폰트 하나가 **40/40쪽**에 걸려 있어, 상한이 없으면 교재 전체가 이미지가 된다
(300DPI JPEG **36MB**, 텍스트 소실). 초과 시엔 굳히지 않고 "그 폰트를 설치하라"고 경고만 한다.

⚠ 이미지화한 쪽은 300dpi 대조 시 평균 오차 1.75/255지만 **진한 픽셀이 83%로 줄어든다**
(헤어라인이 1px 미만이라 회색으로 렌더). 어디까지나 최후 수단이다.

---

# 6. 안내 문구

결과에 따라 세 갈래로 알린다(`outlineResultNote()`).

| 상황 | 문구 |
|---|---|
| 이미 전부 임베드 | ✔ 이미 모든 폰트(N종)가 파일에 들어 있어 다시 굽지 않았습니다 |
| 설치본으로 이음 | 🔗 파일에 빠져 있던 폰트(…)를 이 PC에 설치된 같은 폰트로 찾아 실었습니다 |
| 소수 쪽 이미지화 | 🖼 이 PC에 없는 폰트(…) 사용 N쪽(…p)은 300DPI 이미지로 굳혔습니다 |
| 대부분이 대상 | ⚠ … gs 대체 글꼴로 나갑니다 → 그 폰트를 이 PC에 설치한 뒤 다시 저장하세요 |

사이드바 설명도 갱신: "이미 모든 폰트가 들어 있는 문서는 그냥 건너뜁니다(대기 0초)".

---

# 7. 검증

`scripts/test/font-embed-fast.e2e.js` 신규 — **26개 전부 통과**.

| 단계 | 확인 |
|---|---|
| 1 | 재귀 스캔이 XObject 안 폰트를 찾는다 (페이지 단위는 0종) |
| 2 | nullpage 프로브 동작·3초 이내 |
| 3 | pdfwrite 1회 · 설치본으로 이음 · 이미지화/경고 없음 · 잔여 미임베드 0 |
| 4 | 캐시 적중 즉시 + 경고/이미지화 정보 복원 |
| 5 | 이미 전부 임베드 → gs 0회 · 입력 그대로 · 0.07초 |
| 6 | **진짜로 없는** 폰트가 1/10쪽 → 그 쪽만 이미지화 |
| 7 | **진짜로 없는** 폰트가 10/10쪽 → 이미지화 없이 경고 |

⚠ 하네스 함정 두 가지(기록):
- main.js의 gs 핸들러는 **소스에서 추출해 eval**로 등록한다(복사본은 플래그가 드리프트한다).
- gs 호출 횟수는 **메인에서** 세야 한다 — contextBridge로 넘어간 `electronAPI`는 얼려 있어
  렌더러에서 함수를 갈아끼워도 조용히 무시된다(처음에 0회로 오판했다).

그 외: `npm run smoke` 통과 · 이미지화 페이지 gs 픽셀 대조 · 포터블 앱 동기화.

---

# 8. 함께 커밋된 이전 세션 작업

- **pdf.js 사전 정의 CMap**(`src/libs/cmaps/`, `openPdfDoc` 공용화, preload `cmapDir`) —
  아크로뱃이 넣은 비임베드 CID 폰트 머리글·바닥글이 화면에서 사라지던 문제. 편집기 창은
  별개 스코프라 `editor.html`에 같은 내용을 유지해야 한다.
- **E-book 독립 도구에 한국어 CMap 인라인**(`build-ebook-standalone.js`, 약 190KB).
- **`pageEditedByUser`** — 문서 열 때 도는 '방향 자동 맞춤'이 '적용 필요' 깜박임을 켜던 문제.
- 테스트 추가: `cmap-cid-font` · `apply-outline-state` · `ebook-standalone` · `gs-image-lossless`,
  `ebook-lay90` 포인터 치우기 보정.
