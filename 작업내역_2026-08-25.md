# 작업내역 2026-08-25 — 환경 정비 · 셋업 스크립트 수정 · 안내문구 정리

전날(2026-08-24) 기능 8건 작업([작업내역_2026-08-24.md](작업내역_2026-08-24.md))에 이어,
개발 환경 정비와 셋업 스크립트 결함 수정, 시작 화면 문구를 정리했다.

---

## 1. Ghostscript — 이미 설치돼 있었다 (오판 정정)

전날 "이 PC에 Ghostscript가 없다"고 판단해 `C:\Program Files\gs\gs10.07.1`에 설치했으나,
**사실 `D:\Ghostscript\gs10.07.1`에 이미 설치돼 PATH에 등록**되어 있었다.
`C:\`만, 그것도 깊이 5까지만 훑고 **PATH를 확인하지 않은** 것이 원인.

- 중복본(`C:\Program Files\gs`, 64MB) 제거
- 앱은 `main.js findGhostscript()`의 PATH 폴백으로 D: 설치본을 정상 사용 (실측 확인)
- **교훈**: 외부 도구 설치 여부는 `Get-Command`(PATH)와 다른 드라이브까지 확인할 것

## 2. 죽은 PATH·레지스트리 항목 정리

| 대상 | 처리 |
|---|---|
| PATH(시스템) `D:\Ghostscript\gs10.04.0\bin` | 제거 (폴더 없음) |
| PATH(시스템) `D:\FreeFlow_Remote_Print_Server\jre\bin` | 제거 (폴더 없음) |
| 제거 항목 `GPL Ghostscript 10.04.0` | 제거 |
| `HKLM\SOFTWARE\GPL Ghostscript\10.04.0` | 제거 |

안전장치: 항목이 **정확히 1개만 줄고** 핵심 시스템 경로(`system32`·`Windows`·`Wbem`)와
살아 있는 gs 경로가 **남아 있을 때만** 반영하도록 했다.

**⚠ PATH 수정 함정 두 가지 (실제로 밟음)**

1. `[Environment]::SetEnvironmentVariable('Path', …, 'Machine')`은 값 형식을
   **REG_EXPAND_SZ → REG_SZ로 바꿔 버린다**. 레지스트리 키를 직접 열어
   `SetValue(…, RegistryValueKind::ExpandString)`으로 써야 한다. (되돌려 놓음)
2. 항목 실존 판정은 반드시 `ExpandEnvironmentVariables`로 확장한 뒤 `Test-Path` 할 것.
   안 그러면 `%SystemRoot%\…` 같은 정상 항목을 죽은 항목으로 오판해 지운다.

`%APPDATA%\npm`은 폴더가 없어도 **남겨 두었다** — npm 전역 설치 시 생기는 정상 항목이다.

되돌리기용 백업: `.backup-gs-cleanup/` (PATH 원문 + 환경변수 레지스트리 `.reg`, gitignore)

## 3. 셋업 스크립트 결함 2건 (`scripts/install-portable.ps1`)

### 3-1. Ghostscript 설치 단계가 동작하지 않았다

- `winget install --id ArtifexSoftware.GhostScript` — **패키지가 winget 저장소에서 사라졌다**
  (현재 Artifex 퍼블리셔는 `mutool`만 남음) → 항상 실패
- 공식 설치 파일(`gs10071w64.exe`)은 NSIS인데도 **`/S` 무인 플래그를 무시하고 GUI 마법사를
  띄운 채 멈춘다** (CPU 0으로 대기 → 무인 설치 중으로 착각하고 기다리면 무한 대기)

**교체한 방식**: 공식 릴리스(Artifex GitHub) 다운로드 → **Authenticode 서명 확인**
(`CN="Artifex Software, Inc."`) → **7-Zip으로 추출해 `%ProgramFiles%\gs\gs<버전>`에 배치**.
gs는 실행 파일 기준 상대경로로 lib/Resource를 찾으므로 레지스트리 없이 정상 동작한다.

- 버전 폴더명은 파일명에서 추측하지 않고 **실행 파일의 버전 리소스**(`ProductVersion`)에서 취득
  (`gs10071` → 10.07.1인지 1.00.71인지 규칙이 일관되지 않음)
- `Test-GsRuns`로 실행 가능 여부까지 판정하고, 실패 시 동봉 `vcredist_x64.exe`로 MSVC 런타임 보충
- `Find-Gs`를 앱(`main.js findGhostscript`)과 같은 규칙(ProgramFiles 양쪽·버전 역순)으로 정리

**검증**: 스크립트에서 함수 4종을 AST로 추출해 임시 ProgramFiles 샌드박스에서 실제 구동 —
다운로드·서명확인·추출배치·실행·경로탐색 5단계 모두 통과.

### 3-2. 상태 점검만 해도 변환 프로그램이 실행됐다

`Test-Com`이 `New-Object -ComObject`로 설치 여부를 확인해, **`-Check`로 점검만 해도
포토샵·인디자인·한글·오피스가 실제로 실행**됐다(각 1~1.5GB 상주, 창 없는 좀비로 남음).
특히 한글은 단일 인스턴스라 좀비가 남으면 앱의 HWP 변환이 막힌다.

→ ProgID가 `HKEY_CLASSES_ROOT`(또는 `HKCU:\SOFTWARE\Classes`)에 등록돼 있는지만 확인하도록 교체.
6종 판정 결과는 동일하고 아무것도 실행되지 않는다(실측 확인).

## 4. 시작 화면 안내문구 정리

- 타이틀 하단: `PDF · 한글 · MS Office · Adobe · Image  인쇄용 조판하기`
- 드롭 안내를 두 줄로: `📂 파일을 여기로 드래그` / `(여러 파일 가능)`
  — 아랫줄은 같은 글꼴·굵기로 한 단계만 작게(`.drop-hint-sub`)
- `파일 선택하기` 하단 안내: `0.8em → 0.68em`으로 축소하고 **`<b>` 강조를 제거해 굵기를 통일**.
  `.drop-sub`에 `font-family: inherit; font-weight: 400`을 고정하고
  `.drop-sub b, .drop-sub strong { font-weight: 400 }` 안전장치를 걸어, 나중에 `<b>`가
  다시 섞여도 굵기가 어긋나지 않게 했다.

## 5. 전날 작업 8건 — 실제 앱으로 최종 검증

`electron .`로 앱을 띄우고 CDP(원격 디버깅)로 조작해, 하네스가 아닌 **실제 앱**에서 확인했다.

| 항목 | 실측 결과 |
|---|---|
| 1 이미지 첨부 | 1200×900 PNG → 1쪽 PDF, 318×238mm (파일의 96DPI 기록을 우선 적용) |
| 2 크게 보기 | 컬러 픽셀 86,174개(3.33%) — 빨간 원만 자홍색, 회색 도형은 그대로 |
| 3 폰트 안전화 | 메인·사이드바에 존재, 편집 사이드바엔 없음 |
| 4 편집모드 유지 | 들락 후 결과·미리보기·컬러수·흑백확정 모두 유지 |
| 5·6 임포징 | 생성 43ms → 버튼 잠김 → 설정 변경 시 재활성 |
| 7 최근 작업 | 프리셋·블리드·표지 스냅샷 저장·복원 |
| 8 재단선 | (노드 추출 테스트 + 콘텐츠 스트림 좌표 대조 + 렌더 확인) |

**잉크 정규화 실측** — 깨끗한 상태에서 `gs inkcov`:

| 페이지 | C | M | Y | K |
|---|---|---|---|---|
| 1·3·5 (컬러 요소 있음) | 0.018 | 0.018 | 0.018 | 0.26 |
| **2·4·6 (회색만)** | **0** | **0** | **0** | 0.25 |

잉크 판정 `[1,3,5]` = 화면 판정 `[1,3,5]` 완전 일치. 흑백 페이지는 CMY가 정확히 0이라
프린터가 흑백으로 과금한다.

> 참고: 여러 테스트를 겹쳐 돌린 상태에서는 "7장 전부 컬러"로 나온다. 이는 상태 오염 때문이며,
> 초기화 후 재측정하면 위와 같이 정확하다. **잉크 판정 검증은 반드시 깨끗한 상태에서** 할 것.

검증 뒤 테스트가 남긴 최근 파일 2건·최근 작업 기록 3건을 제거해, 실제 작업 기록 6건만 남겼다.

---

## 변경 파일

`scripts/install-portable.ps1` · `src/index.html` · `src/style.css` · `.gitignore`
→ `dist/win-unpacked/resources/app/` 동기화 완료 (체크섬 대조 확인)

---

# (오후 작업) 저장 파일명 · 임포징 반영 지연 · 폰트 안전화 · 체험판 라이선스

## 5. 저장 파일명 자동 번호 (`main.js`)

같은 이름이 있으면 `문서.pdf → 문서-1.pdf → 문서-2.pdf`를 **기본 이름으로 제안**한다.
기준 폴더는 `_lastSaveDir`(직전 저장 폴더, 없으면 다운로드 폴더). 사용자가 다이얼로그에서
다른 폴더·이름을 고르는 것은 그대로 자유이고, 그때 같은 이름이면 OS가 덮어쓰기를 묻는다.

`uniqueSavePath(dir, name)` — 확장자 앞에 번호를 붙인다(`a.b.json → a.b-1.json`).
검증: main.js에서 함수를 **정규식으로 추출해 eval**한 노드 테스트로 4회 연속 발급·확장자 처리 확인.

## 6. "메인으로 돌아와도 임포징 전 화면" — 원인 3가지

1. **순서 문제(진짜 원인)**: `applyChanges`(app-core)에서 gs 폰트 안전화가 **화면 렌더보다 앞**에
   있었다. 편집 모드 '저장하고 닫기'는 창을 먼저 닫으므로, gs가 끝날 때까지 메인 화면이
   임포징 전 상태로 굳어 있었다. → 안전화를 적용 단계에서 제거(아래 7번).
2. **적용 거부**: 흑백변환이 켜져 있고 선택 페이지가 없으면 `applyChanges`·`saveAndCloseWorkspace`
   가드가 **임포징만 켠 상태의 적용을 통째로 거절**했다. 임포징·블리드·편집도 단독 수정사항으로
   인정하도록 수정.
3. **미리보기 캐시 미적용**: 임포징 시트는 원본 1:1 매핑이 없어 `_pvPageCache` 키를 만들지 못해
   **매번 전 시트를 재렌더**했다. 결과 바이트 지문(fp) 기반 키를 추가.

## 7. 폰트 출력 안전화 — 다운로드 단계로 이동 + 기본값 변경

**실측**(맑은고딕 40쪽 텍스트 밀집, gs 10.07):

| 방식 | 소요 | 결과 용량 (원본 56KB) | 원본 대비 렌더 오차(150dpi) |
|---|---|---|---|
| ✒ 곡선화 `-dNoOutputFonts` | 2.6초 | 11.2MB (약 200배) | 평균 10.3 |
| 🔤 완전 임베드 `-dEmbedAllFonts` | **0.30초** | **35KB** | **평균 0.66** |

곡선화가 9배 느리고 용량 200배인데다, **원본과의 렌더 차이도 오히려 더 크다**(힌팅 손실).
"안전 = 곡선화"라는 통념의 반례.

- 기본 방식을 **완전 임베드**로 변경 (`localStorage outlineMode === 'outline'`일 때만 곡선화)
- 안전화는 **적용 단계에서 제거** → 화면은 임포징 결과로 즉시 갱신.
  적용 직후 `prewarmOptimizedOutput`이 백그라운드로 gs를 돌려 캐시 → 다운로드 시 대기 ≈ 0
- 직접저장 분기(`directOutputBytes`, 블리드 생성 등)도 안전화를 반영하도록 수정
  (예전엔 이 경로가 안전화를 **통째로 건너뛰었다**)
- `setOutlineEnabled`/`setOutlineMode`는 더 이상 `invalidateProcessed()` 하지 않는다 —
  모양이 바뀌지 않는 처리라 적용 결과를 버릴 이유가 없다(대용량 재적용 방지)

## 8. 체험판 1회용 키 시스템 (1단계 — 코어)

테스터 배포용. **설계 확정 사항(사용자 결정)**: 서버 = 사무실 PC 상시 가동 · 보호는 L1+L2+L3 ·
기간 중엔 워터마크 없이 깨끗, **만료 시 저장·출력만 차단**(열기·분석·미리보기는 허용) ·
서버 재확인 **7일 주기 + 유예 3일** · 발급은 **앱 내장 관리자 화면**.

```
[관리자 PC]                                  [테스터 PC]
개인키 %USERPROFILE%\.pdfeditor-license       공개키만 앱에 내장(PUBKEY_B64)
관리자 화면에서 키 발급 ── PDFE-XXXX-XXXX-XXXX ──▶ 첫 실행 시 키 입력
활성화 서버(8736)  ◀─── 키 + 기기지문 ──────────┘
   │ 키 소진 처리(재사용 불가) + 만료일 확정
   └── 서명 토큰 ──▶ userData/license.dat, 만료까지 오프라인 · 7일마다 재확인(유예 3일)
```

**새 파일**: `license.js`(판정) · `license-server.js`(활성화 API) · `src/license.html` +
`preload-license.js`(인증·발급 창, **Ctrl+Shift+Alt+L**)

**차단 지점은 메인 프로세스 4곳**(렌더러가 아니다 — 화면 코드를 고쳐도 파일이 안 나간다):
`dialog:saveFilePath` · `print:toPDF` · `hotfolder:finish` · `session.will-download`

**핵심 함정·결정**
- 진짜 "1회용"은 **서버 기록으로만** 보장된다. 기기 바인딩만으로는 흔적을 지우면 뚫린다.
- 만료일은 **최초 활성화 시각 + 발급 일수로 한 번만 확정**. 같은 PC 재설치는 재발급을 허용하되
  기간은 늘어나지 않는다. 다른 PC는 409로 거부.
- 관리자 자료를 userData가 아닌 **홈 폴더**에 둔 이유: 개발 실행과 포터블 실행의 userData
  경로가 달라, userData에 두면 "개발에선 관리자, 배포본에선 아님"이 된다.
- `PUBKEY_B64`(license.js)를 바꾸면 **기존 발급 라이선스가 전부 무효**가 된다. 재생성 금지.
- HWID = MachineGuid + Win32_ComputerSystemProduct.UUID + 호스트명 → sha256 24자.
  `reg query`는 값이 없을 때 stderr를 뱉으므로 `stdio:['ignore','pipe','ignore']` 필수.
- 시계 되돌리기: `userData\lic.mark` + `HKCU\Software\PDFEditor\Lic\st` 중 큰 값 기준 10분 초과.
- 관리자 PC(개인키 보유)는 항상 무제한(`mode:'admin'`) — 자기 앱이 만료되는 사고 방지.
  남의 `admin.json`을 갖다 놔도 `pub !== PUBKEY_B64`면 거부.

**검증 30항목 전부 통과** (실제 모듈을 그대로 구동)

| 테스트 | 내용 |
|---|---|
| `test-license.js` (22) | 발급 · 활성화 · 다른 PC 재사용 거부(409) · 같은 PC 재설치 허용(기간 불변) · 없는 키 404 · 만료일 위조 토큰 검증 실패 · 서버 DB 소진 기록 · 원격 취소(403 revoked) · 무차별 대입 429 · 그 외 경로 404 |
| `test-tester.js` (8) | `USERPROFILE`을 임시폴더로 바꿔 '관리자 아님'을 만들고 → 미등록/정상/만료/타 PC/재확인 유예 초과/유예 안/서명 위조/**시계 되돌리기** 판정 |

> 테스트가 남긴 레지스트리 마크는 반드시 `reg delete HKCU\Software\PDFEditor\Lic /f`로 정리할 것.
> (미래 시각 마크가 남으면 실제 앱이 "시계 이상"으로 판정한다)

인증 창 UI는 브라우저에 stub API를 물려 렌더 확인(관리자 탭·발급 폼·서버 상태·발급 내역 표).

## 9. 덤으로 고친 것 — 빌드가 깨져 있었다

`package.json`의 `build.files`는 **화이트리스트**인데 `remote-server.js`가 빠져 있었다.
→ 새로 `npm run build`를 하면 `require('./remote-server')`에서 **실행 즉시 크래시**하는 상태.
(그동안 `dist/win-unpacked`에 수동 동기화만 해서 드러나지 않았다)
라이선스 파일 3개와 함께 등록했다.

---

## 🔜 다음 단계 — 2단계 하드닝 (미착수)

**지금 상태의 한계**: 아직 평문 JS라 `license.js`의 검사 줄을 지우는 우회가 가능하다.

계획:
1. `asar: true` (현재 false — 배포본이 평문 그대로 노출)
2. `main.js` · `preload.js` · `license.js` · `license-server.js` → **bytenode(.jsc) 컴파일**
   (같은 Electron/V8로 구워야 함. 빌드 스크립트가 자동 처리)
3. 렌더러 3파일(app-core/process/ui)은 `nodeIntegration:false` 클래식 스크립트라
   **바이트코드화 불가** → 난독화가 한계. 대신 검사는 전부 메인/preload 쪽에 있다.
4. 배포본 오류 로그를 파일로 남기기(바이트코드라 스택 추적이 어려워지므로)
5. 개발 실행(`npm start`)은 지금처럼 평문·검사 없이 유지 — **원본 소스 활용에 지장 없음**

### 사용자 테스트 절차 (1단계 확인용)

1. 앱에서 **Ctrl+Shift+Alt+L** → `🛠 관리자` 탭
2. `📡 활성화 서버` 켜기 → 공유기에서 **8736 포트 포워딩**
3. `🔑 새 체험 키 발급` (기간·메모) → 키 자동 복사 → 테스터에게 키 + `공인IP:8736` 전달
4. 테스터 PC: 첫 실행 → 인증 창에 키·서버 주소 입력 → 등록 후 저장·출력 정상 동작 확인
5. 확인할 것: ① 등록 성공 ② 같은 키를 다른 PC에서 거부 ③ 저장 차단 동작(만료 전엔 정상)
   ④ 포트 포워딩 없이도 되는지(사설망/VPN 여부)

**테스트 결과를 받은 뒤 2단계 진행 예정.**

## 변경 파일 (오후)

`main.js` · `preload.js` · `package.json` · `license.js`(신규) · `license-server.js`(신규) ·
`preload-license.js`(신규) · `src/license.html`(신규) · `src/index.html` · `src/style.css` ·
`src/app-core.js` · `src/app-process.js` · `src/app-ui.js`
→ `dist/win-unpacked/resources/app/` 동기화 완료

## 회귀 테스트 위치

```bash
node scripts/test/license-server.test.js    # 발급·활성화·재사용거부·취소·남용방지 (22항목)
node scripts/test/license-tester.test.js    # 테스터 PC 판정 7종 + 시계 되돌리기 (8항목)
```
둘 다 **실제 `license.js`/`license-server.js`를 그대로 require**한다(복사본 없음 — 드리프트 방지).
`license-tester.test.js`는 `USERPROFILE`을 임시폴더로 바꿔 '관리자 아님' 상태를 만든다.

## 10. 단일 EXE 재빌드 (테스터 배포본)

`npm run build` → **`dist\PDF분석기-포터블-3.1.0.exe`** (73.1MB, 2026-08-25 21:19)

- 7월 23일자 옛 exe는 `PDF분석기-포터블-3.1.0_구버전_2026-07-23.exe`로 이름만 바꿔 보관(덮어쓰지 않음)
- 빌드가 `dist\win-unpacked`도 소스에서 새로 생성 — 그동안 수동 동기화로 섞여 있던
  잔여물(`resources\app\dist\*.apk`)이 정리됐다
- 패키징 확인: `license.js` · `license-server.js` · `preload-license.js` · `remote-server.js` ·
  `src\license.html` 모두 포함(9번의 `files` 화이트리스트 수정 덕분), **개인키는 미포함**
- 부팅 검증: 포터블 exe를 실제로 실행 → 창(`PDF Editor`) 정상 표시, 프로세스 4개 유지 확인 후 종료

**빌드 직전 수정 1건**: 저장 길목이 `license.status()`(캐시)를 보고 있어, 앱을 켜 둔 채
기간이 만료되면 재시작 전까지 저장이 계속 허용됐다. → `license.refresh()`로 **저장 시점에
다시 판정**하도록 변경(`licenseGate` · `hotfolder:finish` · `will-download` 3곳).

> 참고: `package.json`의 version이 **3.1.0**이라 파일명도 3.1.0이다. 실제 앱은 v3.6 이후 상태이므로,
> 배포 전에 version을 올려 두면 테스터와 버전 혼동이 없다(미변경 — 사용자 결정 필요).

## 11. v3.7.0 — 버전 상향 + Ghostscript 동봉

**`dist\PDF분석기-포터블-3.7.0.exe`** (92.3MB, 2026-08-25 21:38) — 테스터 배포본.
`package.json` version 3.1.0 → **3.7.0** (앱 실체가 v3.6 이후였는데 버전 문자열이 3.1.0에 멈춰 있었다).

**설치 파일이 아니라 실행본을 넣었다** — 요청은 "gs 설치 파일 포함"이었으나:
- 프로젝트에 있던 `gs10071w64.exe`는 10번 빌드가 `win-unpacked`를 재생성하며 사라졌다(수동 배치 파일이었음)
- 설치 파일 경로는 **테스터 PC에 7-Zip이 있어야** 동작한다(`Install-GsByExtract`). 없으면 NSIS가
  `/S`를 무시하고 GUI 마법사를 띄운 채 멈춘다 (3번 절에서 확인한 문제)

→ 이 PC의 `D:\Ghostscript\gs10.07.1`에서 실행에 필요한 것만(bin·lib·Resource·iccprofiles, 42MB.
doc·examples 23MB 제외, gsdll64.lib 제외) `vendor\gs`로 추려 **extraResources로 동봉**.
`vendor/`는 gitignore (저장소 비대화 방지).

```json
"extraResources": [ { "from": "vendor/gs", "to": "gs" } ]
```

`findGhostscript()`(main.js)에 **동봉본 최우선** 탐색을 추가:
`process.resourcesPath\gs\bin` → `__dirname\vendor\gs\bin` → `C:\Program Files\gs\gs*` → PATH.
결과: 테스터는 **설치·다운로드·7-Zip 없이** 잉크 판정·폰트 안전화·아웃라인이 바로 동작한다.

**검증**
- main.js에서 `findGhostscript`를 추출해 **패키징 레이아웃**(resourcesPath=dist\win-unpacked\resources)으로
  평가 → 동봉본 경로 선택 확인
- 동봉본을 그 위치에서 직접 실행: `--version` = 10.07.1, **inkcov 정상 출력**(Resource·lib 완비 확인)
- 포터블 exe 실행 → 25초 기동 유지, 임시 추출 폴더에 `resources\gs\bin\gswin64c.exe` 존재 확인 후 종료

> ⚠ **AGPL**: Ghostscript 동봉은 제3자 배포에 해당한다. 소수 테스터 대상이면 실무상 문제는 거의
> 없으나, 정식 판매 단계에서는 소스 제공 의무 또는 Artifex 상용 라이선스를 정리하고 가야 한다.

**배포 파일 정리**

| 파일 | 용도 |
|---|---|
| `PDF분석기-포터블-3.7.0.exe` | ✅ **테스터에게 줄 것** (92.3MB, gs 동봉) |
| `PDF분석기-포터블-3.1.0.exe` | 오늘 21:19 빌드 — gs 미동봉, 3.7.0으로 대체됨 |
| `PDF분석기-포터블-3.1.0_구버전_2026-07-23.exe` | 7월 옛 버전 (라이선스·최근 기능 없음) |
| `PDF분석기-포터블-1.0.0.exe` | 6월 옛 버전 |
