# 2026-09-16 작업내역

> **한글 보안창 자동 클릭이 사용자의 한글 창까지 누르던 문제 — 대상을 좁히고 실한글로 검증.**
>
> 2026-09-15 안정성 점검에서 "실화면 클릭이 필요해 보류"로 남겨 둔 항목.

---

# 1. 증상 (옛 코드로 재현)

워처가 한글 프로세스의 **아무 창**에서 `모두 허용 > 허용 > 확인/예/계속` 순으로 눌렀다.
HEAD의 C#을 그대로 떼어 가짜 대화상자 3개(우리 파일 보안 창 / 다른 파일 보안 창 / 저장 확인 창)에 돌린 결과:

```
OTHER:모두 허용(N) | DECOY:확인 | OURS:모두 허용(N)
```

사용자가 켜 둔 한글의 **저장 확인 [확인]** 과 **다른 파일의 보안 창**까지 눌렸다.

---

# 2. 처리 (src/convert_hwp.ps1)

| 무엇 | 어떻게 |
|---|---|
| 누르는 창 | '…접근하려는 시도가 있습니다' 보안 창만. 확인·예·계속·동의 순위 **삭제** |
| 누구 것인지 | 우리가 띄운 한글이면 그 PID(`COMPID`)의 창만. 켜져 있던 한글이면 **창 글자에 변환 중 파일 이름**이 있을 때만 |
| 어떤 버튼 | 우리 한글 = '모두 허용', 켜져 있던 한글 = '접근 허용'(이 파일만) |
| 클릭 안전 | 좌표 맨 위가 정말 그 버튼일 때만(`AutomationElement.FromPoint`). 가려져 있으면 누르지 않고 앞으로 올리기만. 사용자가 마우스 버튼을 누르고 있으면 건너뜀 |
| 시작 시점 | COM 생성 뒤(누구의 한글인지 알고 난 뒤) — 보안 창은 Open 때 뜬다 |

가려진 버튼은 UIA가 클릭 좌표를 주지 않는다(`TryGetClickablePoint` false) → BoundingRectangle 가운데로 잡고 위 '맨 위 확인'으로 가려짐을 판정한다.

---

# 3. 검증

## 3-1. 가짜 대화상자 (`scripts/test/hwp-dialog-watch.test.ps1`, 8개 통과)
`convert_hwp.ps1`의 C#을 파일에서 그대로 떼어 쓴다(복사본 드리프트 없음). 가짜 창은 가장 왼쪽 모니터.

- 켜져 있던 한글 가정: 우리 파일 보안 창만 '접근 허용', 저장 확인 창·다른 파일 보안 창은 건드리지 않음
- 우리가 띄운 한글 가정: 그 프로세스의 보안 창 두 개를 '모두 허용', 저장 확인 창은 그대로
- 한글이 아닌 프로그램의 같은 모양 창: 무시
- 보안 창이 다른 창에 가려짐: `occluded`로 판정, 가린 창을 클릭하지 않음

## 3-2. 실제 한글 (2026-09-16, 사용자가 한글을 닫고 승인)
- 한글 꺼진 상태 변환: 보안 창 '모두 허용' 클릭 → 3초, PDF 22KB 생성
- 한글 켠 상태에서 다른 문서 변환: 2초 성공, **켜 둔 문서 창은 그대로 살아 있음**
- 붙은(attached) 경로: 변환 스크립트의 워처를 끄고 파일 이름 대조 모드 워처만 따로 돌려 **'접근 허용' 클릭 성공**

### 실한글에서 확인한 사실
- 실제 버튼 이름은 `모두 허용 : ALT+N`, `접근 허용 : ALT+Y` — 부분 일치라 그대로 동작
- 보안 창에 **변환 파일 경로가 글자로 보인다** → 파일 이름 대조가 성립
- **`HWPFrame.HwpObject`는 한글이 이미 떠 있어도 자기 프로세스를 새로 띄운다** → '사용자 한글에 붙는' 상황 자체가 드물다(그래도 붙은 경로는 유지)

## 3-3. 기타
- `npm run smoke` 통과, 포터블 앱 동기화(체크섬 일치)
- 이 검사는 창을 띄우고 실제 마우스를 움직이므로 smoke에는 넣지 않았다


---
---

# 4. "화면은 컬러 7쪽인데 출력은 3쪽만 컬러" — 칼7흑213.pdf

## 4-1. 제보와 확인
- 사용자: 컬러 7쪽이어야 하는데 실제 출력은 3쪽만 컬러, 화면에서는 7쪽 모두 컬러
- 저장본 `칼7흑213-1.pdf`(224쪽)를 **엔진 4개**로 그려 대조 — 모두 컬러 3쪽(81·83·84)
  - Ghostscript(inkcov·RGB 렌더), pdf.js, Windows.Data.Pdf, **Acrobat**(IAC `PDPage.CopyToClipboard`)
  - 함정: Acrobat 클립보드 렌더는 LCD 서브픽셀 글자 번짐 때문에 **검정 글자만 있는 쪽도 채도 픽셀 수천 개** → 사진 영역을 확대해 눈으로 확인(회색)
- 결론: 파일 자체가 3쪽 컬러, RIP 출력이 정확. "PCL6로는 7쪽 컬러"는 다른 파일이었음(사용자 확인)

## 4-2. 원인 ① — 저장 경로가 공유 사진을 같이 회색으로
- 원본 `칼7흑213.pdf`와 쪽 대응(빈 쪽 2·4·6·224 삽입 보정): 회색이 된 76·79·85·86쪽은 **뒤쪽 흑백 206·207·214·215쪽과 같은 이미지 객체를 공유**, 컬러로 남은 78·80·81쪽은 공유 없음
- 적용(화면) 경로 `ensureBwConverted`는 흑백 쪽만 따로 복사해 멀쩡, **다운로드 경로 `buildBaseOptimized`만** 전 쪽을 `copyPages` 한 번으로 복사한 뒤 흑백 쪽을 제자리 변환
- 처리: 흑백 대상/비대상을 **두 묶음으로 나눠 복사**(묶음 안 공유는 유지)
- 대가: 공유 사진이 컬러·회색 두 벌(+589KB, 정답), 두 묶음이 함께 쓰던 글꼴 두 벌(+133KB ≈ 3%)

## 4-3. 원인 ② — 체크섬 틀린 스트림에서 쪽 전체가 컬러로 남음
- 아래 컬러 검수가 실파일 28개에서 **진짜로 색이 남은 5쪽**을 찾음(gs inkcov로 CMY 확인): 심재준 p19, 명함시안_ANCHOR-2 p17·30·36·62
- 워커 `stream-grayify`가 `TypeError: …reading 'length'`로 죽음 → 그 쪽은 원본 색 유지(함정 12의 안전장치)
- 진짜 원인: zlib 체크섬(adler32)이 틀리거나 빠진 콘텐츠 스트림에서 **`pako.inflate`가 예외 없이 undefined** 반환(Acrobat·pdf-lib은 정상으로 엶)
- 처리: zlib 머리 2바이트를 떼고 raw deflate로 푸는 폴백(`inflateRawTolerant`·`inflateLenient`), 결과는 `ArrayBuffer.isView`로 확인
- 전수: 28개 파일 Flate 스트림 5,393개 중 pako 실패 11 → 복구 10(pdf-lib과 바이트 일치), 1개는 원래 빈 스트림

## 4-4. 재발 방지 — 🎨 저장본 컬러 검수
- `checkColorIntent`(app-process): 다운로드 base를 저장하기 직전 쪽마다 `pageIsNeutral`(libs/gray-blend.js — 렌더 없이 내용만)로
  - **lost**: 흑백 대상이 아닌데 **원본에 색이 있었고** 저장본은 무채색
  - **leftover**: 흑백 대상인데 색이 남음
- 의도와 다르면 저장 전 확인창(쪽 번호), 맞으면 성공 메시지에 "🎨 컬러 검수: 컬러 N쪽 · 흑백 변환 M쪽 — 의도와 일치"
- 헛경보 제거 과정
  - 분석값(isColor)만 기준 → K 100% 검정(`0 0 0 1 k`)을 pdf.js가 따뜻한 RGB로 그려 **59쪽 헛경보** → 원본 쪽과 대조로 변경
  - `/DeviceN [/Black]` 이미지를 색으로 봄 → 먹 한 가지 Separation·DeviceN은 무채색
- 기각: pdf.js 렌더 비교는 224쪽에 76~213초

## 4-5. 검증
- `bw-shared-image.e2e.js` — 합성 6/6, 실파일 6/6
  - 저장본 컬러 [78,80,81] → **[76,78,79,80,81,85,86]**, gs inkcov도 정확히 7쪽
  - 검수: 고친 저장본 경고 없음(컬러 7·흑백 213), 옛 방식 재현 시 76·79·85·86쪽을 정확히 지목
- 컬러 검수 헛경보: 실파일 28개 × (전 쪽 흑백 / 컬러 유지) **0건**
- `inflate-tolerant.test.js` 12/12(워커 메시지 처리 전체 포함), 두 파일 전 쪽 흑백 후 gs inkcov CMY 0
- 흑백 회귀 28개: CMY 0, 밝기·흰색 반전 동일
- smoke(단위 8종) · E2E 12종 통과(임포징 독립 도구 68 포함), 포터블 동기화, 독립 도구 재빌드

## 4-6. 작업 함정
- 하네스에서 `startLoad`는 열린 문서에 **합쳐** 연다 → 파일마다 `closeTab` 후 `createTab`+`activateTab`+`analyzePDF`
- vm으로 워커 코드를 돌리면 `instanceof Uint8Array`가 realm이 달라 거짓
- 사용자에게: 이미 저장된 `칼7흑213-1.pdf`는 고쳐지지 않음 — 원본으로 다시 작업·저장
