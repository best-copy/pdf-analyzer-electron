# 2026-09-16 작업내역

> **한글 보안창 자동 클릭이 사용자의 한글 창까지 누르던 문제 — 대상을 좁히고 실한글로 검증.**
>
> 2026-09-15 안정성 점검에서 "실화면 클릭이 필요해 보류"로 남겨 둔 항목.

---

# 1. 증상 (옛 코드로 재현)

워처가 한글 프로세스의 **아무 창**에서 `모두 허용 > 허용 > 확인/예/계속` 순으로 눌렀다.
HEAD의 C#을 그대로 떼어 가짜 대화상자 3개(우리 파일 보안 창 / 다른 파일 보안 창 / 저장 확인 창)에 돌린 결과:

```
OTHER:모두 허용(N) | DECOY:확인 | OURS:모두 허용(N)
```

사용자가 켜 둔 한글의 **저장 확인 [확인]** 과 **다른 파일의 보안 창**까지 눌렸다.

---

# 2. 처리 (src/convert_hwp.ps1)

| 무엇 | 어떻게 |
|---|---|
| 누르는 창 | '…접근하려는 시도가 있습니다' 보안 창만. 확인·예·계속·동의 순위 **삭제** |
| 누구 것인지 | 우리가 띄운 한글이면 그 PID(`COMPID`)의 창만. 켜져 있던 한글이면 **창 글자에 변환 중 파일 이름**이 있을 때만 |
| 어떤 버튼 | 우리 한글 = '모두 허용', 켜져 있던 한글 = '접근 허용'(이 파일만) |
| 클릭 안전 | 좌표 맨 위가 정말 그 버튼일 때만(`AutomationElement.FromPoint`). 가려져 있으면 누르지 않고 앞으로 올리기만. 사용자가 마우스 버튼을 누르고 있으면 건너뜀 |
| 시작 시점 | COM 생성 뒤(누구의 한글인지 알고 난 뒤) — 보안 창은 Open 때 뜬다 |

가려진 버튼은 UIA가 클릭 좌표를 주지 않는다(`TryGetClickablePoint` false) → BoundingRectangle 가운데로 잡고 위 '맨 위 확인'으로 가려짐을 판정한다.

---

# 3. 검증

## 3-1. 가짜 대화상자 (`scripts/test/hwp-dialog-watch.test.ps1`, 8개 통과)
`convert_hwp.ps1`의 C#을 파일에서 그대로 떼어 쓴다(복사본 드리프트 없음). 가짜 창은 가장 왼쪽 모니터.

- 켜져 있던 한글 가정: 우리 파일 보안 창만 '접근 허용', 저장 확인 창·다른 파일 보안 창은 건드리지 않음
- 우리가 띄운 한글 가정: 그 프로세스의 보안 창 두 개를 '모두 허용', 저장 확인 창은 그대로
- 한글이 아닌 프로그램의 같은 모양 창: 무시
- 보안 창이 다른 창에 가려짐: `occluded`로 판정, 가린 창을 클릭하지 않음

## 3-2. 실제 한글 (2026-09-16, 사용자가 한글을 닫고 승인)
- 한글 꺼진 상태 변환: 보안 창 '모두 허용' 클릭 → 3초, PDF 22KB 생성
- 한글 켠 상태에서 다른 문서 변환: 2초 성공, **켜 둔 문서 창은 그대로 살아 있음**
- 붙은(attached) 경로: 변환 스크립트의 워처를 끄고 파일 이름 대조 모드 워처만 따로 돌려 **'접근 허용' 클릭 성공**

### 실한글에서 확인한 사실
- 실제 버튼 이름은 `모두 허용 : ALT+N`, `접근 허용 : ALT+Y` — 부분 일치라 그대로 동작
- 보안 창에 **변환 파일 경로가 글자로 보인다** → 파일 이름 대조가 성립
- **`HWPFrame.HwpObject`는 한글이 이미 떠 있어도 자기 프로세스를 새로 띄운다** → '사용자 한글에 붙는' 상황 자체가 드물다(그래도 붙은 경로는 유지)

## 3-3. 기타
- `npm run smoke` 통과, 포터블 앱 동기화(체크섬 일치)
- 이 검사는 창을 띄우고 실제 마우스를 움직이므로 smoke에는 넣지 않았다
