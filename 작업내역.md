# PDF 분석기 데스크탑 앱 — 작업 내역

> 작업일: 2026-06-01  
> 프로젝트 경로: `D:\claude\projects\pdf-analyzer-electron\`  
> 원본 HTML: `D:\바탕화면\PDF칼라흑백 페이지확인.html`

---

## 1. 기존 HTML 앱 수정 (Electron 이전 전)

### 1-1. 폰트 아웃라인 기능 롤백
- `🔤 폰트 아웃라인` 버튼 및 `downloadWithOutline()` 함수 제거
- `.outline-btn` CSS 스타일 제거

### 1-2. 흑백변환 없이도 다운로드 가능하도록 수정
- PDF 로드만 되면 `📥 적용 후 다운로드` 버튼 활성화 (옵션·편집 무관)
- `⬛ 흑백변환` 옵션이 켜졌을 때만 "페이지 선택" 오류 메시지 표시

### 1-3. 썸네일 다중 선택 (Ctrl+클릭 / Shift+클릭)
- **Ctrl+클릭**: 기존 선택 유지하며 개별 토글
- **Shift+클릭**: 마지막 클릭 기준으로 범위 전체 선택
- `lastClickedIdx` 변수로 범위 기준점 관리

### 1-4. JPEG 흑백변환 → 프린터 칼라 오인식 수정
| | 기존 | 수정 후 |
|---|---|---|
| 인코딩 | JPEG (3채널 YCbCr) | FlateDecode (1채널 raw) |
| ColorSpace | `DeviceRGB` ❌ | `DeviceGray` ✅ |
| 프린터 인식 | 칼라 페이지 | 흑백 페이지 |

- `canvas.toBlob('image/jpeg')`은 R=G=B 픽셀이어도 항상 3채널 JPEG 생성
- 픽셀을 1채널로 추출 → `pako deflate` → `FlateDecode + DeviceGray`로 저장
- PNG Sub 예측 필터(`Predictor 15, Colors 1`) 적용으로 압축률 유지

---

## 2. Electron 데스크탑 앱 구축

### 2-1. 프로젝트 구조
```
D:\claude\projects\pdf-analyzer-electron\
  ├── main.js          — 메인 프로세스 (창 생성, IPC 핸들러)
  ├── preload.js       — 렌더러 ↔ 메인 IPC 브릿지 (fs, dialog 등)
  ├── package.json     — Electron + electron-builder 설정
  ├── src\
  │   ├── index.html   — 앱 UI (기존 HTML 기반으로 Electron 전환)
  │   └── libs\        — 로컬 번들 라이브러리 (CDN 대체)
  │       ├── pdf.min.js
  │       ├── pdf.worker.min.js
  │       ├── pdf-lib.min.js
  │       └── pako.min.js
  └── dist\
      ├── PDF 분석기 Setup 1.0.0.exe   — 설치 파일 (NSIS)
      └── win-unpacked\PDF 분석기.exe  — 포터블 실행 파일
```

### 2-2. 브라우저 HTML에서 달라진 점
| 항목 | 브라우저 HTML | Electron 앱 |
|------|-------------|-------------|
| 라이브러리 | CDN (인터넷 필요) | 로컬 번들 (오프라인 동작) |
| PDF 열기 | `<input type="file">` | 시스템 파일 열기 다이얼로그 (IPC) |
| PDF 저장 | `a.click()` 자동 다운로드 | 시스템 저장 다이얼로그 (위치 선택) |
| 드래그&드롭 | 그대로 지원 | 그대로 지원 |
| 줌 | 브라우저 기본 | Ctrl+`+`/`-`/`0` 단축키 구현 |
| 업무 관리 연동 | `window.open(file://)` | Electron BrowserWindow (localStorage 공유) |

---

## 3. 버그 수정 이력

### 3-1. 파일 선택 버튼 / 드래그 미작동 (3개 원인)

| # | 에러 | 원인 | 수정 |
|---|------|------|------|
| 1 | `module not found: fs` | Electron 샌드박스 모드에서 Node.js 모듈 차단 | `webPreferences`에 `sandbox: false` 추가 |
| 2 | `SyntaxError: await is only valid in async` | `exportToBusinessMgmt()`가 일반 함수인데 `await` 사용 | `async function`으로 변경 |
| 3 | 드래그 시 창이 파일 경로로 이동 | Electron 기본 동작 | `document` 레벨 `dragover/drop` 기본 동작 차단 |

### 3-2. PDF.js 워커 asar 로드 실패
- **원인**: 패키지 내 `app.asar` 안의 파일은 Web Worker로 직접 로드 불가
- **수정**: preload의 `fs.readFileSync`로 워커 파일 내용 읽기 → **Blob URL** 생성 → `workerSrc`에 적용

### 3-3. 대용량 PDF IPC 전송 실패
- **원인**: 파일 전체를 JSON 배열로 IPC 전달 → 50MB+ PDF에서 느리고 불안정
- **수정**: main은 **파일 경로만 반환**, preload의 `readFile(path)`로 `ArrayBuffer` 직접 반환

### 3-4. 내보내기 후 업무 관리 앱에 견적서 미표시
- **원인**: `shell.openPath()`로 시스템 브라우저에서 열면 Electron localStorage와 분리됨
- **수정**: `BrowserWindow.loadFile()`로 Electron 창에서 열기 → 동일 세션 localStorage 공유
- 추가: 이미 열려 있으면 새 창 대신 기존 창 포커스

---

## 4. 업무 관리 앱 수정

### 4-1. 비밀번호 제거
- `app.js` — `APP_PASSWORD` 상수 및 `initLogin()` IIFE 전체 제거
- `index.html` — 로그인 오버레이 HTML(`#login-overlay`) 제거
- `index.html` — 로그아웃 버튼(`#btn-logout`) 제거

---

---

## 5. 2026-06-04 추가 작업

### 5-1. Dot Gain 선택 드롭다운 제거
- 처리 옵션에서 Dot Gain 선택 UI(`#dotGainSelect`) 삭제
- `getDotGain()`은 요소 없으면 `0` 반환 — 동작에 영향 없음

### 5-2. 버그 수정: 변환 후 PDF 열리지 않는 오류

**원인:** pako가 단일 청크 출력 시 내부 버퍼의 subarray를 반환
- `deflated.length` (실제 압축 크기) < `deflated.buffer.byteLength` (내부 버퍼, 최대 16KB)
- worker에서 `deflated.buffer` 전체를 전송 → 메인 스레드에서 잘못된 `Length` 기록
- PDF 리더가 스트림을 올바르게 파싱하지 못해 파일이 열리지 않음

**수정:** `worker-gray.js` — `jpeg2gray`, `flate2gray`, `icc-flate2gray` 3곳
```js
// 수정 전
self.postMessage({ deflated: deflated.buffer, ... })

// 수정 후
const deflatedBuf = deflated.buffer.slice(deflated.byteOffset, deflated.byteOffset + deflated.length);
self.postMessage({ deflated: deflatedBuf, ... })
```

### 5-3. 버그 수정: Acrobat 페이지 표시 오류

**원인:** FlateDecode 이미지 변환 후 `Filter`/`DecodeParms` 형태 불일치
| Filter | DecodeParms | 유효 여부 |
|--------|-------------|----------|
| `/FlateDecode` (이름) | `<< ... >>` (딕셔너리) | ✅ |
| `[/FlateDecode]` (배열) | `[<< ... >>]` (배열) | ✅ |
| `[/FlateDecode]` (배열) | `<< ... >>` (딕셔너리) | ❌ 기존 코드 |

일부 PDF에서 `Filter`가 배열 형태로 저장되어 있는데, 변환 후 `DecodeParms`만 딕셔너리로 바꾸고 `Filter`는 배열로 남아 Acrobat이 이미지 디코딩 실패.

**수정:** `index.html` — `convertFlateXObjectToGrayscale` (일반 + ICCBased 경로)
- `Filter`를 `/FlateDecode` (이름)으로 명시 정규화
- `BitsPerComponent: 8`, `Width`, `Height` 명시 갱신 추가

### 5-4. 버그 수정: Acrobat 페이지 표시 오류 (2차) — 명명된 색공간 처리

**원인 1 — 명명된 색공간 이미지 미변환**

InDesign 등 전문 DTP 도구로 만든 PDF는 이미지의 ColorSpace를
`/DeviceRGB` 직접 명시 대신 `/Cs1` 같은 명명된 색공간으로 지정.
`convertFlateXObjectToGrayscale`이 이 형태를 인식 못해 변환 없이 리턴.

**원인 2 — 미변환 이미지의 색공간이 Resources에서 삭제됨**

③ 색공간 정리 단계가 변환 실패한 이미지가 아직 참조 중인 색공간도
Resources.ColorSpace에서 삭제 → Acrobat이 해당 이미지 렌더링 불가
→ "페이지를 표시할 수 없음" 오류

**수정 내용 (`index.html`)**

1. `resDict`를 `convertXObjectImageToGrayscale` → `convertFlateXObjectToGrayscale`로 전달
2. 명명된 색공간 해결: csObj가 `/Cs1` 형태면 `resDict.ColorSpace`에서 실제 정의(`[/ICCBased ...]` 등)로 교체 후 기존 변환 경로 재사용
3. ③ 색공간 정리: XObject가 아직 참조 중인 색공간은 삭제 보류
   ```
   stillUsedCSNames = {XObject들의 ColorSpace 이름 수집}
   toDelete에서 stillUsedCSNames에 있는 항목 제외
   ```

### 5-5. 버그 수정: Acrobat 페이지 표시 오류 (3차) — Pattern 색공간 처리

**원인 분석 (첨부 PDF 직접 분석)**

`2025학년도 2학기_간호대학 사본.pdf`를 Node.js + pdf-lib로 분석한 결과:
- 이미지 XObject 없음 (순수 벡터 PDF)
- 6,295개 Form XObject (투명도 그룹 3,263개)
- Resources.ColorSpace:
  - `/CS0` → `[/ICCBased ...]` — ICC 색공간
  - `/CS1` → `[/Separation ...]` — 별색
  - **`/CS2` → `[/Pattern]`** — 패턴 색공간 (그래디언트 34개)
- 컨텐츠 스트림: `/CS2 CS /P0 SCN` (패턴 채색)

**버그 재현 경로:**
1. 코드가 `/CS2 CS` 제거 → strokeCS만 기록하고 연산자 삭제
2. `/P0 SCN` (패턴 이름 인수)은 숫자 정규식과 불일치 → **변환 없이 스트림에 잔존**
3. ③ 색공간 정리가 `/CS2`(Pattern)를 Resources에서 삭제
4. Acrobat: Pattern CS 없는 상태에서 `/P0 SCN` 실행 → **페이지 렌더링 오류**

**수정 내용 (`index.html`, `worker-gray.js`)**

1. csGrayMap 빌드 시 Pattern 타입 색공간 기록: `csGrayMap[name] = { type: 'Pattern' }`
2. `grayifyStream`: Pattern 타입 CS 연산자 제거 않고 유지
   ```
   /CS2 CS → /CS2 CS 그대로 (Pattern이므로)
   /P0 SCN → 변환 없이 유지 (숫자가 아닌 이름 인수)
   ```
3. ③ 색공간 정리: `/Pattern` 색공간은 삭제 대상에서 제외
   - Pattern 색공간 보존 → Acrobat이 정상 렌더링

### 5-6. 버그 수정: Pattern 색공간 오류 (4차) — csGrayMap 키 불일치

**원인**

5-5 수정에서 grayifyStream의 Pattern 판별 코드에 키 불일치 버그 존재:
```javascript
// 버그: csGrayMap['CS2']  → undefined (키는 '/CS2'로 저장됨)
const info = csGrayMap && csGrayMap[name];

// 수정: csGrayMap['/CS2'] → { type: 'Pattern' } (정상)
const info = csGrayMap && csGrayMap['/' + name];
```

| 케이스 | 수정 전 | 수정 후 |
|--------|---------|---------|
| `/CS1 CS` (Separation) | REMOVE ✓ | REMOVE ✓ |
| `/CS2 CS` (Pattern) | REMOVE ✗ | KEEP ✓ |

CS2(Pattern)가 제거되면 `/P0 SCN` 등 패턴 호출 앞에 활성 Pattern 색공간이
없어지므로 Acrobat이 렌더링 실패.

**수정 (`worker-gray.js`):** `csGrayMap[name]` → `csGrayMap['/' + name]`

---

## 5-7. 버그 수정: 레이어 겹침 영역 칼라 잔류 (2026-06-07)

**증상:** PDF에서 투명도로 겹치는 레이어 영역이 흑백변환 후에도 칼라로 남음

**원인:** ExtGState의 SMask(소프트 마스크) 및 투명도 그룹(Form XObject Group)이 변환되지 않음
- PDF 투명도 합성 구조: `gs` 연산자 → `ExtGState.SMask` → Form XObject `G` (마스크 그룹)
- 기존 코드는 `XObject` 리소스만 처리, `ExtGState`는 완전히 무시
- Form XObject의 `Group/CS`(합성 기준 색공간)가 RGB/CMYK로 남아 합성 시 색상 나타남

**수정 1차 (`index.html`):**
1. `processXObjects` 내 Form XObject 처리 시 `Group/CS → DeviceGray` 변환 추가
2. `processExtGState` 함수 신규 추가:
   - 페이지 `ExtGState` 딕셔너리 순회
   - 각 그래픽스 상태의 `SMask.G` Form XObject 탐색
   - SMask Form의 `Group/CS → DeviceGray`
   - SMask Form 콘텐츠 스트림 색상 연산자 변환 (`processContentStream`)
   - SMask Form 내부 리소스 재귀 처리 (`processXObjects`, `processExtGState`)
3. `processXObjects` 호출 후 `processExtGState(resDict)` 호출 추가

**수정 2차 (`index.html`) — Shading·페이지 Group 추가:**
4. `convertShadingFunc` 함수 추가:
   - Type 2(지수) 함수: C0/C1 RGB/CMYK → 그레이 스칼라 변환, ColorSpace 교체
   - Type 3(스티칭) 함수: 하위 함수 재귀 처리
5. `processShading` 함수 추가:
   - Resources.Shading 딕셔너리 순회, Type 1~3 Shading을 DeviceGray로 변환
   - Form XObject·SMask Form 내부 Resources.Shading도 재귀 처리
6. 페이지 노드의 `Group/CS → DeviceGray` 처리 추가 (페이지 전체 합성 색공간)

---

## 5-8. 버그 수정: 콘텐츠 스트림 내 인라인 이미지 칼라 잔류 (2026-06-07)

**증상:** 흑백변환 후에도 특정 구간(선이 겹치는 부분)에 파란색/보라색 작은 이미지가 남음

**원인 분석 (original.pdf 구조 파악):**
- PDF 페이지 → Form XObject (obj 8) → 677개 RGB 래스터 이미지 XObject (별도 obj)
- Form XObject의 콘텐츠 스트림(1.3MB 비압축)에 **541개의 인라인 이미지(BI/EI)** 포함
- 인라인 이미지 예시: `/W 41 /H 9 /BPC 8 /CS /RGB` + 바이너리 RGB 픽셀 데이터
- 픽셀값 `8D 84 B7` = (141, 132, 183) = 파란-보라색 → 스크린샷의 파란 선 원인

**기존 코드 문제:**
- `grayifyStream`은 `(...)`, `<...>` 문자열 리터럴만 보호하고 `BI`/`EI` 블록은 완전 무시
- 바이너리 픽셀 데이터가 regex에 의해 color operator로 오인식될 위험도 있음
- 인라인 이미지는 XObject 처리 코드와 완전히 별개 → 변환 대상에서 누락

**수정 (`worker-gray.js`):**
- `grayifyStream`의 문자 스캐너 while 루프에 BI 감지 분기 추가
- `BI`가 줄 첫 머리에 나타나면:
  1. 딕셔너리(`biGetNum`, `biGetCS`, `biGetF`) 파싱 → W, H, BPC, CS, F 추출
  2. **비압축 RGB/CMYK (Filter 없음)**: W×H×channels 바이트 읽어 BT.601 그레이 변환, `/CS /G`로 교체
  3. **이미 그레이 또는 알 수 없는 CS**: 바이너리 데이터를 regex로부터 보호하며 그대로 통과
  4. **필터 있음(압축)**: heuristic으로 EI 탐색 후 그대로 통과 (미구현, 안전 처리)

---

## 5-9. UI 개선: 애플 스타일 테마 + 썸네일 화질 향상 (2026-06-08)

**변경 내용:**

1. **애플 스타일 테마 전면 적용**
   - 폰트: `-apple-system, BlinkMacSystemFont, "SF Pro Display"` 계열로 교체
   - 배경: `#f5f5f7` (Apple 라이트 그레이)
   - 헤더: `#1d1d1f` (Apple 다크) 단색, 그라데이션 제거
   - 버튼: Apple Blue `#0071e3`, 둥근 모서리(980px 필버트), 절제된 그림자
   - 진행바: 4px 슬림, 단색 블루
   - 에러/성공 메시지: 배경색 더 밝게, border 추가
   - 스티키 패널: `backdrop-filter: blur(16px)` 반투명 효과

2. **썸네일 화질 향상**
   - 색상 분석: 기존 0.5x 스케일 유지 (성능 유지)
   - 썸네일 렌더링: 0.5x → **1.5x** 별도 렌더링 (3배 해상도)
   - JPEG 품질: 0.5 → **0.88** 향상

3. **칼라 페이지 썸네일 구분 강화**
   - 컬러 페이지: 테두리 2px → **3px**, 색상 `#30d158` (Apple Green), 배경 `#f0fff4`
   - 흑백 페이지: 테두리 `#c7c7cc`, 배경 `#f5f5f7`

4. **선택 시 다크 배경**
   - 클릭 선택 시: 배경 `#2c2c2e` (Apple Dark), 텍스트 자동 밝게
   - 재클릭 시: 원래 배경으로 복원 (기존 토글 로직 그대로)

---

## 5-10. UI 개선: 썸네일 크기 조절 + 컬러 테두리 강화 (2026-06-08)

**변경 내용:**

1. **썸네일 크기 단계별 확대/축소**
   - `−` / `+` 버튼으로 단계 이동 (80 → 100 → 120 → 140 → **160** → 200 → 240 → 300px)
   - 기본값 160px = 100%, 현재 퍼센테이지 실시간 표기 (`80%`, `100%`, `125%` …)
   - CSS `--thumb-size` 커스텀 프로퍼티로 그리드 컬럼 너비 동적 조절
   - 최솟값/최댓값 도달 시 해당 방향 버튼 비활성화

2. **컬러 페이지 테두리 3px → 4px**

---

## 5-11. 견적서 PDF 저장 기능 추가 / 업무관리 앱 내보내기 제거 (2026-06-08)

**변경 내용:**

1. **`📤 업무 관리 앱으로 내보내기` 버튼·함수 제거**
   - `exportToBusinessMgmt()`, `BM_APP_PATH` 상수 삭제
   - `shell:openBizApp` IPC 핸들러 제거 (`main.js`)
   - `preload.js`의 `openBizApp` API 제거

2. **`📄 PDF로 저장` 버튼 추가**
   - Electron `printToPDF()` API 활용: 숨겨진 BrowserWindow에 견적서 HTML을 로드 후 A4 PDF 변환
   - `main.js`: `print:toPDF` IPC 핸들러 추가 (임시 HTML 파일 경유 → printToPDF → 임시파일 삭제)
   - `preload.js`: `electronAPI.printToPDF(html)` API 노출
   - 저장 파일명: `견적서_고객명_YYYYMMDD.pdf`
   - 저장 성공 시 앱 상단 성공 메시지 표시

3. **`buildQuoteHTML(forPrint)` 공용 함수 분리**
   - 인쇄(`printQuote`)와 PDF 저장(`saveQuotePDF`) 모두 동일한 HTML 템플릿 사용
   - `forPrint=false` → `window.print()` 스크립트 포함 (인쇄창 자동 실행)
   - `forPrint=true` → 스크립트 제외 (PDF 변환 시 불필요)

---

## 5-12. UI 대규모 개편 — 썸네일·사이드바·단축키·드래그 (2026-06-08)

### 5-12-1. 썸네일 크기 조절 (플로팅 줌 위젯)
- `position: fixed` 우하단 플로팅 위젯 (`−` / `100%` / `+`)
- 10% 단위 50%~300% (26단계), CSS `--thumb-size` 커스텀 프로퍼티로 그리드 컬럼 동적 조절
- 단축키: `Ctrl+[` 축소 / `Ctrl+]` 확대
- PDF 로드 시 자동 표시, 탭 닫히면 자동 숨김

### 5-12-2. 썸네일 해상도 향상
- 색상 분석: 0.5x 스케일 유지 (성능)
- 썸네일 렌더링: 별도 **2.5x** 스케일 + JPEG 품질 **0.92**

### 5-12-3. 칼라/흑백 테두리 구분
- 칼라 페이지: `border: 4px solid #30d158` (Apple Green)
- 흑백 페이지: `border: 4px solid transparent` (테두리 없음, 레이아웃 크기 동일 유지)
- 선택 상태: 배경 `#2c2c2e` (다크), 파란 테두리

### 5-12-4. 컨텍스트 메뉴 (우클릭)
- 썸네일 우클릭 → 다크 반투명 메뉴 팝업
- 메뉴 항목: 빈 페이지 추가 / 오른쪽 회전 / 왼쪽 회전 / 흑백변환 선택 / 선택 해제
- 메인 그리드 + 사이드바 모두 동일 메뉴 연결

### 5-12-5. 단축키 전체 목록
| 단축키 | 동작 |
|--------|------|
| `Ctrl+Z` | 삭제 되돌리기 |
| `Ctrl+[` | 썸네일 축소 |
| `Ctrl+]` | 썸네일 확대 |
| `W` | 빈 페이지 추가 (마지막 호버 썸네일 뒤) |
| `R` | 오른쪽 90° 회전 |
| `L` | 왼쪽 90° 회전 |
| `B` | 흑백변환 선택 |
| `C` | 선택 해제 |
| `Esc` | 컨텍스트 메뉴 닫기 |

### 5-12-6. 드래그 앤 드롭 페이지 순서 이동
- 메인 그리드·사이드바 모두 좌클릭 드래그로 페이지 순서 변경
- 드롭 위치: 파란 세로 인디케이터 (`drag-before` / `drag-after`)
- `movePage(srcIdx, dstIdx, insertAfter)`: pageResults 배열 재정렬 → 페이지 번호 재부여 → 재렌더링
- 이동된 페이지로 자동 스크롤

### 5-12-7. 왼쪽 미니 썸네일 사이드바
- `position: fixed; left: 0; width: 280px`
- 2열 CSS 그리드 레이아웃, 상하 패딩 50px
- 스크롤바: 12px 사각형, 상시 표시
- 클릭 → 해당 페이지로 스크롤 + 0.55s 반전·글로우 애니메이션
- IntersectionObserver: 현재 화면 페이지 → 좌측 2px 액센트 바 표시
- 우클릭 → 메인 그리드와 동일한 컨텍스트 메뉴
- 드래그로 페이지 순서 이동 (메인 그리드와 `dragSrcPageNum` 공유)

### 5-12-8. 메인↔사이드바 hover 동기화
- 이벤트 위임 방식 (`pagesGrid`, `sidebar` 컨테이너 레벨)
- 메인 그리드 hover → 사이드바 대응 항목 파란 배경 하이라이트
- 사이드바 hover → 메인 그리드 대응 항목 파란 테두리 글로우
- `clearAllPeerHover()`: 항상 1개만 유지, 빠른 이동 시 잔류 없음

### 5-12-9. 흑백변환 선택 메인↔사이드바 동기
- `selectPageEl`: 메인 그리드 + 사이드바 이미지 동시 흑백 변환, `sb-selected` 클래스 추가
- `deselectPageEl`: 메인 그리드 + 사이드바 이미지 동시 원본 복원, `sb-selected` 제거
- `renderSidebar` 재렌더링 후 선택 상태·흑백 이미지 자동 복원
- 회전 시 메인 그리드 + 사이드바 이미지 동시 CSS transform 적용

### 5-12-10. 견적서 PDF 저장
- `📤 업무 관리 앱으로 내보내기` 제거
- `📄 PDF로 저장` 추가: Electron `printToPDF()` → A4 PDF → 저장 다이얼로그
- `buildQuoteHTML(forPrint)` 공용 함수 (인쇄·PDF 저장 공유)
- `main.js`: `print:toPDF` IPC 핸들러 (임시 HTML 파일 경유)
- `preload.js`: `electronAPI.printToPDF(html)` 노출

---

## 7. 2026-06-09 버그 수정

### 7-1. Indexed 색공간 이미지 흑백변환 실패 수정 (index.html)
- **원인**: `convertFlateXObjectToGrayscale` 함수 내 `predictor` 변수를 `let`으로 line 2380에 선언했으나, Indexed 처리 블록(line 2334)에서 그보다 먼저 참조하여 **ReferenceError (temporal dead zone)** 발생 → `catch`에 잡혀 조용히 실패
- **수정**: `predictor` 읽기 블록을 함수 최상단(ColorSpace 분기 이전)으로 이동 → Indexed 블록에서도 정상 참조 가능
- **영향 PDF**: `[/Indexed /DeviceRGB N ref]` 형태 팔레트 이미지가 있는 PDF에서 해당 이미지가 컬러로 남아있던 문제 해결

### 7-2. 배포용 단일 HTML 파일 생성 (PDF분析기_배포판.html)
- **파일 위치**: `D:\바탕화면\PDF분析기_배포판.html` (약 178KB)
- **목적**: Electron 없이 브라우저에서 직접 실행 가능한 배포판
- **변경 내용**:
  - 로컬 `./libs/*.js` → CDN (pdf.js 3.11.174 / pdf-lib / pako 2.1.0)
  - `worker-gray.js` → `<script id="workerGraySource" type="text/plain">` 인라인 내장 후 Blob URL 생성
  - `electronAPI.openFile()` → `<input type="file" multiple accept=".pdf">`
  - `electronAPI.saveFile()` → `URL.createObjectURL()` + `<a download>`
  - `electronAPI.printToPDF()` → `window.open()` + `printWin.print()`

### 7-3. 배포용 HTML 렌더링 오류 수정
- **증상**: 브라우저에서 열면 견적서 화면에 `${customer}` 등 템플릿 변수와 JS 소스코드가 그대로 출력됨
- **원인**: `</head>` 문자열 전체 치환 시 `buildQuoteHTML` 함수 내 템플릿 리터럴의 `</style></head><body>`에도 워커 스크립트가 이중 삽입됨
  → 메인 `<script>` 블록 내부에 `<script id="workerGraySource">` 삽입
  → 워커 닫는 `</script>`가 메인 스크립트 블록을 조기 종료
  → `buildQuoteHTML` 이후 모든 JS 코드가 HTML 텍스트로 렌더링
- **수정**: `  </style>\n</head>` (개행+들여쓰기 포함 패턴)로 교체 대상을 한정 → 실제 `<head>` 위치에만 워커 삽입

---

## 8. 2026-06-12 변환속도 최적화 (배포판 HTML #51·52 이식)

> 배포판 HTML에만 적용했던 속도 최적화를 Electron 앱(src + dist\win-unpacked)에 이식.
> 기능 변경 없음 — 흑백변환 결과물은 동일, 처리 속도만 향상.

### 8-1. pako deflate level 6 → 1 (압축 2~4x 속도 향상)
- `worker-gray.js`: flate2gray·icc-flate2gray·기타 재압축 4곳
- `index.html`: JPEG→FlateDecode 변환 등 2곳
- level 1도 PNG Sub 예측 필터와 조합 시 용량 차이 미미

### 8-2. decodeLatin1 → 청크 단위 String.fromCharCode.apply (O(n))
- 기존: 문자 단위 `+=` 누적 루프 (대형 스트림에서 O(n²) 양상)
- 수정: 32KB(0x8000) 청크로 `String.fromCharCode.apply` 후 join
- **주의**: 배포판 HTML에서 썼던 `TextDecoder('latin1')`은 실제로는 windows-1252 디코더
  → 0x80~0x9F 바이트가 €(U+20AC) 등으로 디코딩 → `charCodeAt & 0xff` 재인코딩 시
  문자열 리터럴·인라인 이미지 픽셀 등 바이너리 손상 (0x80→0xAC 등). 사용 금지.
- 0x00~0xFF 전 바이트 256KB 왕복 무결성 테스트 통과

### 8-3. applyOps 정규식 pre-compile
- 기존: 매 세그먼트 호출마다 `new RegExp` 16개 생성
- 수정: 모듈 로드 시 1회 생성 — 워커 `_RE_*` 16개, 메인 `_MS_RE_*` 16개

### 8-4. processXObjects / processExtGState 병렬화 (index.html)
- 순차 `await` 루프 → 작업 배열(`xobjTasks`/`egTasks`) 수집 후 `Promise.all`
- 페이지 내 이미지 여러 장이 동시에 워커 변환됨

### 8-5. 페이지 세마포어 개선 (index.html `applyAndDownload`)
- setTimeout(10ms) 폴링 → Promise 큐 기반 (`semQueue`/`releaseSem`)
- 동시 처리 수: 고정 8 → `Math.max(navigator.hardwareConcurrency, 4)` (SEM_LIMIT)

### 적용 파일
- `src\index.html`, `src\worker-gray.js`
- `dist\win-unpacked\resources\app\src\` 동일 복사 완료

---

## 9. 2026-06-12 Mesh 그라데이션 변환 + Pattern 처리 + 사이드바 UX 개선

### 9-1. Shading Type 4-7 (mesh) 그라데이션 흑백변환 구현 (index.html)
- **증상**: mesh 그라데이션(Gouraud 삼각형·Coons/tensor 패치)이 흑백변환 후에도 칼라로 남음
- **구현**: `convertMeshShading()` — 정점 비트스트림의 색상 성분을 직접 그레이로 재작성
  - Type 4: `[flag][x][y][색상]` 정점마다 바이트 경계 패딩 / Type 5: 연속 패킹
  - Type 6/7: `[flag][12~16점][2~4색]` 패치 단위, flag별 가변 구조
  - MSB-first 비트 리더/라이터로 좌표·flag는 그대로 복사, 색상만 n채널→1채널 변환
  - Decode 배열 좌표 4개 유지 + 그레이 `[0,1]`로 재작성, ColorSpace→DeviceGray
  - FlateDecode 재압축 (level 1), 구조 해석 실패 시(미소비 비트 >128) 원본 유지
- **검증**: Type 4/5/6 합성 데이터 단위 테스트 + 실제 pdf-lib 객체 E2E 테스트 통과

### 9-2. FunctionType 0 (샘플 함수) 변환 추가
- 그라데이션 함수가 Type 2/3가 아닌 샘플 함수(Type 0)인 경우도 그레이 변환
- 샘플 스트림의 RGB/CMYK 튜플 → 그레이 1채널 재작성, Range→[0,1]
- `_shFuncConvertible()` 사전 검사 — 변환 불가 함수(PostScript Type 4 등)는
  CS를 건드리지 않고 통과 (반파 상태로 렌더링 깨지는 것 방지)

### 9-3. Pattern 리소스 처리 추가 — 그라데이션 채우기의 주 경로 ★
- **발견**: `sh` 연산자 외에 **Shading Pattern(scn /P0 채우기)은 지금까지 전혀 미변환**
  → Illustrator/오피스 계열 그라데이션이 칼라로 남던 핵심 원인
- `processPatterns()`: Resources.Pattern 순회
  - PatternType 2 (Shading Pattern): 내부 Shading을 `convertOneShading()`으로 변환
  - PatternType 1 (Tiling Pattern): 자체 콘텐츠 스트림 grayify + 내부 Resources 재귀
- 호출 위치: 페이지 레벨 + Form XObject 내부 + ExtGState SMask 내부
- Background 배열 `[c1..cn]` → `[gray]` 변환 포함, processedObjs로 공유 객체 중복 방지

### 9-4. 사이드바 UI/UX 개선
| 개선 | 내용 |
|------|------|
| 폭 드래그 리사이즈 | 오른쪽 가장자리 핸들(#sbResizer), 180~560px, localStorage 저장 |
| 열 수 자동 조정 | `repeat(2,1fr)` 고정 → `repeat(auto-fill, minmax(110px,1fr))` |
| 접기/펼치기 토글 | 좌측 중앙 탭 버튼(#sbToggle ◀/▶) + **S 단축키** |
| Ctrl/Shift+클릭 선택 | 사이드바에서도 메인 그리드와 동일한 페이지 선택(개별/범위) 가능 |
| 본문 여백 연동 | `padding-left: calc(var(--sb-width) + 12px)` CSS 변수 기반 |

### 적용 파일
- `src\index.html` (worker-gray.js 변경 없음)
- `dist\win-unpacked\resources\app\src\` 동일 복사, 포터블 앱 부팅 스모크 테스트 통과

---

## 10. 2026-06-12 칼라 잔류 근본 수정 — Indexed/Separation 이미지 (test_원본.pdf 분석)

> 사용자 제공 원본/변환본 비교 분석으로 잔류 원인 3가지 확정 후 전부 수정.
> 검증: 잔류 칼라 이미지 623개 전수 변환 테스트 → 성공 623 / 실패 0

### 10-1. 원인 분석 (도구: analyze_residual.js — 보관)
| 원인 | 규모 | 기존 동작 |
|------|------|-----------|
| `[/Indexed /DeviceCMYK]` 이미지의 픽셀 스트림이 /Length 손상으로 잘림 → `pako.inflate`가 undefined 반환 → TypeError | 360개 잔류 | catch로 조용히 건너뜀 (Acrobat·PDF.js는 관대 디코딩이라 화면은 정상) |
| `[/Separation PANTONE… /DeviceCMYK fn]` 별색 틴트 이미지 미지원 | Flate 138 + 무필터 125 잔류 | `csn=''` → 조용히 반환 |
| Filter 없는(비압축) 이미지 전부 건너뜀 | 〃 | `if (!filter) return;` |
| (추가 발견) Separation **DCT** 이미지 1,410개는 변환됐지만 **톤 반전** (픽셀값=틴트 t를 그대로 휘도로 사용) | — | t=1(진한 잉크)→흰색으로 잘못 변환 |

### 10-2. 수정 내용 (index.html + worker-gray.js)
1. **Indexed → 팔레트만 교체** (`convertIndexedImagePalette`)
   - `[/Indexed base N pal]` → `[/Indexed /DeviceGray N <그레이 hex>]`, 픽셀 무수정
   - 손상 스트림도 안전 + bpc 1/2/4/8 모두 지원 + inflate/deflate 생략으로 고속 (이 파일 기준 3,206개)
2. **Separation 이미지 변환** (`convertSeparationImage`)
   - 틴트 함수 평가기(`buildPdfFnEvaluator`: Type 2/0/3 지원)로 256-LUT 생성
   - 무필터/Flate(8bpc): 픽셀 LUT 재작성 → DeviceGray
   - DCT: 워커 jpeg2gray에 LUT 전달 (톤 반전 수정)
   - 스트림 복구 불가 시 폴백: CS만 `[/Separation name /DeviceGray 샘플함수]`로 교체 (렌더링 보존)
   - 이미지 /Decode 배열([1 0] 반전 등)을 LUT에 합성
3. **관대한 inflate** (`inflateLenient` / 워커 `inflateLenientW`)
   - 스트리밍 모드로 가능한 만큼 복구 + 부족분 흰색 채움 (RGB 255 / CMYK 0 / predictor행 0)
   - flate2gray·icc-flate2gray 워커 경로에도 적용
4. **비압축(Filter 없음) RGB/CMYK 이미지** — raw 모드로 변환 (`isRaw` 플래그, 워커 inflate 생략)
5. **명명된 색공간(/Cs1) 해석을 라우팅 단계로 이동** — DCT/JPX/무필터 이미지도 실제 CS 기준 라우팅

### 적용 파일
- `src\index.html`, `src\worker-gray.js` + `dist\win-unpacked` 동일 복사, 부팅 스모크 테스트 통과
- 진단 도구 `analyze_residual.js` 프로젝트 루트에 보관 (사용: `node analyze_residual.js <변환본.pdf>`)

---

## 11. 2026-06-14 로고 사라짐 수정 + PDF 분석 속도 최대화

### 11-1. 로고/이미지 사라짐 버그 수정 — SMask 보존 (index.html)

**원인**  
JPEG·Flate·JPX 이미지 흑백변환 후 `img.dict.delete(Nm('SMask'))` 4곳에서 소프트 마스크를 무조건 삭제하고 있었음.

SMask = DeviceGray 투명도 알파 마스크 XObject.  
- 주 이미지가 DeviceGray로 변환돼도 SMask 자체는 여전히 DeviceGray로 유효하므로 삭제 불필요  
- 삭제 시: 투명 배경 로고(또는 페이드 인 이미지)가 흰색 불투명 직사각형이 됨  
  → 흰색 배경 페이지에서 로고가 시각적으로 "사라지는" 현상

**수정**  
4개 함수의 `img.dict.delete(Nm('SMask'))` 라인 제거:
- `convertSeparationImage` (픽셀 직접 변환 경로)
- `convertJpegXObjectToGrayscale`
- `convertJpxXObjectToGrayscale`
- `convertFlateXObjectToGrayscale` (ICCBased 경로 + 일반 경로)

`SMaskInData`(JPEG 내장 SMask)는 변환 후 무효화되므로 계속 삭제 유지.

**검증**  
test_원본.pdf p2 렌더링: KIU·RISE 로고 정상 표시 확인 (WinRT 렌더링).

---

### 11-2. PDF 드롭 분석 속도 최대화 (index.html)

**변경 전**  
각 페이지마다 두 번 렌더링:
1. `scale=0.5` → 색상 분석 (픽셀 샘플링)
2. `scale=2.5` → 썸네일 JPEG 생성 (1,487×2,105px, A4 기준)

**변경 후**  
단일 렌더링으로 색상 분석 + 썸네일 동시 처리:

```javascript
// 0.5x 한 번 렌더링 → 색상 분석 + 썸네일
const vp = page.getViewport({ scale: 0.5 });
await page.render({ canvasContext: ctx, viewport: vp }).promise;
// 색상 분석 (sampleStep 기준 30000 → 15000)
const isColor = gray / sampled < 0.999999;
// 썸네일: 같은 캔버스 → toDataURL (quality 0.92 → 0.85)
const thumbnail = canvas.toDataURL('image/jpeg', 0.85);
```

| 항목 | 변경 전 | 변경 후 | 효과 |
|------|---------|---------|------|
| 렌더링 횟수 | 2회/페이지 | 1회/페이지 | **~2배** 속도 향상 |
| 썸네일 해상도 | 2.5x (1487px) | 0.5x (297px) | 메모리 ~25배 절감 |
| sampleStep 기준 | 30,000픽셀 | 15,000픽셀 | 색상 분석 약 1.5배 향상 |
| JPEG 품질 | 0.92 | 0.85 | toDataURL 소폭 빠름 |
| CONCURRENCY | 16 고정 | `min(32, max(8, hardwareConcurrency))` | CPU 코어 수 자동 활용 |

A4 24페이지 기준 예상 개선: 약 2~3배 빨라짐.

### 적용 파일
- `src\index.html` + `dist\win-unpacked` 동일 복사 완료

---

## 6. 주요 설정값

### main.js webPreferences
```js
webPreferences: {
  preload: path.join(__dirname, 'preload.js'),
  contextIsolation: true,
  nodeIntegration: false,
  sandbox: false,   // preload에서 fs/path 사용 허용
}
```

### 줌 단축키 (main.js)
```js
win.webContents.on('before-input-event', (event, input) => {
  if (!input.control || input.type !== 'keyDown') return;
  if (input.key === '=' || input.key === '+') wc.setZoomLevel(wc.getZoomLevel() + 0.5);
  else if (input.key === '-')                  wc.setZoomLevel(wc.getZoomLevel() - 0.5);
  else if (input.key === '0')                  wc.setZoomLevel(0);
});
```

### IPC 채널 목록
| 채널 | 방향 | 역할 |
|------|------|------|
| `dialog:openFile` | renderer → main | 파일 열기 다이얼로그 (경로 반환) |
| `dialog:saveFile` | renderer → main | 저장 다이얼로그 + 파일 쓰기 |
| `shell:openBizApp` | renderer → main | 업무 관리 앱 BrowserWindow로 열기 |

### preload.js 노출 API
| 함수 | 역할 |
|------|------|
| `openFile()` | 파일 열기 다이얼로그 IPC 호출 |
| `readFile(path)` | Node.js fs로 파일 읽기 → ArrayBuffer 반환 |
| `saveFile(opts)` | 저장 다이얼로그 IPC 호출 |
| `openBizApp()` | 업무 관리 앱 열기 IPC 호출 |
| `getWorkerContent()` | PDF.js 워커 파일 내용 반환 (Blob URL용) |
