# 2026-08-28 작업내역

> 여러 파일을 한 번에 열 때 쓰는 **📚 열 순서 정하기** 창을 실무에 맞게 뜯어고치고,
> 저장 다이얼로그의 기본 폴더를 **문서를 연 폴더**로 바꿨다.

---

# 1. 열 순서 정하기 — 통합 ▲▼ + 자유 드래그

## 문제
- 파일마다 ▲▼ 버튼이 붙어 있어 목록이 시끄럽고, 한 칸씩만 옮겨졌다.
- 드래그가 **"행 위에 떨어뜨리기"**만 지원해서 원하는 자리에 못 넣었다.

## 바꾼 것 (`src/app-core.js`, `src/index.html`, `src/style.css`)
- 행별 버튼 제거 → 목록 위 **고정 이동 바 하나**: `⤒ 맨 위` `▲` `▼` `⤓ 맨 아래`
  - 행을 **클릭해 선택**해야 활성화(선택 행 노란 테두리, 바에 `3번 · 파일명` 표시).
    같은 행 재클릭 = 선택 해제.
  - **선택이 옮겨진 파일을 따라간다**(`_orderSel = to`) → 연속 클릭으로 몇 칸이든 이동.
    이게 없으면 한 번 누른 뒤 선택이 제자리에 남아 다음 클릭이 엉뚱한 파일을 옮긴다.
- 드래그: 커서 **중점 기준 삽입 위치**(`dropIndex`) 계산 + 노란 선으로 자리 미리보기.
  - 함정: 뺀 다음 넣으므로 **`at > from`이면 `at--`** 보정이 필요하다(안 하면 한 칸씩 밀림).
- 키보드: `↑↓` 선택, `Ctrl+↑↓` 이동, `Ctrl+Home/End` 맨 위/아래, `Esc` 닫기.
- 이름순 정렬 후에도 **선택한 파일이 유지**된다(인덱스가 아니라 객체로 다시 찾음).

## 창 UI
- 닫기는 **제목줄 우상단 ✕**(하단 '취소' 제거), 하단 버튼은 **`📄 개별창` / `📚 통합창`** 2개.
- 하단 버튼 한 줄 고정: `.ord-foot { flex:0 0 auto; white-space:nowrap }` + 푸터 `flex-wrap:nowrap`.
- **크기 조절**: CSS `resize:both`는 **우하단 모서리에서만** 잡혀 실무에서 불편 →
  손잡이 3개를 직접 구현(`.ord-grip-r` 우측=가로, `.ord-grip-b` 하단=세로, `.ord-grip-c` 모서리=동시).
  - `max-width/height: none` + 모달 오버레이 `overflow:auto` → **화면보다 크게** 키워도 안 잘림.
  - 최소 380×300, 최대 8000. 크기는 `localStorage.orderPanelSize`에 기억, 손잡이 **더블클릭 = 기본 크기**.

# 2. 순서대로 '한 파일'로 합쳐 열기

- 기존에는 여러 파일을 열면 **파일마다 탭**이 생겼다(이미 분석된 문서가 있을 때만 챕터로 합쳐졌다).
- `openMergedAsChapters(files)` 신규 — 정한 순서대로 이어붙여 `첫파일명_합본.pdf` 한 탭으로 연다.
  `chapters=[{name,start,count}]`를 탭에 실어 두면 분석 종료 시 페이지마다 `chapter`가 태깅돼
  편집 사이드바의 챕터 범위·그리드 구분이 그대로 동작한다(= `appendImportedFiles`와 같은 규약).
- **파일 열기 다이얼로그에서 2개 이상 고를 때도** 순서 창이 뜬다(전에는 '보내기'로 받을 때만).

# 3. 저장 기본 위치 = 문서를 연 폴더

- `main.js`의 `_lastSaveDir`(저장 다이얼로그 기본 폴더)를 **문서를 연 폴더로 선점**한다.
  - `preload.setSaveDir(dir)` → `ipcMain.on('app:docDir')` (존재하는 디렉터리일 때만 반영)
  - 렌더러 `reportSaveDir(path)`를 `prepareFiles`(모든 열기 경로)와 `openWorkFilePath`(.pdfw)에서 호출.
- 저장 call site 12곳을 건드리지 않는 게 요점 — **채널 하나로 기본 폴더만 바꾼다**.
  이후 사용자가 다른 폴더에 저장하면 종전처럼 그 폴더가 이어서 기본값이 된다.

# 검증

| 대상 | 방법 | 결과 |
|---|---|---|
| 순서 이동·선택·드롭 인덱스 | `scripts/test/order-dialog.test.js` — app-core.js 실제 함수를 떼어 DOM 스텁 위에서 실행 | 19/19 |
| 창 크기 조절·푸터 한 줄 | `scripts/test/order-resize.e2e.js` — 오프스크린 BrowserWindow에 실제 index.html+preload, 포인터 드래그 주입 | 14/14 |
| 저장 폴더 전달 | `scripts/test/savedir.e2e.js` — 실제 파일 열기 경로 → preload → main 수신 확인(역슬래시 포함) | 4/4 |
| 합본 | 실제 `openMergedAsChapters`를 3개 PDF(3+2+4쪽)로 구동 → 9쪽·챕터 `[1-3][4-5][6-9]`, gs `txtwrite`로 `A-1…C-4` 순서 확인 | ✅ |
| 회귀 | `npm run smoke` | ✅ |

## 남은 메모
- 창 밖에서 파일을 **드래그&드롭**으로 넣는 경로는 File 객체라 디스크 경로가 없어
  순서 창·저장 폴더 지정 대상이 아니다(경로가 있는 열기만 해당).
