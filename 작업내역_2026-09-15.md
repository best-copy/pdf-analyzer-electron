# 2026-09-15 작업내역

> **전체 안정성 점검 — 드러나지 않던 오류를 옛 코드로 재현하고, 새 코드로 사라진 것을 확인했다.**
>
> 검토자 3명(요구사항·품질·보안연동)으로 전체 코드를 훑고, 찾은 것마다 "옛 코드에서 실제로 틀리는가"를 먼저 재현했다.
> 흑백 출력은 "화면이 회색"이 아니라 **프린터가 흑백으로 세는가(gs inkcov CMY 0)**를 기준으로 끝까지 추적했다.

---

# 1. 검사 자체가 거짓 통과하던 문제 — smoke

## 원인
사용자가 앱을 켜 두면 단일 인스턴스 잠금 때문에 검사용 앱이 곧바로 꺼졌다. 로그가 비어 "오류 없음 = 통과"로 나왔다.

## 처리
- 검사 모드(`TEST_WINDOW=left`)는 사용자 폴더를 따로 쓰고, 임시파일 정리·원격 서버·인쇄 감시·라이선스 창을 돌리지 않는다
- 앱이 세 app-*.js의 전역 함수를 확인해 `[SMOKE] BOOT_OK`를 찍어야 통과

---

# 2. 흑백변환 색 오류

## 2-1. 이미 회색인 색이 흰색으로 반전
| 증상 | 원인 |
|---|---|
| `/DeviceGray cs 0 sc`(검정)가 흰색 → 글자 사라짐 | 성분 1개를 별색 틴트로 짐작해 1-t |
| DeviceN 2·5성분 `0.3 0.8 scn` → `0.3 0.2000 g` | 뒤 숫자만 잡혀 피연산자 남음 → Acrobat 페이지 오류 |
| 팬톤 명함 원고가 흰색 | 대체 색공간이 **Lab ICC(N=3)** 인데 RGB로 계산 |

## 2-2. 처리
- 색공간 정의를 설명자로(`buildCsDesc`) → 워커 `csToGray`가 실제 색을 계산 (별색·DeviceN 함수 Type 0/2/3/4, Indexed, Lab, ICC 헤더 서명)
- 사용자 지시: **모르는 조합도 DeviceGray로** (컬러로 남기면 프린터 과금) — 성분 수 추정으로라도 회색
- 콘텐츠 스트림을 못 풀었으면(LZW·inflate 실패) 그 쪽의 색공간 리소스를 지우지 않음(`streamSkipped`)

## 2-3. 검증
실제 PDF 28개·108쪽 옛/새 변환 → gs 렌더로 원본 밝기 대조: 큰 편차 691 → **0**, 흰색 반전 픽셀 기존 수준 유지, gs 오류 0. 단위 테스트 `gray-colorspace.test.js`.

---

# 3. 흑백 사진 → DeviceGray JPEG (사용자 결정)

## 원인
Chromium 캔버스는 1성분 JPEG를 못 만들어 회색 사진을 R=G=B **RGB JPEG**로 담았다 → gs inkcov에 CMY가 잡혀 프린터가 컬러로 셀 수 있음.

## 측정 (결정 자료, 실파일 10개·사진 158장)
| 방식 | 용량 | 화질 |
|---|---|---|
| 기존 RGB JPEG | 4.40MB (100%) | 42.4dB |
| **DeviceGray JPEG** | **4.12MB (94%)** | 42.4dB (같음) |
| Flate 무손실 | 177~213% | 원본 |

측정 함정: 표준 허프만 표로 재면 108%로 나와 오판 — 이미지별 최적 허프만(부록 K.2)이어야 공정한 비교.

## 처리
- `src/libs/gray-jpeg.js` 자체 인코더(IJG 양자화표·최적 허프만, q82) — 측정본과 바이트 동일, 20MP 0.33초
- 워커 `jpeg2gray`가 이 인코더로, PDF에는 `/DeviceGray` 기록. 실패 시 Flate 폴백

## 검증
28개 파일: 사진 CMY 9개 → 0, 밝기 동일, 용량 같거나 작음(854→754KB 등), pdf.js 렌더 차이 ≤0.014/255. 단위 테스트 `gray-jpeg.test.js`(jpeg-js로 되읽어 구조·화질).

---

# 4. 흑백인데도 남던 컬러 잉크 세 원인

| 파일 | 원인 | 처리 |
|---|---|---|
| 목차 PDF | 색상표가 **ASCII85** 스트림 → Flate만 풀어 이미지를 건너뜀 | `pdfStreamDecoded`(모든 필터, 예측자 있으면 모름) — 색상표·ICC·함수 표본 공용 |
| A1 포맥스 | **페이지 투명도 그룹 없음** → gs·RIP이 격리 그룹 폼(/I true)을 출력 색공간에서 합성, 회색이 C=M=Y+K | 저장 직전 `savePdfDoc` → `libs/gray-blend.js` |
| 교안 PDF | 인라인 이미지 사전 안 색상표 `/CS[/I/RGB 15 <…>]` | 워커 `inlineIndexedToGray` — 색상표만 회색 |

## 4-1. 포맥스 — 처음 짚은 원인이 틀렸다
- 처음엔 SMask 안의 `/Group /CS /DeviceRGB` 3개를 의심 → 폼 안 ExtGState 순회 누락을 고쳤지만 CMY 그대로
- 실험: 페이지에 `/Group <</S/Transparency/CS/DeviceGray>>`를 달면 CMY 0, 같은 양이 K로
- 흑백변환 쪽만 고치면 안 됨: 임포징 `embedPage` 판에는 그룹이 없어 되살아나고, 폼에 그룹을 달아도 무효(실측)
- 그래서 **모든 저장이 지나는 `savePdfDoc`**(app-core·worker-assemble·editor·임포징 독립 도구)에서
  "그룹 없음 + 투명도 사용 + 그리는 색이 전부 무채색"인 쪽에만 DeviceGray 그룹을 단다
  - 컬러가 한 점이라도 있으면 달지 않음(색이 회색으로 합성됨)
  - 투명도 없는 쪽은 달지 않음(Acrobat 인쇄 평탄화 방지)
- 재현 함정: 단순 반투명(ca 0.5)·SMask 이미지로는 gs에서 CMY가 안 생긴다 → 합성 E2E를 격리 그룹 폼으로 만들고, 기능을 끈 대조 실행으로 실패를 확인

## 4-2. 검증
- 실파일 28개(앱의 `savePdfDoc` 경로): CMY 남은 파일 **11 → 0**, 밝기 동일, gs 오류 0
- `gray-blend-imposition.e2e.js`: 흑백 적용·중철 판·다운로드 세 경로 inkcov CMY 0 (끄면 셋 다 0.16)
- 단위 테스트 `gray-blend.test.js` 19개(컬러 섞인 판·별색·RGB 사진·인라인 이미지·문자열 속 연산자 글자)
- 비용: 겹친 그림 많은 포스터 쪽 약 10ms/쪽, 일반 쪽 1ms 미만(스캐너를 바이트 단위로 다시 짜고 Flate는 pako — 처음 판의 2배)

---

# 5. 캐시·탭 전환 — 빈 A4와 섞인 결과

| 증상 | 원인 | 처리 |
|---|---|---|
| 810쪽 적용본 앞 10쪽이 빈 A4 | `_bwCache` 800 FIFO가 방금 넣은 쪽을 지움 | 상한 제거 |
| 파일 교체·내부편집 중 옛 프리웜 결과가 섞임 | 탭 id만 비교(교체는 id 유지) | 캐시 세대 `_cacheGen` — 쓰기 전·붙이기 전 비교 |
| 적용 중 탭 전환 → 결과가 새 탭에 붙음 | 같은 원인 | 사용자 결정: 전환 허용, 끝난 결과는 버리고 안내 |

검증 `cache-generation.e2e.js` 15개(옛 코드에서 실패 재현).

---

# 6. 저장·파일·변환 안정성

- **.pdfw 저장 중 실패 시 원본 0바이트** → `.part` 쓰기 + fsync + rename(`writeFileSafe`)
- **2GB 넘는 작업 파일 저장 실패** → 원본·적용본을 합치지 않고 조각별로 이어 쓰고(`packWorkFileParts`·`writeBig`) 구간 읽기로 열기. 형식 동일. 압축 저장은 실측 86.6%라 기각. 2.4GB 실저장 11초·열기 11초·바이트 일치 (`workfile-2gb.e2e.js`)
- **핫폴더 C:→D:/NAS에서 rename EXDEV → 무한 재처리** → 복사+삭제 폴백(`hfMove`)
- **COM 변환(한글·Office·Adobe)**
  - 사용자 결정: 켜져 있던 Adobe 앱은 닫지 않고 우리가 연 문서만 닫기
  - 새로 띄운 프로세스만 `COMPID:n`으로 알리고 시간 초과 시 그것만 taskkill
  - Office 매크로 차단(`AutomationSecurity=3`)
- **편집기 미세회전이 /Rotate 잃음** → 굽는 쪽에 원래 회전 유지
- **WorkerPool onerror** 시 죽은 워커 제거(중복 등록 방지)
- **2GB 줄이기 임시파일(pdfedit_shrink/split)**이 정리 대상 밖이라 수 GB 남음 → 화이트리스트 추가

---

# 7. 보안 — 원격 서버·인쇄

- **`/print/topdf`가 file:// 로 로컬 파일을 PDF로 유출**(옛 코드 실측 재현) → 인쇄 전용 세션, 임시 HTML(realpath 비교)·data:/blob:만 허용, JS 끔, 30초 제한
- 원격 인증 실패 제한을 IP별로(토큰 없는 요청은 세지 않음)

---

# 8. 기능 요청

- **◐ Dot Gain 4단계**(없음·10·15·20%) — 사이드바 ⚙ 처리옵션·상단 옵션 줄 연동, 앱 전체 설정, 컬러→흑백 쪽만 적용, 캐시 서명에 포함. 곡선 식 한 벌로 정리(`dotGainCurve`, 워커와 대조 테스트). `dotgain.e2e.js`
- **파일명 '정합4up' 통일** — [📖 임포징 생성] 저장도 다운로드와 같은 `정합Nup`
- **임포징 독립 도구 빌드**에 `savePdfDoc`·`gray-blend.js` 포함 + 빠진 함수 빌드 시 검사

---

# 9. 검증 요약

- `npm run smoke` 통과 — 단위 테스트 7종(seam-repair·inline-image 42·gray-colorspace 26·gray-jpeg 25·gray-blend 19·workfile·download-name)
- E2E: gray-blend-imposition 7, imposition-standalone 68, imposition-perchapter 26, append-merge 9, chapter-edit 14, close-save 13, replace-file 21, workcache 15, inknorm-default 10, dotgain 8, rgb-gray-warn 10, workfile-close-clean 5, chapter-focus-workfile 16, hf-editor 11, imposition-marks 16, merge-orient 7, cache-generation 15, workfile-2gb 4 — 전부 통과
- 포터블 앱 16개 파일 체크섬 일치, `dist/임포징도구.html` 재빌드
- **확인 못 한 것**: Acrobat·실제 프린터 출력, PowerPoint·HWP·Adobe 실변환(Word만 실변환), 한글 보안창 자동 클릭이 사용자 한글 창을 누를 수 있는 문제(실화면 클릭이 필요해 보류)

# 10. 작업 함정 (다음 작업자용)

- Git Bash `sed -i`가 CRLF 파일을 LF로 바꾼다. 셸 heredoc·`node -e`는 `\\`를 먹는다 → 편집 스크립트는 파일로 써서 실행
- 줄 단위 치환 스크립트는 줄바꿈 형식부터 확인 — CRLF로 가정해 split하다 테스트 파일 전체를 덮어쓴 일이 있었다(HEAD에서 복구)
