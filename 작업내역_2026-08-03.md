# 작업내역 2026-07-30 ~ 08-03

## 1. 임포징 정리
- 프로파일 불러오기 = **옵션만 적용** (편집 적용은 '📖 임포징 PDF 생성' 또는 메인 '✔ 적용'을 눌러야 진행)
- 파일명 꼬릿말: 복제2부 → `2up`, 정합 → `1up` / 버튼명 `반반 (Cut&Stack)`

## 2. 안드로이드 완전판 (1·2단계)
- **1단계 — PC 내장 LAN 변환 서버** (`remote-server.js`): HWP/Office/Adobe 변환·잉크판정·견적서 인쇄를 폰에서 위임.
  QR 접속(토큰 인증·무차별대입 차단), 폰 브라우저 테스트 페이지, APK 다운로드(/apk), 딥링크(pdfeditor://) 앱 등록.
  변환은 main.js의 기존 COM 큐를 IPC와 공유(직렬화). ※ 이 PC는 공인 IP — 스위치는 쓸 때만 켜기 권장. (현재 UI 숨김 — 추후 사용)
- **2단계 — Capacitor 앱** (`mobile/`): 분석·흑백·임포징·견적은 폰 단독. mobile-bridge.js(electronAPI 어댑터, mob:// 가상경로),
  mobile-boot.js(워커 프리로드 후 core→process→ui 순차 로드), build-www.js(데스크톱 소스 무수정 빌드 변환),
  WolPlugin.java(⚡ PC 깨우기 — UDP 매직패킷). APK 빌드·실기기 설치 완료. 빌드 함정: `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=D:\Tools\tmp` 필수 (mobile/README.md).

## 3. 내부 편집기 대개편 (editor.html)
- **원본 폰트 100% 유지 글자수정**: 콘텐츠 스트림의 글자 코드만 교체(scanShowOps·ToUnicode 역변환·applyOneStreamEdit).
  서브셋에 없는 글자만 오버레이 폴백+경고. 더블클릭=편집(클릭은 무해), 무변경 시 원본 자동 복원, Enter=저장·Ctrl+Enter=줄바꿈.
- **🔍 찾기·바꾸기**(Ctrl+F): 현재/전체 페이지 일괄 치환 — 원본 폰트 유지.
- 선굵기 개선 4종: 기본 1pt·소수 두께(mm 병기), 실치수 기준 줌(1:1·⤢), 선 SVG 렌더(AA), HiDPI 캔버스.
- 줄바꿈 화면·저장 일치(wrapTextLines), 얇은 선 히트영역 8px, 이미지도 더블클릭 추출, 재편집 원본폰트 복원,
  색상 스포이드(💧), 스냅 정렬 가이드(Alt 해제).

## 4. 실무 도구 4종
- **🧪 프리플라이트**(메인 헤더+사이드바): 폰트 미임베드·저해상도 이미지(실효 dpi)·크기 혼재·RGB·블리드·주석 검사 리포트.
- **◲ 블리드 자동 생성**: 8방향 미러 확장 + TrimBox 기록 (벡터 유지, gs 시각검증).
- **✒ 폰트 아웃라인화**: 버튼→**체크 옵션**으로 개편 — '✔ 편집 적용'·'⇩ 다운로드' 최종 단계에 일괄 반영, 프리셋 저장·복원 지원. 평탄화 옵션.
- **📑 목차 생성**: 선택 페이지=장 시작, 제목 자동 인식(상단 최대 폰트 라인) → 목차 페이지를 **실제 페이지로 맨 앞 삽입**(원본 보기 표시·내부 편집기로 수정 가능·재생성=교체) + 북마크는 다운로드 시 태그 기반 적용(재배열 추적).

## 5. OS 연동
- **보내기 메뉴** 자동 등록(포터블 실행 시) + 단일 인스턴스(기존 창으로 전달). 다중 문서는 **📚 열 순서 정하기** 다이얼로그(드래그·▲▼·이름순) 후 챕터 병합.
- **가상 프린터 'PDF Editor'**: 어떤 앱에서든 인쇄→이 앱으로 수신(페이지 범위 지정 가능). 앱 꺼져 있어도 **상주 감시자가 앱 자동 실행**(WMI 기동 — Electron spawn은 앱 종료 시 동사(同死)하는 함정 실측), 시작 시 대기 인쇄물 자동 열기, 설치 버튼은 미설치 시에만 표시.

## 6. 버그 수정
- **다운로드 재조립이 외부 변환(아웃라인·블리드) 결과를 소실** → `directOutputBytes` 도입(그대로 저장 경로).
- **회전+크기변경 시 180° 어긋남**: worker-assemble `drawFit`이 /Rotate(시계방향)를 반시계로 그림 → 방향 수정, gs 정답 렌더(90/270°) 대조 검증.
- **선택=흑백 오동작**: 선택은 순수 선택(회전·복제·목차)으로 — 흑백 미리보기·카운트는 '⬛ 흑백변환' ON일 때만. 미리보기 선택 시 bw 몰래 켜던 `ensureBwOn` 제거, 메인·사이드바 버튼 동시 동기화.
- 다중 선택 회전 일괄 적용(+썸네일 즉시 반영), 회전 시 썸네일 **대지(판형) 스왑**, 빈 곳 클릭 선택 해제(문서 전역, Ctrl/Shift 예외),
  목차 생성 후 그리드 미갱신(refreshResults는 카운트만 — renderAllPages 직접 호출), 진행효과(.btn-busy) 장시간 버튼 전체 적용.

## 7. UI
- 처리 옵션 버튼 규격 통일(38px·radius10·B&Y), 프린터판정→프리플라이트→해제 순 재배치.
- 편집 사이드바: 임포징 최상단, 회전·조판 섹션 제거(조판은 임포징 N-up으로 통합).

검증: 매 변경 `npm run smoke` + 순수 함수 노드 추출테스트(스트림 치환 15건·랩핑 7건·목차·블리드·drawFit gs 렌더 대조) + 실기기(폰)·실환경(프린터) e2e. 전 변경 포터블(dist/win-unpacked) 동기화 완료.
