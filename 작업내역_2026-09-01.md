# 2026-09-01 작업내역

> 닫기 확인·임시파일 정리, **챕터 집중 편집**, **챕터(파일)별 임포징**, 임포징 거터 밀착,
> 그리고 실사용 사고 2건(**프린터가 흑백을 컬러로 셈**, **저장 폴더가 Temp**) 수정.

---

# 1. 닫기 확인과 임시파일 정리

## 1-1. 파일만 열고 닫아도 저장을 묻는다

닫기 확인은 '저장 안 한 편집'이 있을 때만 떴다. 분석·설정만 해 둔 상태도 되살릴 가치가 있어
**문서가 열려 있으면** 묻도록 넓혔다. 버튼 4개:

`💼 작업 저장하고 닫기` / `📁 다른 이름으로 저장하고 닫기` / `저장 안 하고 종료` / `취소`

`.pdfw`를 열었거나 한 번 저장했으면 그 파일에 덮어쓰고(버튼에 파일명 표시), 아니면 위치를 묻는다.
탭마다 작업 파일 경로(`workPath`)를 기억한다. preload에 `saveFileTo`, main에 `dialog:confirmSavePath` 추가.

**후속 수정**: "저장했는데 또 묻는" 문제 — 저장 성공 경로가 상태를 갱신하지 않았다.
탭의 `workSaved` 플래그(저장·작업파일 열기 시 true, `setDirty(true)` 시 false)를 도입하고
`reportDocState()`가 `isTabReady(t) && !t.workSaved`인 탭만 보고하게 했다.

## 1-2. 종료 시 임시파일 정리

`sweepTempConversions`의 정규식이 **`pdfedit_aicover_*.png`(표지 AI 생성본)를 빠뜨리고** 있었다 →
`pdfedit_.*\.(pdf|bin|png)`로 수정.

**검증 2단계**: (a) main.js에서 함수를 추출해 가짜 tmpdir에 실제 생성 이름 11종을 깔고 실행
(무관한 파일 3개는 보존), (b) 실제 앱을 띄우고 **부팅 후에** 임시파일을 만든 뒤 WM_CLOSE로 정상 종료 → 사라짐 확인.
`child.kill()`은 강제 종료라 `will-quit`가 돌지 않으므로 이 검증에 쓸 수 없다.

---

# 2. 📄 챕터 집중 편집

챕터의 `✏ 편집`은 **설정만** 그 챕터에 적용됐고(원래 정상), 화면에는 문서 전체가 보였다.
이제 편집 모드 화면에도 **그 챕터의 쪽만** 나온다. 쪽번호는 문서 기준 그대로.

미리보기 경로가 **셋**이라 전부 손봐야 했다:

| 경로 | 조건 | 필터 방법 |
|---|---|---|
| 전체 렌더 `renderProcessedPreview` | 16쪽 미만·임포징 등 | `computeOutputSourceMap`으로 출력쪽→원본쪽 매핑 후 선별 |
| 표본 미리보기 `runWorkspaceSamplePreview` | 16쪽 이상 | `sel`(문서 인덱스 목록)로 복사·마스크 추림, `opts.pick`으로 로마자 번호 정렬 |
| **편집 전 화면** `showWorkspaceBasePreview` | 수정사항이 없을 때 | `source:'original'` — 빈 페이지 삽입·삭제로 쪽 수가 어긋나면 srcMap을 못 쓰므로 `originalIdx`로 선별 |

세 번째가 **예전 작업 파일에서 안 좁혀지던 실제 원인**이었다(실사용 파일: 343쪽·13챕터·빈 페이지 3장).

**작업 파일 재열기**: 저장된 `scope.mode='chapter'`를 그대로 되살리면 다시 열어 편집을 눌렀을 때
지난번 그 챕터만 열려 "문서가 일부만 열린다"로 보였다 → 열 때 범위를 `all`로 되돌린다
(챕터별 설정 `byChapter`는 유지되어 결과는 동일).

---

# 3. 📖 챕터(파일)별 임포징

합본을 **파일마다 독립 대수로 걸어 이어붙이는** 옵션 `📄 챕터(파일)별로 따로 임포징`(합본일 때만 표시).
중철이면 파일마다 별도 소책자가 되어 합본 한 번의 인쇄로 파일별 제본이 나온다.

- 구현은 `buildImposedBytes` 한 곳 — 미리보기·적용·다운로드가 모두 이 함수를 지난다.
  `computeOutputSourceMap`으로 구간을 나눠 각각 build → 이어붙임. **양면일 때 구간 시트 수가 홀수면
  빈 시트 한 장**을 넣어 다음 파일이 시트 앞면부터 시작하게 한다.
- `📖 임포징 PDF 생성` 버튼은 저수준 빌더를 직접 부르므로 공용 `imposeForGenerate()`로 묶어 네 모드가 함께 따라오게 했다.
- 구간(`_impChapterRanges`)을 기록해 챕터 집중 시 그 파일의 대수만 화면에 보여준다.

## 3-1. ❗결과에서 다른 챕터를 지우면 안 된다

중간에 "적용 범위가 챕터면 그 챕터만 남기는" 방식으로 만들었더니, 메인으로 나왔을 때 **다른 파일이 통째로 사라지고**
작업 파일을 다시 열면 그 결과가 잠깐 보였다가 전체로 바뀌었다. 되돌렸다:

- **결과물(적용·다운로드·생성)에는 언제나 문서 전체**가 들어간다.
- 챕터 집중은 **편집 화면의 보기 범위**일 뿐이다.
- 파일별 분리는 오직 `📄 챕터별로 따로 임포징` 옵션이 한다.

## 3-2. 성능 — "편집 내용 반영 중"이 10초

343쪽 34MB 실측: 임포징을 켠 채 편집 옵션을 하나 바꾸면 **10.7초**(전체 재조립 + 172시트).
챕터 집중 + 챕터별 임포징이면 그 챕터만 잘라 조판·임포징하는 `runFocusedImposedPreview`로 **0.71초**.

> **측정 함정**: 오프스크린(`show:false`) 창은 requestAnimationFrame이 1fps로 조여져 pdf.js `page.render`가
> **페이지당 정확히 1초**로 보인다. 해상도를 낮춰도 1초 그대로면 이 함정. 반드시 `show:true`로 재야 한다.

## 3-3. 끈 '임포징' 체크가 되살아남

프리셋 불러오기·수정·임포징 생성 셋이 체크를 강제로 켜고 있었다 → `_impUserOff`(직접 끔)를 존중하고,
그 경우 생성 결과는 `directOutputBytes`로 채택해 화면과 저장 파일을 맞춘다.

---

# 4. 임포징 거터 0 — 맞닿게

시트를 균등 분할한 칸 **가운데**에 원고를 앉히다 보니, 16:9 가로 원고를 세로 A4에 1열 2행으로 놓으면
칸(595×421)보다 원고가 납작(595×335)해 **거터가 0인데도 86pt가 떠 보였다.**

칸을 실제 그려질 재단 크기로 좁히고(`drawnTrimSize`) 배치 블록 전체를 정렬 → 간격 = 거터 값 그대로.

**검증**(gs ppmraw 픽셀 측정): 거터 0 → 흰 줄 **0px**, 거터 10mm → **28px**(28.35pt).
100%·지정배율이라 원고가 칸보다 크면 기존 그대로. 중철·복제 2부는 접지 기준이라 손대지 않음.
독립 도구(`dist/임포징도구.html`)도 같은 빌더를 추출하므로 `BUILDERS` 목록에 헬퍼를 추가하고 재빌드.

---

# 5. 실사용 사고 — 프린터가 흑백을 컬러로 셈

## 5-1. 진단 (test.pdf 10쪽)

| 항목 | 값 |
|---|---|
| 150dpi 렌더 비중성 픽셀 | **0개** (눈에도 완전한 흑백) |
| 원본 콘텐츠 스트림 | `0.199951 0.199951 0.199951 rg` ← **DeviceRGB로 칠한 회색** |
| 원본 inkcov | C=M=Y=**0.00006** → 기기가 **컬러로 과금** |
| 앱 출력(적용·다운로드·정규화만·임포징·블리드·gs 안전화) | 전부 `0.2000 g`, inkcov **0/0/0** |

즉 **앱 흑백 처리에는 결함이 없다.** 원고가 회색을 RGB로 칠했고, RIP이 RGB→CMYK로 바꾸며 미세한 CMY를 만든다.

## 5-2. 두 번째 사례 (칼24흑10.pdf)

앱이 만든 분리 출력물인데 기기가 32/2로 셈. 흑백 구간 10쪽이 전부 `0.00006` — test.pdf와 **소수점까지 같은 값**.
혼합 문서(컬러 4쪽 + RGB회색 10쪽)로 재현하니 **잉크 정규화를 끈 경우에만** 그 값이 나온다.

| 설정 | 흑백 구간 |
|---|---|
| 정규화 ON / 흑백변환(선택) / 흑백변환(전체) | 컬러 잉크 **0쪽** |
| **정규화 OFF** | **10쪽 모두 0.00006** ← 배달된 파일과 동일 |

## 5-3. 조치

1. **감지 경고** `docPaintsInColorSpace()` — 컬러 판정 0쪽인데 `rg/RG/k/K`가 있으면 분석 결과 패널에 경고.
   토스트에 쓰면 '지난 작업 기록' 안내에 덮이므로 패널에 남긴다. 요약을 그리는 지점이 4곳이라 전부 부착.
   > **pako 함정**: `stream`~`endstream`을 그대로 넘기면 끝의 개행 때문에 `pako.inflate`가 통째로 실패한다
   > (node zlib는 통과 → 노드 검증만으로는 안 보인다). 개행을 자르고, 실패 시 `inflateLenient`로 재시도.
2. **저장 직전 확인창** — 정규화가 꺼진 채 저장하려 하면 "흑백 N쪽이 컬러로 과금됩니다" 확인.
3. **잉크 정규화는 항상 기본 켬** — 꺼지는 경로 셋을 막았다:
   - `✖ 해제` 버튼이 처리 옵션 전부를 끄던 것 → 정규화는 켠 채 유지
   - 프로파일·최근작업·작업파일 복원이 저장된 `false`를 되살리던 것 → **꺼짐 값은 무시**(켜짐만 반영)
   - 직접 끄기는 가능하되 **안내를 띄우고, 새 문서를 열면 다시 켜진다**

---

# 6. 저장 폴더가 Temp로 열리던 문제

가상 프린터 접수본은 `%TEMP%\pdfedit_print_*.pdf`에 놓이는데, 렌더러가 "문서를 연 폴더"를
저장 기본 위치로 보고해 저장창이 `AppData\Local\Temp`를 가리켰다.

- `isTempPath()`로 **임시 폴더는 저장 위치 후보에서 제외**(HWP·Office 변환본도 함께 해결).
- 인쇄 작업명(PrintService 로그 / 인쇄 큐)은 **전체 경로로 오는 경우가 많다** → `printedSourceDir()`로
  그 폴더를 뽑아 저장 기본 위치로 쓴다(= 바탕화면에서 인쇄하면 바탕화면).
- 경로를 못 얻으면 `printSaveDir()`가 **바탕화면**으로 폴백. 마지막 폴백도 바탕화면 → 다운로드.

---

# 7. 테스트

| 파일 | 내용 | 결과 |
|---|---|---|
| `chapter-focus.e2e.js` | 챕터 집중(전체 렌더·표본·편집 전 화면, 쪽 수 불일치 포함) | 16 |
| `chapter-focus-workfile.e2e.js` | 작업 파일 재열기 시 범위 복원·임포징 결과 유지 | 16 |
| `imposition-perchapter.e2e.js` | 파일별 대수 분리·구간 기록·전체 보존·임포징 체크 유지 | 20 |
| `rgb-gray-warn.e2e.js` | RGB회색 감지·경고·정규화 위험 판정 | 10 |
| `inknorm-default.e2e.js` | 정규화 항상 기본 켬(해제·복원·새 문서) | 10 |
| `save-dir-print.test.js` | 임시 폴더 제외·인쇄 원본 폴더·바탕화면 폴백 | 20 |
| `close-save.e2e.js`(보강) | 저장 후 닫기 질문 없음·다시 편집하면 다시 물음 | 13 |

회귀: chapter-edit 14 / imp-profile 12 / imposition-marks 16 / imposition-standalone 68 /
sidebar-buttons 31 / workcache 15 / hf-scope 14 / workfile 17 / docstate 18 · `npm run smoke` 통과.
