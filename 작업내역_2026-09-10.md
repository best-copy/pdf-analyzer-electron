# 2026-09-10 작업내역

> **"블리드에 얇은 선이 나온다"를 네 번 틀리고 다섯 번째에 잡았다.**
>
> 범인은 우리 코드가 아니라 **원고 맨 가장자리의 한 줄**이었다. 원본에서는 페이지 끝이라
> 안 보이던 줄이, 블리드를 만들면 재단선 안쪽으로 옮겨 앉아 인쇄된다.
>
> 못 찾은 이유도 분명했다 — **내 스캔이 맨 바깥 행/열을 계산에서 빼고 있었다.**
> 정확히 그 자리에 있는 것을 보지 않는 검사로 네 번을 "없음"이라 보고했다.

---

# 1. 블리드 — 얇은 선 (해결)

## 1-1. 틀린 가설 넷

| 가설 | 검증 | 결과 |
|---|---|---|
| 이음매에 틈이 벌어진다 | 미러를 0.26mm 겹쳐 그림 | 선 그대로 |
| 원고 테두리가 반사돼 두 줄 | '가장자리 늘리기' 방식 새로 만듦 | **미러와 픽셀까지 동일** |
| 비정수 오프셋(3mm=35.43px) 위상 차이 | 96·150·300·600dpi 렌더 | 화면만의 문제 아님(인쇄에도 나옴) |
| 미러가 페이지 끝까지 안 닿는다 | 바깥 끝 틈 검사 | 걸린 335곳은 **미러된 그림 내용**이었다 |

## 1-2. 실제 원인

사용자가 준 `test.pdf`(선이 나온 블리드본)에서 선을 특정했다:

```
아래에서 1.02mm 안쪽 · 폭의 99%를 가로지르는 1px 어두운 선 (238 → 179)
```

그리고 **원고의 맨 바깥 줄**을 보니:

```
원고 아래쪽 (바깥→안쪽): 179 → 238 → 238 → 238 → 238
                          ↑ 선의 값과 정확히 같다
```

디자인 툴이 남긴 가장자리 자국이다. 원본에서는 페이지 경계라 보이지 않지만,
블리드를 만들면 원고가 1mm 안쪽으로 들어가면서 그 줄이 **재단선 안에** 앉는다.

## 1-3. 고친 방법

- **반사축을 가장자리가 아니라 `ov`(0.2~0.5mm) 안쪽으로** 잡는다 → 미러가 그 줄을 반사하지 않는다
- **미러 띠를 원본 위에 덮는다**(순서를 바꿨다) → 원고의 그 줄을 띠가 가린다
- 덮이는 자리는 재단선 바로 옆(잘려 나가는 부분)이라 인쇄물에는 영향이 없다

검증 — 같은 원고·같은 측정:

| 블리드 폭 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 1mm | y=1758에 어두운 선(평균 59) | **없음** |
| 3mm | — | **없음** |
| 5mm | — | **없음** |

---

# 2. 블리드 — 소수점 입력 (해결)

**사용자가 원인을 짚어 주었다**: "블리드 체크가 켜져 있으면 입력이 안 되고, 해제하면 된다."

`bleedSettingsChanged()`가 **키 한 번마다** 블리드를 새로 만들고 화면을 다시 그리고 있었다.
그 사이 입력이 끊겨 `3.2`의 둘째 글자부터 먹히지 않았다. 체크를 풀면 이 함수가 첫 줄에서
빠져나가므로 잘 쳐졌다.

- 손을 멈춘 뒤(0.7초) **한 번만** 반영하고, 그때 칸을 만지고 있었으면 **커서 자리를 되돌린다**
- 임포징 옵션도 같은 구조라 함께 적용
- `step="0.5"` → **`0.1`** (네 곳: 임포징 블리드·원고 블리드·블리드 생성·표지 블리드)
  — 전에는 0.5 배수가 아닌 값이 브라우저 기준으로 '유효하지 않은 값'이었다

---

# 3. E-book — 하드커버 · 리플렛 (신규)

## 3-1. 하드커버(양장)

두꺼운 표지판(본문보다 물림) · 둥근 책등 · 깊은 그림자 + **면지 앞뒤 한 장씩 자동 삽입**.
표지·뒤표지는 제자리에 두고 그 안쪽에만 넣는다.

## 3-2. 리플렛 — 실제로 접힌다

처음 구현은 칸마다 따로 회전할 뿐이라 **진짜 접기가 아니었다**.
칸을 **경첩으로 이어 붙인 사슬**로 쌓도록 다시 만들었다 — 기준 칸은 제자리, 바깥 칸이
이전 칸의 **자식**으로 들어가 맞닿은 모서리를 축으로 돈다. 부모가 접히면 자식도 딸려 간다.

| | 펼침 | 50% | 접힘 |
|---|---|---|---|
| 3단 두루마리 | 801px | 284 | **270** (≈ 첫 칸 폭) |
| 대문접지 | 801px | 403 | **403** (≈ 가운데 두 칸) |

설정 3가지: **칸 너비(mm 직접 입력)** · **접는 방향(왼쪽/오른쪽/양쪽)** · **접는 방식**
(3단 두루마리 / 3단 지그재그 / 대문접지). 문서를 열면 시트 폭을 읽어 칸 너비를 자동으로
채우고, 합이 시트 폭과 달라도 비율로 맞춘다. 뷰어에서도 슬라이더·방향 버튼으로 바로 바꾼다.

실무 치수: 두루마리는 **말려 들어가는 칸을 좁게**, 대문접지는 **날개를 가운데보다 좁게**
— 접었을 때 종이가 겹쳐 부풀지 않게.

---

# 4. 2GB 넘는 PDF (해결)

## 4-1. 벽이 두 개였다

**첫 번째 — 파일 읽기.** `fs.readFileSync`가 2GiB에서 무조건 거부한다:

```
File size (2214592512) is greater than 2 GiB
```

→ 직접 열어 1GB씩 나눠 읽도록 바꿨다. 저장(`writeFileSync`)도 같은 한계가 있어 함께 뚫었다.
덤으로 기존 코드의 **복사 한 벌**(2GB 파일이면 4GB 사용)도 없앴다 — ArrayBuffer를 먼저 잡고
그 위로 바로 읽는다. 2.06GB 실측: 읽기 0.4초 · 저장 1.0초 · 1GB·2GB 이음매와 맨 끝까지 온전.

**두 번째 — 화면 메모리.** 이게 진짜 벽이었다. 이분법으로 쟀다:

```
1.95GB 할당 · OK
2.00GB 할당 · 실패 (RangeError: Array buffer allocation failed)
→ 최대 2,044MB (1.996GB)
```

**V8 메모리 플래그를 올려도 똑같이 2,044MB.** Chromium 자체의 상한이라 설정으로 못 올린다.

## 4-2. 해결 — gs로 용량을 줄여 한 문서로 연다

(처음에는 페이지 범위로 나눠 여러 탭으로 여는 방식을 만들었으나, 사용자 요청으로
**줄여서 한 문서로 여는 방식**을 기본으로 바꿨다. 나누기는 못 줄일 때의 최후 수단으로 남겼다.)

1.9GB를 넘으면 gs가 **이미지 해상도만** 낮춘다. 글자·벡터는 손대지 않는다.
품질 높은 단계부터 시도하고 **한계 아래로 내려가는 첫 단계에서 멈춘다**:

| 단계 | |
|---|---|
| 1 | 이미지 400dpi (고품질) |
| 2 | 이미지 300dpi (인쇄 표준) |
| 3 | 이미지 220dpi |
| 4 | 이미지 150dpi (최후) |

매 단계는 **원본에서 다시 굽는다** — 앞 결과를 또 줄이면 열화가 겹친다.
흑백 선화는 600dpi 이상 남기고, `-dAutoRotatePages=/None`은 필수(없으면 페이지가 돌아간다).

**검사가 잡아낸 결함**: gs가 파일을 **오히려 키우는** 경우가 있다(31KB → 751KB).
이미 잘 압축된 무손실 이미지를 JPEG로 다시 구우면 커진다.
→ 원본보다 커진 결과는 버리고 다음 단계로 넘어가며, 어느 단계도 못 줄이면 나누기로 간다.

실측(5.5MB 이미지 8쪽): **5.5MB → 0.8MB**, 400dpi 한 단계에서 중단,
쪽수·페이지 크기·글자와 순서 모두 보존.

---

# 5. 오늘 배운 함정

## 5-1. 가장자리를 보는 검사가 가장자리를 빼고 있었다

`for (let y = 1; y < H-1; y++)` — 이웃을 보려고 양 끝을 제외했더니, 정작 **문제가 있는
맨 바깥 줄**을 한 번도 보지 않았다. 네 번의 "없음" 보고가 여기서 나왔다.
**가장자리 문제를 볼 때는 가장자리를 표본에 반드시 포함할 것.**

## 5-2. PPM 헤더의 `#` 주석 (CLAUDE.md에 있는데 또 밟음)

gs는 `P6\n# Image generated…` 로 주석 줄을 넣는다. 건너뛰지 않으면 폭·높이가 NaN이 되어
**전 구간이 조용히 오판정**된다(`렌더 NaNxNaN`).

## 5-3. Bash 힙독이 이스케이프를 먹는다 (세 번째 재발)

`\\n` → 진짜 줄바꿈, `\\(` → `(`, `D:\\Ghostscript` → `D:Ghostscript`.
하네스가 조용히 멈추거나 정규식이 깨진다.
**이스케이프가 든 코드는 Write/Edit 도구로 쓴다.**

## 5-4. 시험 데이터가 현실과 다르면 결론도 다르다

용량 줄이기 시험에서 '잡음 이미지'를 `(i*7)%256` 같은 **규칙적인 패턴**으로 만들었더니
PNG가 31KB로 줄어, gs가 오히려 키우는 결과가 나왔다. `Math.random()`으로 바꾸니 5.5MB가
되어 실제 원고와 같은 결과(→0.8MB)가 나왔다. 다만 이 '틀린 시험' 덕에 **gs가 키우는 경우**를
발견해 방어 코드를 넣었다 — 헛돌지는 않았다.

---

# 6. 검사 현황

| 하네스 | 항목 | 결과 |
|---|---|---|
| `ebook-forms.test.js` | 면지·접지 계산(사용자 값·방향) | 35개 통과 |
| `ebook-fold.js` | 리플렛이 실제로 접히는가 | 33개 통과 |
| `ebook-view.js` | 하드커버·리플렛 화면 렌더 | 25개 통과 |
| `bigfile.test.js` | 2GB+ 읽기·쓰기 | 9개 통과 |
| `shrink.test.js` | 용량 줄이기(내용 보존) | 11개 통과 |
| `split.test.js` | 나누기(쪽 순서·누락) | 10개 통과 |
| `findline.js`·`bleedscan.js` | 블리드 선 위치 특정 | 선 소멸 확인 |

`npm run smoke` 통과 · 포터블 앱 동기화(체크섬 일치) 완료.

부수적으로, **검사용 창을 모두 왼쪽 모니터로** 옮겼다(주 모니터는 사용자 작업 공간).
메모리에 기록해 다음 세션에도 지켜진다.
