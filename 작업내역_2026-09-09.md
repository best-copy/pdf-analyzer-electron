# 2026-09-09 작업내역

> **페이지 이동 위젯을 만들고 여덟 번 고쳤다. 그 과정에서 내 실수 하나가 오래 숨어 있었다.**
>
> 사용자가 "대각선 빛이 아니라 왼쪽만 빛난다"고 한 것은 눈의 문제가 아니라
> **내가 CSS 주석을 깨뜨려 파서가 `@keyframes`를 통째로 삼킨 것**이었다.
> 애니메이션이 없으니 왼쪽에 놓인 그라데이션만 정지 상태로 남아 있었다.
> 그 상태로 "빛이 지나간다"고 보고했다 — 검증이 규칙(선언)만 보고
> **실제 재생 여부를 보지 않았기 때문**이다. 이후 픽셀 비교로 판정 방법을 바꿨다.

---

# 1. 페이지 이동 위젯 (신규)

## 1-1. 최종 모양

화면 오른쪽 **위**에 떠 있는 검정 알약. 화면비율(줌) 위젯과 같은 폭(114px = 36+1+40+1+36).

```
                                          [ 3 ▲▼]  / 12
```

| 요소 | 동작 |
|---|---|
| 숫자칸 | 지금 보고 있는 쪽 (스크롤 따라 자동 갱신) |
| ▲ | 이전 쪽 |
| ▼ | 다음 쪽 |
| 휠 | 위=이전 / 아래=다음 |
| `/ 12` | 전체 쪽수 |

- 화살표는 **늘 보인다**(호버 조건 없음).
- 위치는 툴바 첫 버튼 줄(🧾 프린터 판정)과 같은 높이 — **스크롤과 무관하게 고정**.
  스크롤을 따라 움직이게 했더니 연타 중 버튼이 커서 밑에서 달아났다.

## 1-2. 위치 결정 — 왜 다운로드 줄이 아닌가

사용자 요청은 "다운로드 버튼 줄에 맞춰라"였다. 맞춰 보니 **다운로드 버튼을 덮었다**.
툴바 두 줄의 실제 빈 공간을 재서 판단했다:

| 줄 | 오른쪽 빈 공간 | 판정 |
|---|---|---|
| 처리 옵션(적용·다운로드) | 파일 교체↔적용 사이 **115px** | 위젯 114px — 1px 남음, 창 폭 바뀌면 바로 가림 |
| 선택 컨트롤(프린터 판정) | 오른쪽 끝 **268px** | 여유 있음 → **채택** |

`top`은 '툴바가 상단에 붙었을 때의 자리'를 패널 기준 상대좌표로 계산해 고정한다.
스크롤 세 지점에서 `top 47 / left 1428` 동일, 툴바 버튼과 겹침 0건으로 확인.

## 1-3. 연타가 제자리걸음 하던 문제

칸의 숫자는 스크롤을 따라가므로, 스크롤 도중 중간 쪽으로 바뀐다.
그 값에서 이어 세면 ▲를 연타해도 앞으로 못 간다.
→ **향하던 목표(`_jumpTarget`)에서 이어 센다.** 단, 사용자가 칸에 직접 숫자를 치면
그 값이 우선(`_jumpShown`으로 앱이 쓴 값과 사람이 친 값을 구분).

## 1-4. 화면이 튀던 문제 (원인 둘)

1. **스크롤 중 썸네일 재생성** — `scheduleThumbUpgrade`가 스크롤마다 고해상도 썸네일을
   다시 만들어, 프레임이 끊기고 카드 크기가 바뀌며 **커서 밑 요소가 달라졌다**.
   → 이동 스크롤이 도는 0.9초 동안은 미룬다(검증: 스크롤 중 호출 **0회**).
2. **`scrollIntoView({block:'center'})`** — 이미 보이는 쪽으로 넘겨도 화면을 다시
   가운데로 끌어왔다. → **`block:'nearest'`**. 보이는 쪽이면 화면은 가만히 있는다.

---

# 2. 이동한 쪽 강조 — 네 번 갈아엎었다

| 시도 | 문제 |
|---|---|
| 주황 링(#ff9f0a) | 컬러 페이지 표시(#e6a700)와 색이 겹쳐 구분 안 됨 |
| 검정 링 + 확대 + 그림자 | 카드 테두리와 **이중 테두리**로 보임 · 그림자 키프레임이 매 프레임 재도색 → 끊김 |
| 대각선 빛 | **CSS 주석이 깨져 `@keyframes`가 삼켜짐** → 왼쪽만 정지한 채 빛남 (아래 5장) |
| **네 모서리 ㄱ자** (최종) | — |

## 최종 사양

- 네 귀에 `30×7` / `7×30` ㄱ자 8조각. 카드 테두리 색을 진하게:
  컬러 `#b8860b` · 흑백 `#1d1d1f` · 선택 `#ffd60a`.
- **깜박이지 않는다**(애니메이션 없음). 카드를 확대하지도, 그림자를 씌우지도 않는다.
- 사라지는 때는 **딱 둘** — 다른 쪽으로 이동, 또는 사용자가 스크롤.
  시간으로는 사라지지 않는다(2.5초 뒤에도 남아 있음을 확인).
- 이동이 일으킨 스크롤(0.9초)은 무시한다. 안 그러면 방금 켠 표시를 스스로 지운다.

---

# 3. 임포징 결과에 컬러/흑백 표시가 없던 문제

**증상**: 임포징 후 화면에서 컬러/흑백 구분이 "풀린다"(분석 숫자는 맞음).

**원인**: 메인 결과 격자(`.pv-cell`)에 컬러/흑백 표시를 **아예 붙이지 않고 있었다.**
사이드바 썸네일(`sb-color-page`)에만 있었다. 통계는 따로 세므로 숫자만 맞았던 것.

**수정**: 시트마다 실제 렌더 결과로 판정해 `pv-color`/`pv-mono` + '컬러/흑백' 문구를 붙인다.
지연 렌더로 나중에 채워지는 시트도 그려지는 순간 함께 표시된다.
(문구 줄 16px만큼 `pv-geo`·가이드 오버레이의 `bottom`을 22→38px로 함께 밀었다)

검증: 컬러 4쪽+흑백 4쪽 → 중철 4시트 **전부 표시**(미표시 0), 사이드바·분석 숫자와 일치.

---

# 4. 챕터를 지워도 편집 모드에서 되살아나던 문제

**원인**: 편집 모드 기본 미리보기가 **원본 바이트를 그대로** 띄우고 있었다.

```js
// 고치기 전 — showWorkspaceBasePreview()
if (originalPdfBytes) renderProcessedPreview(originalPdfBytes, { live: true, source: 'original' });
```

원본에는 지운 페이지가 그대로 있다. 메인 화면은 `pageResults`로 그리므로 멀쩡했고,
**편집 모드에 들어갈 때만** 되살아나 보였다. 챕터 삭제뿐 아니라 페이지 삭제·순서·회전·
빈 페이지 삽입도 같은 경로였다.

**수정**: `pageEdited`면 원본 대신 `buildBaseProcessed()`(적용·다운로드와 같은 조립본)를
띄운다. 손댄 적 없으면 기존 빠른 경로 유지. 조립본은 `pageResults`와 1:1이라
`source:'original'` 표식을 붙이지 않는다(챕터 집중이 원본 인덱스로 오판하지 않도록).

**원인 확정 방법**: 고친 부분만 잠시 꺼서 재실행 → 편집 모드가 다시 **7쪽**(증상 재현),
되돌리면 4쪽. 추측이 아니라 스위치로 확인했다.

---

# 5. 오늘 배운 함정

## 5-1. CSS 주석이 깨지면 다음 `@keyframes`가 통째로 사라진다

앞선 수정에서 `*/` 뒤에 설명문이 이어지고 다시 `*/`가 오는 형태를 남겼다.
파서는 그 잘못된 부분부터 다음 블록까지 버렸고, **애니메이션 선언만 사라졌다.**
CSS는 조용히 실패한다 — 오류도, 콘솔 경고도 없다.

증상은 "왼쪽만 빛남"이었다. 규칙(`animationName`)만 확인하는 검사는 이걸 못 잡는다.
→ **판정을 픽셀로 바꿨다**: 화면을 세 시점에 찍어 카드 영역이 실제로 달라지는지 본다.
지금은 주석 자리에 그 사고를 적어 두었다.

## 5-2. 오프스크린 하네스는 애니메이션이 안 돈다

`getComputedStyle(...).animationName`이 `none`, `el.getAnimations()`가 빈 배열.
'움직임 줄이기'가 켜진 것처럼 동작한다.
→ CDP `Emulation.setEmulatedMedia`로 `prefers-reduced-motion: no-preference` 강제.
→ `:hover`도 합성 마우스로는 안 걸린다. CDP `CSS.forcePseudoState`로 진짜로 건다.
→ 그래도 안 잡히는 건 **화면 캡처 픽셀 비교**로 판정.

## 5-3. GPU 프로세스가 죽으면 분석이 영원히 안 끝난다

하네스가 세 번 연속 멈춰 원인을 추적했더니, 썸네일 `toBlob`이 돌아오지 않았다.
로그에 `GPU state invalid after WaitForGetOffsetInRange`.
좀비 Electron이 남아 프로필 잠금까지 겹쳤다.
→ 하네스는 `app.disableHardwareAcceleration()`으로 돌린다.
→ 측정 전 좀비 정리. **단, 사용자의 실행 중인 앱(userData=pdf-analyzer)은 건드리지 않는다** —
   커맨드라인으로 내 하네스(`jumpbar.js`)만 골라 종료했다.

## 5-4. Bash 힙독이 `\n`을 진짜 줄바꿈으로 바꾼다 (재발)

패치 스크립트를 힙독으로 넣으면 JS 문자열 안의 `\n`이 실제 개행이 되어 구문이 깨진다.
정규식의 `\(`도 마찬가지로 뭉개져 `Unterminated group`이 났다.
→ **이스케이프가 든 코드는 Write/Edit 도구로 쓴다.** (CLAUDE.md에 이미 있는데 또 밟았다)

---

# 6. 거래관리 시스템(business-mgmt) 견적 연계 (신규)

인쇄비 산출 결과를 거래관리 앱으로 넘겨, 고객을 고르거나 새로 등록해 견적서를 쓰게 했다.

## 6-1. 연결 방식 — 파일 한 개

두 앱이 약속한 폴더 `%USERPROFILE%\.ilcheong\quote-inbox` 에 JSON 하나를 떨어뜨린다.
프로세스 간 결합을 파일로만 둔 이유: **거래관리 앱이 꺼져 있어도 다음 실행 때 받는다.**
쓸 때는 `.part`로 쓰고 rename — 절반만 쓰인 파일을 상대가 읽는 사고 방지.

- 보내는 쪽(`main.js` `quote:sendToBusiness`): 파일 기록 + 거래관리 앱 실행(꺼져 있으면)
- 받는 쪽(business-mgmt `main.js`): 2초 폴링 → renderer로 전달 → **파일 삭제**(두 번 안 열리게)
  - `fs.watch`를 쓰지 않은 이유: 네트워크·가상 폴더에서 이벤트를 놓친다

## 6-2. 부수·할인을 금액이 같아지게 옮기기

이 앱에만 있는 **부수·할인율**은 거래관리에 없는 개념이다.
→ 부수는 수량에 곱하고, 할인은 단가에 반영하며, 규격란에 `10% 할인 반영 · 3부`로 근거를 남긴다.
검증: 화면 합계 9,720원 = 넘긴 금액 9,720원.

## 6-3. 저장은 하지 않는다

거래관리는 견적서 작성 창을 **품목까지 채워서 열기만** 한다.
고객 선택(자동완성)·`+ 고객등록`은 그 창에 이미 있던 기능을 그대로 쓴다.
사용자가 [저장]을 눌러야 견적번호가 발급된다 — **번호를 헛되이 소모하지 않기 위함**.

## 6-4. 검증

| 대상 | 방법 | 결과 |
|---|---|---|
| 넘길 내용 | 오프스크린 앱에서 payload 생성 | 13개 통과 (금액 일치·빈 품목 제외) |
| 받은편지함 | **business-mgmt/main.js의 진짜 함수를 추출**해 임시 폴더로 구동 | 10개 통과 |
| 창 채우기 | 거래관리 화면을 임시 프로필로 띄워 `importPdfQuote` 호출 | 16개 통과 |

받은편지함 검사에 포함: 파일 삭제(두 번 안 열림)·깨진 파일은 `.bad`로 격리·다른 앱 파일
무시·여러 건 순서대로·폴더가 없어도 안 터짐.

**거래관리 쪽 코드는 다른 세션에서 이미 커밋·재빌드됐다**(`f19e227 v2.58.0`, 17:16).
빌드 산출물 `app.asar` 안에 `PDF_QUOTE_INBOX`·`importPdfQuote`·배너 문구가 모두 들어 있고,
중간에 고친 문자열 연결본이 들어갔음을(깨졌던 템플릿 버전이 아님을) 확인했다.

---

# 7. 검사 현황

| 하네스 | 항목 | 결과 |
|---|---|---|
| `jumpbar.js` | 페이지 이동 위젯 전반 | 47개 통과 |
| `imp-color.js` | 임포징 컬러/흑백 표시 | 8개 통과 |
| `ch-delete.js` | 챕터 삭제 후 편집 모드 | 9개 통과 |
| `biz-send.js` | 거래관리로 넘길 내용 | 13개 통과 |
| `biz-inbox.js` | 받은편지함 처리(추출 검증) | 10개 통과 |
| `biz-modal.js` | 거래관리 견적서 창 채우기 | 16개 통과 |

`npm run smoke` 통과 · 포터블 앱 동기화(체크섬 일치) 완료.
