# 2026-09-08 작업내역

> **"구동 속도의 한계가 자바스크립트 때문인가"를 측정으로 답했다.**
>
> 결론: **아니다.** 같은 렌더를 네이티브 Ghostscript가 우리보다 47% 느리게 한다.
> 병목은 언어가 아니라 ① 문서 유형별 동시성 ② 한글 COM 내부 시간이었다.
>
> 이번 작업의 원칙은 지난번과 같다 — **눈이 아니라 수치로 확정한다.** 다만 이번엔
> 측정 자체가 배신하는 경우를 두 번 겪었고(아래 5장), 그래서 판정 기준을 바꿨다.

---

# 1. 질문 — JS가 한계인가? (C# .NET 별도 프로세스로 20배?)

사용자가 "React(JS) 대신 C# .NET 8 엔진으로 분리해 20배"라는 사례를 제시. 실측으로 갈랐다.

## 1-1. JS 처리량 (Chrome 126 / 16코어)

| 연산 | 처리량 | 판정 |
|---|---|---|
| 픽셀 컬러 판정 루프 | **3,048 MB/s** | 메모리 대역폭 수준 — 더 빨라질 여지 없음 |
| 64MB 바이트 복사 | 2,844 MB/s | 동일 |
| pdf-lib `save()` (660쪽 180MB) | **1,344 MB/s** (0.13초) | 병목 아님 |
| PDF 객체 50만개 문자열화+인코딩 | 266 MB/s | 문제 없음 |
| 객체 200만개 생성+순회 | 149ms | 문제 없음 |
| **zlib 압축 (pako L6)** | **25.6 MB/s** | 네이티브 57.6 MB/s = **2.25배 손해** |

JS가 확실히 손해 보는 건 **압축 하나뿐**이다.

## 1-2. 결정적 반증 — 네이티브가 더 느리다

576쪽을 같은 크기(≈550px)로 썸네일 렌더:

| 엔진 | 시간 |
|---|---|
| **우리 (pdf.js·JS)** — 컬러 판정·JPEG 인코딩까지 함께 함 | **7.9초** |
| Ghostscript (네이티브 C, 이미 번들된 엔진) | 11.6초 |

(pdf.js는 멀티 문서로 코어를 쓰고 gs는 단일 스레드라는 차이는 있다. 사용자 체감은 벽시계 시간이다.)

→ **C#/네이티브로 옮겨도 20배는 나오지 않는다.** 그런 사례의 20배는 "UI 스레드에서 PDF를
주무르던 구조"를 고친 몫이 대부분이고, 우리는 이미 워커·gs 별도 프로세스를 쓰고 있다.

---

# 2. 병목은 문서 유형에 따라 정반대다

CDP `Profiler`(표본 200µs)로 self time 집계. **앱 함수는 `window`에 없어** 몽키패치 계측이
0개가 나온다 — 프로파일러가 유일한 방법이었다.

**벡터 원고** (2up 교재 576쪽 141MB, 7.9초) — **메인 스레드 바운드**

| CPU 자리 | 비중 |
|---|---|
| pdf.js 캔버스 경로 연산 (`bezierBoundingBox` 12.6% + `constructPath` 11.9% + …, 전부 JS) | **36%** |
| idle | 16.5% |
| `analyzePageColor` (우리 코드) | 10.5% |
| 워커↔메인 메시지 역직렬화 | 10.4% |
| GC | 9.1% |

**스캔 원고** (300dpi JPEG 192쪽 20.9MB, 5.6초) — **동시성 바운드**

| CPU 자리 | 비중 |
|---|---|
| **idle — 메인이 워커 디코드를 기다리며 놀고 있음** | **63.8%** |
| `analyzePageColor` | 15.7% |

---

# 3. A-1 — 분석 동시성을 '메모리 예산제'로

## 3-1. 무엇이 틀렸나

기존 규칙: *"96MB 넘으면 보조 문서 0개, 아니면 3개 고정"*. **두 경계 모두 실측과 어긋났다.**

| 조건 | 실측 |
|---|---|
| 스캔 20.9MB·192쪽 | 4문서 5.15초 → **8문서 3.55초** (1.45배) |
| 벡터 141.8MB·576쪽 | 96MB 상한에 걸려 보조 문서 0개 — 1문서 8.12초 → 3문서 6.41초 |
| 보조 문서 여는 비용 | 8개에 **0.14초** (pdf.js는 지연 파싱) — 무시 가능 |

⚠ 지난 실측 메모(`runtime-perf-2026-09`)의 "동시성을 늘리면 손해, 다시 시도하지 말 것"은
**87MB 대형 파일에만 맞는 결론**이었다. 메모리에 부분 정정을 달아 두었다.

## 3-2. 바꾼 것 ([src/app-core.js](src/app-core.js) `analyzePDF`)

- 보조 문서 수 = **예산 512MB ÷ 파일 크기**, 상한은 코어 수와 **실제 물리 RAM**
- 96MB 상한 제거 · 문서 열기는 병렬 (`Promise.allSettled`)
- ⚠ **레인 수를 문서 수에만 맞추면 안 된다.** 처음에 3문서→6레인으로 줄였다가 대형 벡터
  원고가 예전(8레인)보다 느려졌다 — 그쪽은 메인 스레드가 병목이라 레인이 줄면 그대로 손해다.
  → `max(문서×2, 8)`, 상한 코어·16.
- RAM은 preload에 새로 만든 `totalMemoryGB()`로 읽는다.
  ⚠ `navigator.deviceMemory`는 **8GB에서 잘려** 62GB PC와 8GB PC를 구분하지 못한다.

| 문서 | 이 PC(62GB) | 8GB PC | 4GB PC |
|---|---|---|---|
| 스캔 20.9MB·192쪽 | 8문서/16레인 | 4문서/8레인 | 2문서/8레인 |
| 대형 벡터 141.8MB | 3문서/8레인 | 3문서/8레인 | 2문서/8레인 |
| 초대형 300MB·2000쪽 | 1문서/8레인 | 1문서 | 1문서 |

## 3-3. 결과 (각 3회 중앙값)

| 문서 | 전 | 후 | 개선 |
|---|---|---|---|
| 스캔형 192쪽 20.9MB | 5,306ms | **3,718ms** | **1.43배** |
| 국어 워크시트 50쪽 38MB | 1,610ms | **1,475ms** | 1.09배 |
| 대형 벡터 576쪽 141.8MB | 7,847ms | 7,646ms | 변화 없음(오차 범위) |

**메모리 대가**: 스캔 원고 최대 1,210MB → 1,872MB(+58%). 그래서 RAM 상한을 걸었고
**8GB PC는 예전과 동일한 4문서**라 회귀가 없다. 4GB PC는 2문서로 오히려 보수적으로 바꿨다.

---

# 4. 첨부 원고(HWPX) 진단 — 84%는 한글 내부다

`(수정)국어 워크시트-1.hwpx` (22.8MB). "분석이 오래 걸린다"는 신고였는데, 분석은 1초였다.

| 단계 | 시간 | 비중 |
|---|---|---|
| **HWPX → PDF 변환 (한글 COM)** | **14.6초** | **82%** |
| 분석 (열기→썸네일 51쪽) | 1.07초 | 6% |
| 흑백·잉크 프리웜 (17쪽) | 1.54초 | 9% |
| 최적화본 + 폰트 임베드 프리웜 | 0.61초 | 3% |

## 4-1. 변환 14.6초의 구간 분해 (계측 사본, 3회 중앙값)

| 구간 | ms | 비중 | 우리가 손댈 수 있나 |
|---|---|---|---|
| PowerShell 기동 | 368 | 2.5% | ○ |
| 워처(보안창 감시) 기동 | **129** | 0.9% | ○ |
| 한글 COM 생성(한글 실행) | 931 | 6.4% | △ 재사용하면 |
| 문서 열기 Open | 760 | 5.2% | ✗ |
| **FileSaveAsPdf (한글 내부)** | **12,312** | **84.2%** | **✗** |
| 종료·COM 해제 | 52 | 0.4% | ○ |

- 워처의 `Add-Type` C# 컴파일이 2~4초일 거라 의심했으나 **129ms**였다. 코드만 읽은 추측은 틀린다.
- 통제 가능한 전부를 0으로 만들어도 15.5%. 인스턴스 재사용으로 현실적으로 아낄 수 있는 건
  파일당 약 1.3초(9%)뿐이라 **2단계는 하지 않기로 했다**(한글 COM 상태 관리 위험 대비 이득 부족).
- 좀비 한글 프로세스는 처음 2개 관측됐으나 이후 3회 연속 실행에서는 남지 않았다 —
  **재현 조건을 특정하지 못했다.** 결함으로 단정했던 앞선 보고를 정정한다.

## 4-2. 대신 한 것 — 변환 진행 표시 (사용자 선택)

한글·Office COM은 진행률 콜백이 없다. → **경과 시간은 사실대로, 남은 시간은 추정으로만.**

```
(수정)국어 워크시트-1.hwpx → PDF 변환 중 · 6초 경과 / 예상 15초    [▓▓▓▓░░░░] 40%
```

- 추정식 **`2.0 + 0.6 × 파일MB` 초** — 2점 실측(4.15MB→3.9초 / 21.8MB→14.6초)에서 뽑았다.
  실제 검증: 예상 15초 / 실제 14.6초.
- **Office·Adobe는 실측이 없어 추정을 지어내지 않고 경과 시간만** 표시한다.
- 여러 파일이면 `(2/5)` 표시 — 한글은 단일 인스턴스라 순차 변환된다.
- **막대는 95%에서 멈춘다.** 추정을 넘기면 `7초 경과 (예상 3초 — 조금 더 걸리는 중)`으로 문구가
  바뀐다. 끝나지 않았는데 100%가 되는 일은 없다.
- 파일 크기는 preload에 새로 만든 `fileSize()`로 읽는다(내용을 읽지 않아 대용량도 즉시).

검증: 실제 한글 변환에서 2초 13% → 6초 40% → 13초 86% 상승과 화면 캡처 확인,
초과·예상없음·타이머 정지 3가지 경계 각각 확인.

---

# 5. 측정 방법론 — 이번에 배운 것 (중요)

**Electron 벤치마크는 연속 실행하면 첫 회차가 크게 튄다.** 이번에 두 번 헛짚었다:

| 관측 | 재실행 결과 |
|---|---|
| 안내지 1쪽 분석 **47.4초** → "우리 코드가 100배 낭비" 결론 | **417ms** |
| A-1 검증 중 576쪽 **415초** → "심각한 회귀" 결론 | **8.15초** |
| 최종 확인 중 스캔 192쪽 **197초** | 3.7초 |

이전 프로세스의 종료·메모리 압박이 겹쳐 생기는 아티팩트다. 실사용에서도 일어나는지는 확인하지
못했다. → **판정은 반드시 `electron 정리 + 2.5초 휴지 + 3회 중앙값`으로.** 1회 측정으로 결론 금지.

그 밖에:
- **앱 함수는 `window`에 없다** — 최상위 `function` 선언인데도 전역 객체 속성으로 안 잡혀
  몽키패치 계측이 0개가 된다. **CDP Profiler**를 쓸 것(pdf.js 내부까지 함수 단위로 보인다).
- **PS1은 BOM 필수** — 계측 하네스를 BOM 없이 저장했다가 한글 경로가 깨져 즉시 실패했다.
  CLAUDE.md에 적힌 함정을 그대로 밟았다.
- 압축 벤치는 **실제 PDF에 가까운 엔트로피**로 — 반복 데이터로 재면 24MB→0.08MB가 되어
  압축기가 비현실적으로 빨라 보인다.

---

# 6. 하지 않은 것

- **A-2 (썸네일을 워커로 이전)** — 대형 벡터 원고와 UI 프리즈에 듣는 개선이지만, 사용자가
  "좀 더 써 보고 판단하겠다"고 하여 보류.
- **한글 인스턴스 재사용** — 이득 9%(단일 파일) 대비 상태 오염 위험이 커서 보류.
- **C#/네이티브 프로세스 분리** — 1장 근거로 권하지 않음.


---

# 7. 저장 파일명 규칙 정리

## 7-1. 조판 표기가 겹쳐 쌓이던 문제

저장한 파일을 다시 열어 저장하면 이름이 계속 길어졌다 —
`국어_2up.pdf` → `국어_2up_2up.pdf` → `국어_2up_2up_2up.pdf`.

→ 이름에 이미 조판 표기가 있으면 **표기를 또 붙이지 않고 끝 번호만 올린다.**

```
국어.pdf        → 국어_2up.pdf
국어_2up.pdf    → 국어_2up-1.pdf
국어_2up-1.pdf  → 국어_2up-2.pdf
```

- 감지 대상: `숫자+up`(2up·4up…) · `중철` · `모아찍기` · `정합` · `반복`
- **금액 표기도 같이 정리** — `국어_2up_12000원`을 다시 저장하면 예전엔
  `..._12000원_2up_12000원`이 됐다. 이제 떼고 현재 값으로 다시 붙인다.
- 이름 만드는 **6군데**(다운로드 + 중철·모아찍기·정합·반복 생성 + 중철 표지/내지)를
  공용 `nameWithImpTag(base, tag)` 하나로 통일 (app-core.js).

⚠ 오탐 방지 확인: `backup` · `모바일 가이드_10매` · `설계도P2up` · `견적서-2026`은 건드리지 않는다
(경계 문자 앞에 `숫자+up`이 와야 표기로 본다).

## 7-2. 임포징을 하지 않으면 `_1up`을 붙이지 않는다

조판을 하지도 않았는데 `_1up`이 붙는 게 실무에서 거슬린다는 지시.
`impNameTag()`가 임포징 꺼짐일 때 **빈 문자열**을 반환하고, `nameWithImpTag`는 빈 태그면
표기를 붙이지 않는다(이름에 이미 표기가 있을 때만 번호를 올려 원본 덮어쓰기를 막는다).

검증: `scripts/test/download-name.test.js` 신규 — app-core.js에서 함수를 추출해 실제 코드로
**22개 통과**. 실제 앱으로도 확인 — `국어_2up-1.pdf` → 세 모드 모두 `국어_2up-2.pdf`,
표기 없는 원고 + 임포징 꺼짐 → 원본 이름 그대로.

---

# 8. 🔢 페이지 번호 — 머리글·바닥글에서 분리한 새 기능

머리글에 얹혀 있던 번호 기능은 **그대로 두고**(기존 프로파일 호환), 번호만 따로 켜고
서식·위치·제외 페이지를 지정하는 독립 레이어를 만들었다.

## 8-1. 사전에 정한 것 (사용자 결정)

1. 기존 머리글의 번호 기능은 **그대로 유지**(a안) — 실무 파일이 프로파일에 묶여 있어서
2. **내각/외각 = 제본 기준** — 외각=홀수쪽 오른쪽·짝수쪽 왼쪽, 내각은 그 반대
3. UI는 별도 팝업이 아니라 **편집 사이드바 새 섹션**
4. 번호는 **원본 페이지에 찍힌 뒤** 임포징(2up·중철)으로 조판 (코드 순서로 확인)

## 8-2. 구성

| 파일 | 역할 |
|---|---|
| `src/hf-core.js` | 순수 함수 5개 추가 — `pnParseRanges`(1, 3-5 파서) · `pnInScope` · `pnNumberCtx` · `pnFormat` · `pnAnchor`. **워커와 렌더러가 함께 쓰는 단일 원본** |
| `src/app-core.js` | 설정 스키마 `pn: {...}` |
| `src/index.html` | `#secPn` 섹션 |
| `src/app-ui.js` | 입력 바인딩 · `syncPnUI` · `setPnPos` · `pnInsertToken` · 프로파일 저장/복원 · 글꼴 드롭다운 공용화 |
| `src/worker-assemble.js` | 실제 그리기 (hf 레이어와 나란히) |

기능: 기능 사용 토글 · 글꼴/크기/**색상**/굵게(B) · 번호 서식(`{page}`·`{total}` 삽입 버튼) ·
X·Y 이동(mm) · 시작 위치(P)·시작 번호 · 제외 페이지(`1, 3-5`) · 번호 배치 앵커 6종.

**굵게(B)**: 선택한 글꼴의 '굵은 짝'을 시스템 목록에서 이름으로 찾아 쓰고, 없으면
0.025×크기만큼 겹쳐 그려 흉내낸다(벡터 유지 — 이미지로 굽지 않는다).

## 8-3. 함정 — 좁은 사이드바에서 행이 접힌다

`.es-input`·`.es-select`는 CSS에서 **`flex: 1`**이라 한 줄에 여러 칸을 두면 줄어들지 못하고
다음 줄로 밀린다(라벨이 한 글자씩 세로로 접혀 보였다). 한 행에 여러 칸을 둘 때는
**`min-width:0`(또는 고정 폭)과 라벨의 `white-space:nowrap`을 함께** 줘야 한다.
캡처로 확인하고 섹션을 다시 짰다.

## 8-4. 검증

`scripts/test/page-number.e2e.js` 신규 — 실제 앱 파이프라인으로 찍고 **텍스트와 좌표**를
뽑아 확인, **24개 통과**.

| 확인 | 방법 |
|---|---|
| 서식 `{page} / {total}` · 시작 위치 3 · 시작 번호 | 3쪽=`1 / 8`, 10쪽=`8 / 8` (전체 쪽수도 본문 기준) |
| 제외 페이지 `1, 4-6` | 그 쪽만 비고 번호는 이어짐(7쪽=7) |
| 내각/외각 홀·짝 대칭 | x좌표로 좌우 판정 + 거울 대칭 검사 |
| 상단/하단 · X·Y 이동 | y좌표·x좌표 변화량 |
| **색상** | 렌더 후 하단 영역 최저 밝기 픽셀을 뽑아 RGB 대조(빨강·파랑·기본 진회색) |
| 머리글과 독립 | 머리글 꺼진 상태에서도 찍힘 |

---

# 9. 🔁 파일 교체 버튼

같은 판형·머리글·페이지번호·임포징으로 **여러 원고를 연달아 뽑기 위한** 버튼.
상단 처리 옵션 줄에 추가(`replaceFileKeepSettings`, app-core.js).

- **유지**: 크기·여백·제본여백·머리글 문구·페이지 번호·워터마크·임포징·흑백/잉크 옵션, 그리고 **같은 탭**
- **비움**: 흑백 선택 페이지 · 개별 보정 · 내부편집 · 머리글 '체크 선택' 목록
  → 원고가 다르면 3쪽이 같은 3쪽이 아니다. 무엇을 비웠는지 성공 메시지에 표시한다.
- 설정은 **깊은 복사**로 옮긴다 — 참조를 공유하면 옛 문서 상태가 새 문서로 샌다.

검증: `scripts/test/replace-file.e2e.js` 신규 — 6쪽 원고에 설정을 걸고 9쪽 원고로 교체,
**21개 통과**. 교체 직후 적용하면 새 원고에 `- 5 -`(시작 위치 2·시작 번호 5·3쪽 제외)가
그대로 찍히고 머리글도 함께 나오는 것까지 확인.

---

# 10. 이번 회차 검증 요약

- `npm run smoke` 통과
- `download-name.test.js` 22 · `page-number.e2e.js` 24 · `replace-file.e2e.js` 21 — **전부 통과**
- UI는 캡처로 두 번 확인(행 접힘 수정 전/후, 색상 추가 후)
- 포터블 앱 동기화 체크섬 일치
