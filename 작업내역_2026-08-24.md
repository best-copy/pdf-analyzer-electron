# 작업내역 2026-08-24 — 이미지 첨부·크게 보기·임포징 개선 8건

요청 8건을 모두 반영했다. 검증은 `npm run smoke` + 항목별 Electron E2E/노드 추출 테스트로 했다
(PDF 시각 검증은 Electron + vendored pdf.js 렌더로 했다 — 작업 당시 Ghostscript를 못 찾은 줄 알았으나,
사실 `D:\Ghostscript\gs10.07.1`에 이미 설치돼 PATH에 있었다. 검증 결론은 동일).

---

## 1. 이미지 파일 첨부 → PDF 변환·분석 + ⟳ 앱 새로고침 (Force Reload)

**이미지 첨부** — 외부 앱 없이 렌더러에서 1쪽 PDF로 만든다.

- `IMG_RE = /\.(png|jpe?g|gif|bmp|webp|tiff?|avif)$/i`, `CONVERT_RE`에도 포함
  → 파일 선택 다이얼로그·드래그&드롭·최근 파일 경로가 모두 함께 동작.
- `imageToPdfBytes()` (src/app-core.js)
  - PNG/JPEG는 **원본 스트림 그대로 임베드**(무손실·용량 유지),
    GIF·BMP·WEBP·AVIF는 `createImageBitmap` → canvas → PNG 재인코딩.
  - 페이지 크기 = **실제 인쇄 크기**. 파일에 기록된 해상도를 직접 파싱한다
    (PNG `pHYs` / JPEG JFIF density). 기록이 없으면 긴 변 ≥1000px = 300DPI(스캔·인쇄 원고),
    미만 = 96DPI(화면 캡처). 결과가 10~1500mm를 벗어나면 비율 유지로 보정.
- main.js `dialog:openFile` 필터에 이미지 확장자 추가.

검증(Electron E2E): 1200×900 PNG + pHYs 300DPI → 288×216pt ✔ / 해상도 정보 없는 1200px → 288×216pt ✔ /
400px → 300×225pt(96DPI) ✔ / GIF 재인코딩 무오류 ✔

**⟳ 앱 새로고침** — main `app:forceReload`(reloadIgnoringCache) + preload + 메인 처리옵션 줄·좌측 사이드바 버튼.
Electron 기본 메뉴의 **Ctrl+R / Ctrl+Shift+R을 `before-input-event`로 가로채** 렌더러의 확인 절차를 태운다
(안 그러면 편집 중이던 문서가 경고 없이 날아간다).

## 2. 🔍 페이지 크게 보기 (컬러/흑백 판별용)

썸네일 **더블클릭** · 우클릭 메뉴 · **V** 키로 전체화면 뷰어를 연다.

- `globalPdfDoc`에서 원본 페이지를 크게 렌더(회전 반영, 렌더 픽셀 상한 4000px).
- **🎨 컬러 픽셀 표시** — 중성(회색)이 아닌 픽셀만 자홍색으로 칠해, 옅은 도장·로고처럼
  썸네일로는 안 보이는 컬러를 눈으로 찾게 한다. **감도** 기본 1 = 분석기와 같은 기준
  (`|R-G|,|G-B|,|R-B| ≤ 1` 이면 회색).
- 컬러 픽셀 개수·비율 표시, 컬러/흑백 판정 배지, ◀ ▶(← →) 이동, **선택한 페이지만** 순회,
  ⬛ 흑백변환 선택(B), 확대/축소·맞춤, Esc 닫기.

검증: 회색 도형 + 작은 빨간 도장 원고 → "🎨 컬러 판정 / 컬러 픽셀 9,973개 (0.62%)",
도장만 자홍색으로 표시됨(스크린샷 확인).

## 3. ✒ 폰트 출력 안전화·평탄화 → 메인으로 이동 + '저장하고 닫기' 수정

- 편집 사이드바의 `#secOutline` 제거 → **메인 처리 옵션 줄**(`#opt-outline`)과
  **좌측 사이드바 `⚙ 처리옵션` 패널**(켜기 · 곡선화/폰트 완전 임베드 · 평탄화)로 이동.
  적용·다운로드의 최종 단계(Ghostscript)라 편집 옵션이 아니라 처리 옵션이 맞다.
- `saveAndCloseWorkspace()`는 **창을 먼저 닫고 적용을 진행**한다
  (`exitEditWorkspace(needApply)` → `await applyChanges()`).
  예전엔 applyChanges를 await한 뒤 닫아서, gs 변환이 도는 동안 "닫기가 안 먹는다"로 보였다.

검증: 작업공간 열림 → `saveAndCloseWorkspace()` 호출 직후(await 없이) `edit-fullscreen` 해제 확인 ✔,
이후 적용 결과 생성 확인 ✔

## 4. 편집 모드 이탈 시 흑백 적용이 풀려 보이던 오류

원인: 편집 모드의 라이브/표본 미리보기가 `invalidateProcessed()`로 `processedPdfBytes`를 날리고,
나올 때 `closePreview()`가 원본 통계를 복원해 **적용 전 상태로 되돌아간 것처럼** 보였다.

- `enterEditWorkspace`에서 `_wsSavedResult`로 결과를 스냅샷 → `exitEditWorkspace(false)`에서 복원.
- 진입 전에 결과를 보고 있었으면 표본 그리드를 버리고 `renderProcessedPreview`로 즉시 다시 그린다
  (오프스크린 렌더 후 한 번에 교체 → 깜빡임 없음).

검증: 흑백 적용(2쪽 확정) → 편집 모드 진입(결과 무효화 확인) → 이탈 후
결과 바이트·미리보기·컬러 수(1)·확정 흑백 수(2) 모두 유지 ✔

## 5. 📖 임포징 PDF 생성 버튼 1회용

생성이 끝나면 버튼을 잠그고 라벨을 `✅ 임포징 생성 완료 (설정을 바꾸면 다시 활성화)`로 바꾼다.
`adoptImposedResult`에서 `setImpGenDone(true)`, 해제는 `impSettingsChanged()`와
`invalidateProcessed()`(페이지·선택·편집 변경 전부가 여기를 지난다)에서.

검증: 생성 전 활성 → 생성 후 비활성 → 설정 변경 후 다시 활성 ✔

## 6. 편집 작업공간 임포징 딜레이·화면 멈춤

1. **UI 양보** — pdf-lib의 `embedPage`/`drawPage`는 await해도 마이크로태스크로만 풀려
   이벤트 루프로 돌아가지 않는다. 수백 쪽 루프에서 렌더러가 통째로 얼어붙던 실제 원인.
   `uiYield()`(마지막 양보 후 40ms 넘었을 때만 `setTimeout(0)`)를 `embedAllPages`·
   `buildBleedBytes`·임포징 4종 시트 루프에 삽입. 품질·출력은 그대로.
2. **임포징 결과 캐시** — `_impBytesCache`, 키 = `bytesFingerprint(src) + JSON.stringify(currentImpOptions())`.
   ⚠ `impSignature()`는 '임포징 포함'이 꺼져 있으면 빈 문자열을 돌려주므로 캐시 키로 쓰면 안 된다
   (옵션을 바꿔도 캐시가 적중하는 낡은 결과 버그 — 구현 중 실제로 밟았다).
   `clearProcessCaches()`에서 함께 비운다.
3. 임포징 포함 상태의 라이브 미리보기 디바운스 120ms → 420ms.

검증(40쪽 문서): 최초 조립 66ms → 캐시 적중 0ms → 옵션 변경 후 64ms(정상 미스) ✔,
조립 중 `setInterval` tick 5~6회 = 이벤트 루프 살아 있음 ✔

## 7. 🕓 최근 작업 설정 — 누락 없이 저장

원인: 임포징을 **프리셋**으로 잡으면 실제 옵션은 `_impProfile`에 있는데, 스냅샷은 UI 필드만 담고
복원 때 `_impProfile = null`로 지우기까지 해서 제본 설정이 통째로 증발했다.

`captureExtraPreset`/`applyExtraPreset`에 추가:
- `imp.profSel` / `imp.profName` / `imp.profOpts`(=`_impProfile`) — 복원은 **이름 우선 매칭** 후 인덱스 폴백
- `bleed { enabled, mm, crop }` — ◲ 블리드가 통째로 빠져 있었다
- `proc.outlineMode` — 곡선화/완전 임베드 방식
- `cover` — 표지 만들기 값 전체. `captureCoverState()` / `applyCoverState()`를 새로 뽑아
  표지 프리셋(`saveCoverPreset`/`loadCoverPreset`)과 코드를 공유한다.

기록 시점도 늘렸다: 1분 주기·종료 직전 스냅샷 외에 `applyChanges` 성공 지점과
`adoptImposedResult`(임포징 생성)에서도 `recordWorkHistory()`.
요약 문구에 임포징 프리셋 이름·잉크정규화·폰트안전화 추가.

검증: 프리셋 불러오기 → 스냅샷 → 상태 흐트림 → 복원 시 프리셋 이름·드롭다운 선택·포함 상태 모두 재현 ✔

## 8. 매직미러 블리드 후 재단선이 블리드 바깥에 찍히던 오류

원고가 이미 블리드를 품고 있으면 `TrimBox < MediaBox`인데, 임포징은 페이지 전체(=블리드 포함)를
배치 사각형으로 보고 재단선을 그 바깥 테두리에 찍었다.

- `pageTrimInset(pg, w, h, rot)` 신설 — TrimBox → 표시 좌표 사방 인셋(pt).
  **회전 매핑**: 90° = 좌→상·우→하·하→좌·상→우 / 180° = 좌↔우·상↔하 /
  270° = 좌→하·우→상·하→우·상→좌 (embedAllPages의 변환행렬에서 유도).
- `embedAllPages`가 `{ e, w, h, trim }`으로 실어 주고, `drawPlaced`가 인셋만큼 안으로 줄인
  사각형을 반환 → 재단선·프레임이 자동으로 트림 위치에 찍힌다.
- 오탐 방지: 인셋이 음수거나 트림이 페이지의 절반 미만이면 무시(null) → 일반 PDF는 기존 동작 그대로.

검증:
- 노드 추출 테스트(앱 파일에서 실제 함수 eval) — 인셋 검출 ✔ / 회전 90° 매핑 ✔ / 트림 없는 문서 회귀 ✔
- 실사용 경로(◲ 블리드 5mm → 모아찍기 2×1 A3 재단선) 결과 시트의 재단선 좌표를
  콘텐츠 스트림에서 추출해 계산값과 대조 — 트림 모서리 (147.63, 210.94)·(1042.91, 630.94) 일치 ✔
- Electron + pdf.js 렌더 PNG 육안 확인 — 재단선이 미러 블리드 띠 **안쪽** 트림선에 위치 ✔

---

## 변경 파일

`main.js` · `preload.js` · `src/app-core.js` · `src/app-process.js` · `src/app-ui.js` ·
`src/index.html` · `src/style.css` → `dist/win-unpacked/resources/app/` 동기화 완료.
