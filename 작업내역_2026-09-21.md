# 2026-09-17 ~ 09-21 작업내역

> **보내기로 이미지 여러 장이 안 열리던 문제 · Dot Gain 기본 '없음' 고정 · ✂ 분리 저장(좌클릭 적용본 / 우클릭 원본).**

---

# 1. '보내기'로 이미지 여러 장 → 앱만 뜨고 아무 일 없음

## 증상
탐색기에서 이미지 여러 장 선택 → 보내기 → PDF Editor: 프로그램만 실행되고, 원래 뜨던 **열 순서 정하기** 창이 안 뜸.

## 원인
`main.js` `OPEN_DOC_RE`(실행 인자로 받을 확장자)에 **이미지가 없었다** → 이미지 경로가 전부 걸러져 렌더러에 아무것도 안 넘어감.
열기 다이얼로그는 이미지를 받으니, 보내기 경로에서만 생긴 문제. 순서 창·합치기 로직은 멀쩡했다.
PDF와 이미지를 섞어 보내면 이미지만 빠지던 것도 같은 원인.

## 처리
`OPEN_DOC_RE`에 열기 다이얼로그와 같은 이미지 확장자(png·jpg·jpeg·gif·bmp·webp·tif·tiff·avif) 추가.

## 검증 — `scripts/test/sendto-images.e2e.js` (새로 만듦)
앱 본체를 `TEST_WINDOW=left`로 띄우고 CDP(원격 디버깅 포트)로 화면을 읽는다.

| 항목 | 고치기 전 | 고친 후 |
|---|---|---|
| 꺼진 앱에 이미지 3장 → 순서 창에 3장 | ✘ | ✔ |
| 통합창 → 3쪽 한 문서·쪽마다 컬러 판정 | ✘ | ✔ |
| 켜진 앱에 2장 더(second-instance) → 순서 창 | ✘ | ✔ |

옛 `main.js`로 되돌려 같은 검사가 5/5 실패하는 것도 확인(사용자 증상 그대로 재현).

---

# 2. Dot Gain — 켤 때마다 '없음'

## 질문: 예전(드롭다운이 없던 때) 기본값은?
**0% = 보정 없음.** 드롭다운이 없던 동안 `getDotGain()`은 없는 요소를 읽어 늘 0이었다.
→ 지금 4단계의 **"◐ Dot Gain 없음"**과 같다. (그보다 더 전, 최초 버전은 드롭다운 기본 20%였고 그 식은 지금 '강 20%'와 같다)

## 처리
- 항목 이름 "◐ Dot Gain 없음 **(기본)**" (상단·사이드바 두 곳)
- 사용자 지시로 **고른 단계를 기억하지 않는다** — 켤 때마다 없음(0). 옛 버전이 남긴 `localStorage dotGainLevel`은 켤 때 지운다
- 단계를 고르면 안내에 "(프로그램을 다시 켜면 '없음'으로 돌아갑니다)"

## 검증 — `dotgain.e2e.js` 9/9
- 켜면 기본 없음(0), 15%를 고르고 옛 저장값 20%까지 남긴 뒤 다시 켜도 없음
- 없음 = 원래 휘도, 10·15·20%는 곡선대로(기존 항목 그대로 통과)

---

# 3. ✂ 분리 저장

## 기능
완성본을 여러 PDF로 나눠 **한 폴더**에 저장.
- **쪽 수 단위**: 10쪽·20쪽·대수 16쪽 등 (마지막은 남는 만큼)
- **쪽 범위**: `21-39`, `1-20, 41-60`, `7` (하이픈 - – ~ 모두 인정, 겹침 허용)
- 창에서 만들어질 파일 이름·개수를 바로 보여 주고, 잘못 적으면 이유 표시 + 저장 잠금
- 파일 이름 `원고_001-010.pdf`(전체 쪽수 자릿수로 채움), 같은 이름이 있으면 `-1`
- 버튼: 상단 작업줄 · 좌측 사이드바 · 오른쪽 편집 패널
- **좌클릭 = 적용본, 우클릭 = 원본 그대로**(⇩ 다운로드와 같은 규칙, 사용자 요청)

## 설계
- 최종 바이트를 다운로드와 **같은 함수** `buildFinalSaveBytes`로 만든다(최적화 → 목차 북마크 → 폰트 안전화 → 컬러 검수).
  `downloadProcessed`에서 뽑아낸 것 — 두 경로가 따로 조립하면 한쪽에만 단계가 빠지는 옛 버그가 재발한다.
  잉크 정규화 꺼짐 확인도 `inkNormSaveConfirmed` 공용.
- 순수 함수 `splitRangesEveryN`·`parseSplitRanges`·`splitPartFileName` — 노드에서 앱 파일을 떼어 검증.
- 저장: main `dialog:pickSplitFolder`(폴더 1회 선택·라이선스 확인) + preload `saveFilesToFolder`(`basename`만 써서 폴더 밖으로 못 나감, `writeFileSafe`).
- 나눈 파일마다 `savePdfDoc`을 지나므로 흑백 투명도 그룹(함정 16)도 그대로 붙는다. 목차 북마크는 안 들어간다(안내 문구).

## 함정
- **disabled 버튼은 우클릭(contextmenu)도 안 먹는다** — 적용 전이라고 잠그면 원본 분리가 죽는다. 문서만 있으면 누를 수 있게 두고 `btn-dim`으로 흐리게만.
- 검사 하네스에는 Ghostscript IPC가 없어 폰트 안전화(기본 ON)를 꺼야 한다.

## 검증
- `split-save.test.js` 21/21 (smoke에 포함)
- `split-save.e2e.js` 17/17 — 적용 전 버튼 상태·좌클릭 안내, 우클릭 원본 나누기(24-25 → 2쪽), 10쪽 단위·범위 두 방식,
  디스크 파일 목록·쪽 수·**각 쪽 글자(P21~P25, P11~P20)**, 같은 이름 `-1`·`-2`, 문서 밖 범위 잠금
- 다운로드 경로 회귀(공용 함수로 바꾼 뒤): `bw-shared-image.e2e.js` 통과, `cache-generation.e2e.js` 15/15
- 창 모양 캡처 확인(224쪽 16쪽 단위 → 14개)

---

# 공통
- `npm run smoke` 통과, 포터블 앱 동기화(변경 파일 전부 내용 일치)
- CLAUDE.md: 3.2 Dot Gain 기억 안 함, 3.4 분리 저장 절 신설

---

# 4. 복제 2-up 대용량 원고 "Array buffer allocation failed"

> 이 작업은 사용자가 실수로 **business-mgmt 세션**에서 요청해 거기서 진행됐다. 코드·검사·포터블 동기화는 이 저장소에 반영돼 있고,
> 커밋·푸시·CLAUDE.md 함정 21번 추가는 이 프로젝트 세션의 "작업 저장해줘"(2026-09-21)에서 마무리했다.

## 증상 (사용자 보고)
도록 150쪽 작업 파일(`D:\바탕화면\도록_초본_작업중.pdfw`, 원본 998MB + 적용본 952MB)에 프리셋 **270-390_양면_2up_논문**(dup)을 불러 ✔ 적용 →
"처리 중 오류: Array buffer allocation failed". 다운로드가 흐리게 남고, 우클릭 원본 저장은
"The first argument must be … Received null", 적용 버튼은 눌러도 계속 반짝임.

## 원인
- 렌더러 한계 실측(Electron 31.7.7): ArrayBuffer 하나 1.9GiB 성공·2.0GiB 실패, 렌더러 합계 ≈14GB(워커 포함 한 풀).
- `buildDup2upBytes`가 원고를 `embedAllPages`로 **두 번**(정방향·180°) 임베드 — pdf-lib `embedPage`는 호출마다 새 복사기라
  이미지·폰트를 통째로 다시 복사 → 결과 1,956MB(원고 2.05배). 적용 직후 렌더러 15.3GB → 다운로드 복사에서 실패.
- 적용 실패 → `processedPdfBytes=null` → 다운로드 흐림·`needs-apply` 반짝임 유지. 우클릭 null은 풀이 바닥난 상태의 contextBridge 복사 실패로 추정.

## 처리
- `rotate180Embeds(out, emb0)`(app-process.js): 정방향 한 벌만 임베드하고 180°는 그 폼을 `/P0 Do`로 부르는 겉 폼.
  `drawPage`가 `PDFEmbeddedPage` instanceof 검사를 해서 `PDFEmbeddedPage.of(ref, out, Object.create(PDFPageEmbedder.prototype))`로 만든다.
  트림 인셋은 l↔r·b↔t 교환. 결과 1,956MB → **978MB**, 조립 6.3초 → 3.8초.
- preload `asBuffer`: null이면 한국어 안내("메모리가 부족했을 수 있습니다…")로 던진다.
- `scripts/build-imposition-standalone.js` BUILDERS에 `rotate180Embeds` 추가(드리프트 검사 통과, dist/임포징도구.html 재생성).

## 검증
| 검사 | 결과 |
|---|---|
| `dup-2up-memory.e2e.js` 합성 1.1GB / `DUP_PDFW` 실파일 | 6/6 · 5/5 (결과 ≈ 원고 크기) |
| `dup-2up-flow.e2e.js` (하네스, 사용자 흐름) | 옛 코드 **3 실패로 재현** → 새 코드 9/9 |
| `dup-2up-realapp.e2e.js` (앱 본체·CDP·gs·프리웜·실제 저장) | 7/7, 렌더러 최고 12.0GB |
| 옛/새 결과 gs 렌더 비교 (회전 0·90·180·270 + 비대칭 트림 + 재단선) | 6면 픽셀 동일 |
| smoke · imposition-marks · gray-blend-imposition · imposition-perchapter(-nup) · workfile-2gb · split-save · imposition-standalone | 전부 통과 |

포터블 앱 동기화: `src/app-process.js`, `preload.js`.

## 남은 것
- 적용 순간 12GB/14GB — 1GB 원고는 열기만 해도 ≈6GB. `embedAllPages`가 쪽마다 embedPage라 쪽 사이 공유 폰트도 쪽 수만큼 복제(개선 후보).
- ~~CLAUDE.md 함정 추가~~ → 21번으로 추가함.
