# 2026-09-06 작업내역

> **중철 저장 파일명에서 `2up` 표기 제거** — 사용자 지시.

---

# 1. 중철 파일명 표기

중철은 시트 한 장에 두 쪽이 들어가는 것이 정의상 당연하므로, 파일명에 `2up`을 덧붙일 이유가 없다.

```
문서_중철2up.pdf   →   문서_중철.pdf
```

## 처리

`impNameTag()`([src/app-process.js:3859](src/app-process.js)) 의 중철 분기 한 줄.

```diff
-      if (mode === 'booklet')  return '중철2up';
+      if (mode === 'booklet')  return '중철';
```

파일명 조립은 `defaultProcessedName()`(app-core.js)의 `` `${base}_${tag}${amountStr}.pdf` `` 이므로
태그만 바꾸면 그대로 반영된다.

중철 **표지 분리 저장**(`generateBooklet`)은 원래부터 `_중철_표지.pdf` / `_중철_내지.pdf` 였으므로
이제 표기가 서로 일관된다.

## 검증

앱 파일에서 `impNameTag()`를 **추출해 실제 코드로** 돌려 확인(테스트 복사본 금지 — CLAUDE.md 7-2).

| 모드 | 태그 | 비고 |
|---|---|---|
| **중철 (booklet)** | **중철** | ← 변경 |
| 복제 2부 (dup) | 2up | 그대로 |
| 정합 2×2 (cutstack) | 정합4up | 그대로 |
| 모아찍기 2슬1페 (nup) | 2up | 그대로 |
| 반복 3×2 (repeat) | 반복6up | 그대로 |
| 임포징 끔 | 1up | 그대로 |

다른 모드의 태그는 영향 없음. `npm run smoke` 통과 · 포터블 앱 동기화 체크섬 일치.
