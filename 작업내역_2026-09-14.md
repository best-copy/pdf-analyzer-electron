# 2026-09-14 작업내역

> **눈에 보이는 결함 세 가지를 원인까지 내려가 잡았다.**
>
> E-book 넘김 떨림·멈춤, 사진의 1px 흰 가로줄, 흑백+중철본의 Acrobat "페이지 오류".
> 셋 다 "그럴듯한 수정"이 먼저 실패했고, 프레임·픽셀·바이트를 직접 재서야 원인이 드러났다.
> (2026-09-11부터 이어진 작업을 한 번에 저장)

---

# 1. E-book 시안 — 페이지 넘김 깜박임·떨림·멈춤

## 1-1. 증상
- 넘길 때 책등이 깜박이고, 넘김 **시작·착지 순간** 왼쪽/오른쪽 페이지가 살짝 떨림(양방향)
- 착지·시작 순간 짧게 멈칫함

## 1-2. 원인
| 증상 | 원인 |
|---|---|
| 착지 떨림 | 3D 조각(strip leaf)을 평평한 페이지로 바꾸는 순간 1px 어긋남 — `background-image`는 `<img>`와 샘플링이 1px 다르다 |
| 착지 잔상 | `flattenLeaf`가 transform을 풀면서 원래 위치로 되돌아감 |
| 착지 멈춤 | 목록 썸네일이 원본 크기 그림이라 GPU 디코드 |
| 시작 멈춤 | 조각 배경의 data URL 파싱 |

## 1-3. 처리
- 평평한 덮개 `.lflat`를 `<img>`로 칠함(`paintFlat`), 착지 위치를 `T.r.l ± T.r.w`로 고정
- 페이지 그림은 blob 주소(`pageUrl`), 목록 썸네일은 `createImageBitmap`으로 작게(`pumpThumbs`), 이미지 풀(`take`/`giveBack`)
- `decode()`·밑깔개 방식은 효과 없어 채택하지 않음

## 1-4. 검증
- `beginFrameSubscription` 매 프레임 캡처 + LoAF 관측: 40ms 넘는 rAF 공백 0, 잔상 0
- 사용자 파일 `문서 인쇄_시안.html`로 확인
- `scripts/test/ebook-turn-flicker.e2e.js`는 회귀 시 종료 코드 1

---

# 2. 사진의 흰 가로줄 (E-book·출력·미리보기·썸네일)

## 2-1. 원인
한글·오피스 변환 PDF는 사진을 가로 띠로 잘라 넣는다. pdf.js로 그리면 띠 경계마다 1px 흰 줄이 구워진다.

## 2-2. 처리
- 새 파일 `src/seam-repair.js` — `renderPageNoSeams`
  - 렌더 중 `drawImage`를 가로채 그림 위치를 얻음 (`getOperatorList` 별도 호출은 +93%, 이 방식은 +3.4%)
  - 가장자리가 1px 안에서 맞닿고 1.5px 안에서 정렬된 곳만 이음매로 판정 → 그 띠만 보정
  - 처음의 픽셀 기반 메우기는 글자 틈을 망가뜨려 폐기
- 적용처: 래스터화 출력·E-book·작업공간 표본·표지 틀·자동 뒤표지(app-process), 색 분석·썸네일(app-core, `THUMB_RENDER_V=2`로 옛 썸네일 재생성), 지연 미리보기·크게 보기(app-ui), 편집기(editor.html), E-book 독립 도구 빌드
- CLAUDE.md 함정 #10으로 등록

## 2-3. 검증
- 284쪽 PDF: 이음매 위 밝은 픽셀 138,172 → **0**
- 주의: 첫 측정의 흰 줄 탐지기(임계 235)는 JPEG 전 이음매(≈215)를 못 봐 "개선 없음"으로 잘못 나왔음 → 실제 이음매 선 위 픽셀로 재측정
- 단위 테스트 `scripts/test/seam-repair.test.js`

---

# 3. 흑백 + 중철 → Acrobat "이 페이지에 오류가 있습니다"

## 3-1. 재현
`양졸당파 세계도_4부_56.pdf` — 1·56쪽 제외 흑백, 중철 임포징.

## 3-2. 원인
원본 콘텐츠 스트림 줄바꿈이 **CR(\r)**이고 쪽마다 비압축 RGB 인라인 이미지(`BI … ID … EI`)가 있었다.
- 바이트 단계가 `\nBI\n`·`\nID\n`만 찾아 **인라인 RGB가 흑백으로 안 바뀜**
- 문자열 단계가 `\rBI\r`를 인식한 뒤 경계를 못 찾으면 `B`를 버려 **BI → I** → 그림 바이너리가 페이지 명령으로 읽힘
- 중철은 이미 깨진 페이지를 옮겼을 뿐(흑백만 적용한 출력부터 오류)

## 3-3. 처리 (`src/worker-gray.js`)
- `isInlineBIAt` / `findInlineBI` / `scanInlineImage` — 모든 공백을 구분자로, 실패 시 `{ok:false,next}`로 선형 시간 스캔
- 비압축·크기 일치·모호하지 않을 때(ID 뒤 CRLF 판별)만 RGB/CMYK 8bit → 회색 변환, 나머지는 **원본 바이트 그대로**
- 경계 불명이면 글자를 버리지 않음
- 메인 스레드의 죽은 `grayifyStream` 사본 제거
- 작업 파일 `.pdfw`에 `grayV`(=2) 기록 — 옛 파일은 저장된 적용본을 재생성

## 3-4. 검증
- 단위 테스트 39개(`inline-image.test.js` 포함 — LF/CR/CRLF/스페이스, 데이터 속 EI, 글자 속 BI, 압축 그림 등), 옛 워커로 돌리면 실패함을 확인
- 실제 파일 재현: 구조 감사 문제 0, Ghostscript 오류 0(수정 전 "error executing PDF token"), 인라인 그림 RGB→회색
- `.pdfw` 옛 파일 재생성 테스트 통과
- **미확인**: Acrobat으로 직접 열기 → 이미 저장해 둔 결과물은 깨진 채이므로 앱 재시작 후 다시 만들어 확인 필요
- 한계: 압축·Indexed 인라인 그림은 변환하지 않음(원본 유지)

---

# 4. 검사 창은 가장 왼쪽 모니터

- 공통 환경변수 `TEST_WINDOW=left`(옛 이름 `PDFEDIT_TEST_WINDOW`도 받음) — `main.js testWindowPos`, 메인·라이선스·편집기·팝업 창
- 크기도 왼쪽 화면 작업 영역 안으로 맞춤(좁은 화면 대비), `smoke.js`가 변수 전달
- 하네스 15개 `...leftWin(W,H)` 적용(`scripts/test/_leftwin.js`), CLAUDE.md 7-7
- 다른 프로젝트에도 적용: PDF Checker · pdf_rasterizer · business-mgmt(창 위치 기억은 검사 모드에서 읽기/저장 안 함) · Work Folder(PS1)

---

# 5. 목차 검증기 연동 대상 정리 (PDF Checker 저장소)

- PDF- 프로젝트(PDF 편집기)는 **중단** — 다른 프로젝트와 연관 짓지 않음
- 목차 검증기 "✏ **PDF 분석기**에서 이어서 작업"이 PDF 분석기 exe만 찾도록 변경
  - Setup·구버전 제외, 여러 버전이면 최신, 개발 경로 `../pdf-analyzer-electron/dist/win-unpacked`, 바로가기 허용
- 분석기 쪽은 이미 실행 인자 문서 열기·단일 인스턴스 전달을 지원 → 변경 없음
- 검증: 실제 폴더로 탐색 함수 실행 — 바로가기→`PDF 분석기.exe`, dist 폴더→`PDF분석기-포터블-3.7.0.exe`, 없음→안내

---

# 6. 도구 — review-plan / review-implementation 스킬

- `review-plan`: 요구사항 충족 검토 + 사이드 이펙트 검토 서브에이전트 2개
- `review-implementation`: 요구사항 완전성 + 코드 품질·일관성 + 보안·연동·파리티 서브에이전트 3개
- 이번 작업에 두 스킬을 실제로 돌려 지적(CRLF 모호성, 이차 시간 스캔, 길이 불일치, 낡은 .pdfw, 팝업 창 위치, 죽은 코드 등)을 반영

---

## 확인
- `npm run smoke` 통과(구문 + 단위 테스트 2종 + 12초 부팅)
- 포터블 앱 `dist/win-unpacked/resources/app/` 동기화 — 파일 대조 일치
