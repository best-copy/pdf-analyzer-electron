# 2026-08-26 작업내역

> 이 날의 작업 3건: ① 체험판 오프라인 활성화 ② E-book 출력 시안 ③ 작업 파일(.pdfw)

---

# 체험판 활성화: 오프라인 등록 + 외부 접속 터널

## 배경
테스터 PC에서 활성화가 안 됨. 원인은 **관리자 PC(활성화 서버 8736)에 외부 접근이 안 되는 것**
(공유기 포트포워딩 불가). 서버에 못 닿으면 키 등록 자체가 막힌다.

"앱이 이메일을 보내고 내가 메일에서 승인" 안을 검토했으나 채택하지 않음 —
**승인해도 서명 토큰을 테스터 앱에 돌려줄 경로가 여전히 없다**(결국 외부 접근 가능한
엔드포인트를 폴링해야 함). 메일 왕복으로 코드를 주고받는 형태면 아래 오프라인 등록과
같은 일을 더 느리게 하는 셈이고, SMTP 자격증명이 평문 배포본에 박히는 문제도 생긴다.

## A. 오프라인 등록 (서버 없이, 코드 2회 왕복) ← 기본 경로
```
테스터 앱: 키 입력 → [요청 코드 만들기]  →  PDFEQ1.<base64url(키+기기지문+호스트)>.<CRC4>
   ↓ 카톡·이메일 무엇이든 (복붙)
관리자 앱: 🛠관리자 → [오프라인 활성화 코드 발급]에 붙여넣기 → 서명된 활성화 코드
   ↓
테스터 앱: [오프라인 등록] 2단계에 붙여넣기 → 완료
```
- **1회용 보장은 그대로**다. 원래도 서버가 아니라 `keys.json` 기록이 하던 일이라,
  같은 키 다른 PC → 거부, 같은 PC 재설치 → 재발급(만료일 연장 없음)이 동일하게 적용된다.
- 온라인/오프라인이 **`license.js`의 `activateRecord` 한 함수를 공유**한다.
  (`license-server.js`의 `doActivate`는 HTTP 옷만 입힌다 — 규칙을 두 번 쓰면 갈라진다)
- 오프라인 토큰은 payload에 `off:1`, **`next`를 넣지 않는다** → 서버 재확인을 아예 건너뜀.
  대가로 **원격 취소가 닿지 않으므로 기간을 짧게(7~14일)** 주는 것이 안전 (UI에 경고 표시).
- 상태 표시: `체험판(오프라인) · N일 남음`.

## B. 외부 접속 터널 (cloudflared) ← 자동 흐름을 살리고 싶을 때
- `license-tunnel.js` 신규. 관리자 화면 [🌐 외부 접속 터널]에서 켜면
  `https://xxx.trycloudflare.com` 주소가 생긴다. **포트포워딩·공인IP 불필요**(아웃바운드만 사용),
  덤으로 HTTPS라 키가 평문으로 흐르지 않는다. 미설치 시 [cloudflared 설치](winget) 버튼.
- ⚠ 무료 임시 주소는 **재시작마다 바뀐다** → 그 주소로 등록한 테스터는 7일 재확인에 실패하고
  유예 3일 뒤 잠긴다. 장기 사용은 A(오프라인) 권장. UI에 그대로 명시해 둠.
- 앱 종료 시 `will-quit`에서 cloudflared 자식 프로세스 정리.

## 인증 창 재배치 (같은 날 추가 요청)
학교 방화벽이 8736 리스닝 자체를 막아 서버 방식이 원천 봉쇄된 것이 확인되어,
**오프라인 등록을 기본 화면으로** 올렸다.
- 테스터 탭: [🔑 체험 키 등록](키 입력 + 1·2단계 코드 왕복)이 첫 카드.
  예전 서버 방식은 `<details class="fold">`로 접어 [🌐 서버로 등록 (직접 접속되는 경우에만)]에 넣음.
  **키 입력칸(#key)은 두 방식이 공유**한다 — 온라인 등록도 그대로 동작.
- 관리자 탭: 키 발급 → 오프라인 코드 발급 → (접힘) 서버·터널 → 발급 내역 순.
- 카드 제목·요약줄 이모지는 `filter:grayscale(1)`로 무채색화(테마 규칙).
- 오프스크린 Electron 스크린샷으로 3화면(테스터·펼침·관리자) 레이아웃 눈으로 확인.

## 변경 파일
| 파일 | 내용 |
|---|---|
| `license.js` | `activateRecord`(공유 판정) · `offlineRequest/offlineIssue/offlineActivate` · 상태에 `offline` |
| `license-server.js` | `doActivate`를 `activateRecord` 호출로 축소(로직 중복 제거) |
| `license-tunnel.js` | **신규** — cloudflared 탐색·기동·주소 파싱·설치 |
| `main.js` | IPC 5종(`lic:offlineRequest/offlineActivate`, `lic:admin:offlineIssue/tunnel*`) |
| `preload-license.js` | 위 API 노출 |
| `src/license.html` | 테스터 [🔌 오프라인 등록] · 관리자 [🔌 오프라인 코드 발급] · [🌐 터널] |
| `scripts/smoke.js` | **HTML 인라인 `<script>` 구문검사 추가** |
| `package.json` | electron-builder `files`에 `license-tunnel.js` (화이트리스트라 누락 시 크래시) |

## 검증
- `scripts/test/license-offline.test.js` **신규 26항목** — 요청코드 왕복·1회용 유지·온라인↔오프라인
  교차 거부·취소/없는 키·잘린 코드·다른 PC 코드 거부, 그리고 자식 프로세스(USERPROFILE 위장)로
  "60일 전 발급 오프라인 토큰도 재확인 잠금 없음"까지.
- 기존 `license-server.test.js` 22 / `license-tester.test.js` 8 회귀 통과 → **총 56/56**.
- 실제 Electron 창 E2E(오프스크린)로 `src/license.html` + `preload-license.js` 실경로 구동:
  요청코드 생성 → 관리자 발급 → 등록 완료 → `license.dat` 저장까지 확인.
- `npm run smoke` 통과.

## 이번에 걸린 함정 (재발 방지)
1. **HTML 인라인 스크립트가 깨져도 스모크가 통과했다.** 문자열 안에 실제 줄바꿈이 들어가
   `Uncaught SyntaxError`로 인증 창의 스크립트 전체가 죽었는데도 부팅 검사는 초록불이었다.
   → 스모크에 HTML `<script>` 검사를 추가(오류 줄번호를 HTML 기준으로 환산해 표시).
2. 그 검사기 자체의 함정: **여는 태그만 보고 판단할 것.** 본문까지 정규식으로 보면
   body 안의 `type="button"` 같은 평범한 문자열에 걸려 그 파일이 조용히 검사에서 빠진다
   (editor.html이 실제로 그렇게 빠졌다 — "0개"로 표시되는 것이 유일한 단서).
3. `where cloudflared`도 `reg query`와 같다 — 없을 때 stderr를 뱉으므로
   `stdio:['ignore','pipe','ignore']` 필요.

## 남은 것
- 터널 경로는 cloudflared 미설치 상태라 **주소 발급까지 실측하지 못했다**(설치 후 1회 확인 필요).
- 2단계 하드닝(asar + bytenode)은 그대로 미착수.

---

# 📖 E-book 출력 시안 (고객 확인용 HTML)

## 배경·목적
고객에게 출력물이 **어떻게 나오는지** 보내 확인받는 수단. PDF를 그대로 보내면 고객이 열지 못하거나
편집·재사용할 수 있고, 인쇄 시트(임포징)를 보내면 오히려 혼란스럽다.
→ **책처럼 넘겨보는 HTML 파일 하나**를 만들어 메일·카톡에 첨부한다.

## 결과물
- 자체 완결 HTML **한 개** (이미지·CSS·JS 전부 인라인, 외부 요청 0). 더블클릭하면 브라우저에서 열림.
- 보기: **책 넘김**(펼침면) 기본 + **인쇄 대수**(임포징 시트) 토글 · 페이지 확대 · **실제 크기**(모니터 보정)
- 인쇄 실무 표시: 판형(mm)·쪽수·제본, 재단선 안내선(블리드 있을 때), 좌철/우철 넘김 방향
- 워터마크 '시안' 연하게 겹침(옵션), "화면 색상은 참고용" 고지 고정

## 들어간 자리
| 위치 | 내용 |
|---|---|
| 왼쪽 사이드바 `📖 E-book 시안 (고객 확인용)` | 화질 3단(가벼움100/표준150/고화질200) · 좌철/우철 · 워터마크 · 인쇄 대수 포함 · 재단선 · [생성] · 예상 용량 |
| `dist/E북시안도구.html` | PDF를 끌어다 놓으면 시안을 만드는 **독립 도구**(앱 없는 PC용). `node scripts/build-ebook-standalone.js`로 재생성 |

## 구조 (드리프트 방지)
- `src/app-process.js` 끝의 **`// <EBOOK-CORE>` ~ `// </EBOOK-CORE>`** 구간 = 순수 함수만
  (`ebookSpreads`, `EBOOK_CSS`, `EBOOK_JS`, `ebookWatermarkUri`, `buildEbookProofHtml`).
  DOM·앱 전역을 만지면 안 된다 — 독립 도구가 이 구간을 **그대로 떼어** 굽기 때문.
- 렌더(`ebookRenderPages`)도 앱·독립도구가 같은 함수를 공유한다(화질이 갈라지지 않게).
- 책 넘김 소스 = `buildOptimizedBase` + `applyBleedStage` (다운로드본과 동일 경로),
  인쇄 대수 = 거기에 `buildImposedBytes` — 임포징이 켜져 있을 때만.
- 저장: `dialog:saveFilePath`에 `kind:'html'` 추가(없으면 종전대로 PDF). 라이선스 게이트는 그대로 통과한다.

## 검증
- `scripts/test/ebook-proof.test.js` **21항목** — 코어 순수성, 스프레드 구성(표지 단독·홀수쪽·우철 반전·
  37쪽 전수 1회씩), HTML 조립(이스케이프·외부참조 0·인라인 이미지), 스크립트 조기 종료 방어.
- 실제 12쪽 PDF로 **진짜 pdf.js 렌더** → 시안 생성 → Electron으로 열어 측정:
  종이 픽셀 `[255,255,255]` 순백, 색띠 `[217,51,51]`(원본 0.85/0.2/0.2와 정확히 일치),
  150dpi A5 = 875×1240px, 넘김 1→2→3 정상, 인쇄 대수 토글 12시트, 실제 크기 559.4px(=148mm) 정확, 콘솔 오류 0.
- 독립 도구 E2E: PDF 투입 → [시안 만들기] → `카탈로그_시안.html` 315KB 다운로드 완료, 재단선 3mm 반영.
- 실제 앱 부팅 후 UI 배선 확인(함수·섹션·옵션 토글·문서 없을 때 안내문구).
- `npm run smoke` 통과.

## 이번에 걸린 함정
1. **주석 안의 닫는 스크립트 태그가 독립 도구를 통째로 죽였다.** 하필 "닫는 태그 조심"이라고 쓴
   주석의 그 태그에서 브라우저가 인라인 스크립트를 끊어, 도구의 함수가 하나도 정의되지 않았다.
   → 코어 주석에서 제거 + **빌드 스크립트가 태그 짝을 세어 실패시키도록** + 테스트에도 고정.
   (증상이 `Uncaught SyntaxError: Unexpected end of input` 하나뿐이라 원인 찾기가 고약하다.
    `node --check`로는 절대 안 잡힌다 — 파일 자체는 문법상 멀쩡하기 때문)
2. 오프스크린 `capturePage()`는 **전환 애니메이션 중의 흐린 프레임**을 그대로 준다.
   회색으로 보인다고 렌더가 잘못된 게 아니다 — 색은 스크린샷이 아니라 `getImageData`로 재야 한다.

## 남은 것 / 참고
- 앱 내 [📖 E-book 시안 생성] 버튼을 **실제 문서로 끝까지(저장 다이얼로그 포함)** 눌러본 검증은
  남아 있다(다이얼로그가 떠서 자동화가 막힘). 파이프라인 각 조각은 위와 같이 개별 검증됨.
- 용량 감각: 단순한 A5 12쪽 = 0.3MB. 사진이 많은 실제 원고는 150dpi에서 쪽당 100~300KB 잡으면 된다.
  사이드바에 예상 용량을 보수적으로(쪽당 120KB × (dpi/150)²) 표시하고 20MB 넘으면 경고한다.

---

# 💼 작업 파일(.pdfw): 상황 그대로 저장 · 더블클릭으로 이어서 열기

## 요구
작업 중이던 상황을 **파일까지 포함해** 저장해 두었다가, 그 파일을 클릭하면 저장 시점 그대로 프로그램이 열릴 것.

## 결과물
`.pdfw` 파일 하나에 **원본 PDF + 작업 상태 전부**가 들어간다. 더블클릭하면 그 시점 그대로 열린다.
원본이 안에 들어 있어 **다른 PC로 옮겨도** 그대로 열린다(원본 파일을 따로 챙길 필요 없음).

**담기는 것**
- 원본 PDF 바이트
- 설정: 편집(배율·여백·N-up·기울기·정렬·제본여백·머리글/바닥글·워터마크) · 처리(흑백·잉크정규화·폰트안전화) ·
  임포징(모드·프로파일·용지·거터·밀림보정) · 블리드 · 표지
- `editSettings` **통째** — 챕터별 설정(byChapter)·적용범위(scope)·페이지별 개별보정까지
- 문서 상태: 페이지 순서 · 회전 · 빈 페이지 · 흑백 확정 · 선택 · 로마자 번호
- 페이지 **내부편집 결과**(편집된 단일페이지 PDF들)
- 견적 항목

## 파일 형식 (`<WORKFILE-CORE>`, 순수 함수)
```
'PDFEDITWORK1\n'   14바이트 매직
u32LE              매니페스트(JSON) 길이
JSON               { v, savedAt, doc, state, entries:[{k,name,len}] }
블롭들             entries 순서대로 이어붙임 (원본 PDF · 내부편집 결과)
```
ZIP을 쓰지 않은 이유: PDF는 이미 압축돼 있어 이득이 없고, 의존성 없이 앞 14바이트만 보면 우리 파일인지 안다.
`packWorkFile` / `unpackWorkFile` / `describeWorkFile`은 DOM·앱 전역을 안 만지는 순수 함수라 노드에서 단독 검증한다.

## 들어간 자리
| 위치 | 내용 |
|---|---|
| 왼쪽 사이드바 `💼 작업 저장 · 이어서 열기` | [💼 작업 저장 (.pdfw)] · [📂 작업 파일 열기] · [🔗 더블클릭 연결 등록/해제] |
| 탐색기 더블클릭 | `main.js`의 `OPEN_DOC_RE`에 `pdfw` 추가 → 기존 `external:open` 경로로 전달, 렌더러가 분기 |
| 드래그&드롭 | 업로드 영역에 .pdfw를 떨어뜨리면 복원(분석 파이프라인을 타지 않음) |
| 저장/열기 다이얼로그 | `kind:'pdfw'` — 저장 필터·열기 필터 분리 (없으면 종전대로 PDF) |

**더블클릭 연결**은 `HKCU\Software\Classes` 에만 쓴다(관리자 권한 불필요, 같은 버튼으로 해제).
PDF 등 다른 형식의 연결은 건드리지 않는다. 포터블이라 실행 경로가 바뀔 수 있어 **현재 exe 기준으로 등록 여부를 판정**한다.

## 복원 순서 (중요)
1. 안에 든 PDF를 **새 탭**으로 분석 — `startLoad`를 쓰면 열려 있는 문서에 '챕터'로 합쳐지므로 반드시 `createTab`+`analyzePDF`
2. `applyPresetData(state.data)` — 설정·임포징·블리드·표지 UI까지 복원(프로파일 적용과 같은 경로)
3. `editSettings` 통째 되씌우기 → `syncEditUI()`
4. 내부편집 결과를 `contentEdits`에 복원
5. `restoreDocState()` — 순서·회전·빈페이지·흑백·선택·개별보정
6. 견적 항목 복원 → 캐시 비우고 다시 그리기

## 검증
- `scripts/test/workfile.test.js` **17항목** — 앱 파일에서 코어를 그대로 떼어 실행:
  1:1 왕복(PDF·내부편집·상태 JSON), 첨부 0개/0바이트/300KB, 한글 파일명, entries·blobs 개수 불일치 거부,
  남의 파일(PDF)·짧은 파일·잘린 파일·JSON 훼손 거부, **byteOffset이 있는 뷰**에서도 동일 결과.
- 실제 앱 E2E(저장→새로고침→열기): 회전(2쪽 90°·4쪽 180°)·순서 변경·선택(1,2,7)·2-up·B5·여백 17mm·
  제본여백 12mm·개별보정·흑백·임포징 ON을 만든 뒤 저장 → 다시 열어 비교:
  **docState·editSettings·processingOptions 전부 저장 전과 동일(문자열 비교 일치)**.
- **더블클릭 경로**(실행 인자로 .pdfw 전달)도 같은 결과 확인 — 12쪽·회전·선택·설정 모두 복원.
- 전체 회귀: workfile 17 · ebook 21 · license 56 = **94/94**, `npm run smoke` 통과.

## 함정
1. **`startLoad`는 열려 있는 문서에 새 파일을 챕터로 합친다.** 작업 파일 복원에 쓰면 문서가 두 배가 된다
   → `createTab` + `await analyzePDF` 로 새 탭을 명시적으로 만든다.
2. **contextBridge로 노출된 `window.electronAPI.xxx`는 렌더러에서 덮어쓸 수 없다**(조용히 무시).
   테스트에서 저장 다이얼로그를 스텁하려다 실제 다이얼로그가 떠서 하네스가 멈췄다
   → 애초에 `buildWorkFileBytes()`(바이트 생성)와 `saveWorkFile()`(다이얼로그)을 분리했다. 설계도 더 낫다.
3. 검증 하네스에 파일 경로를 **argv로 주면 main.js가 실행 인자로 보고 문서를 한 번 더 연다**(12쪽이 24쪽이 됨).
   환경변수로 넘길 것.
4. 분석 완료 600ms 뒤 뜨는 '🕓 지난 작업 기록' 안내가 복원 안내를 덮는다 → `_openingWorkFile` 플래그로 억제.

## 남은 것
- 여러 탭이 열려 있으면 **활성 탭 하나만** 저장된다(저장 시 안내 문구로 명시). 탭별로 따로 저장하면 된다.
- 더블클릭 **연결 등록 버튼**은 레지스트리를 쓰므로 사용자가 직접 눌러 확인해야 한다(코드·상태 조회는 검증됨).

---

# ⚡ 임포징 미리보기 반영 속도 (편집 내용 반영 중… 단축)

## 증상
임포징을 켜고 편집 옵션을 만지면 "편집 내용 반영 중…"이 오래 걸린다.

## 실측으로 찾은 원인 (64쪽 18MB / 중철 32시트)
| 조작 | 총 시간 | 조립(워커) | 임포징 | **미리보기 렌더** |
|---|---|---|---|---|
| 여백 1mm | 2,961ms | 133 | 180 | **2,647 (89%)** |
| 거터 1mm | 2,850ms | 0(캐시) | 180 | **2,669 (94%)** |

- 임포징·조립은 이미 빠르다. **시간의 90%는 미리보기를 다시 그리는 데** 쓴다.
- **해상도는 범인이 아니다**: 렌더 폭 100→400px(4배)로 올려도 시트당 78~79ms로 동일
  → 래스터화가 아니라 **이미지 디코드·콘텐츠 파싱 고정비**다. 화질을 낮춰도 안 빨라진다.
- 같은 pdf.js 문서를 재사용하면 2,538→416ms(6배) → 출력이 바뀔 때마다 전체 재디코드가 비용의 본질.
- `wsSampleEligible()`의 `if (_impEnabled) return false` 때문에 **임포징을 켜면 표본 미리보기가 꺼져**
  매번 전 시트를 그린다.

## 처리 (A·B·D·E — 화질·결과물 무손실)
- **A. 지연 렌더**: 셀 자리를 먼저 깔아 즉시 화면을 교체하고, **보이는 셀부터** 그린 뒤 나머지를
  배경에서 채운다(IntersectionObserver 우선순위 큐). 그리는 총량·해상도는 그대로.
  `runLazyPreviewRender()` / `stopLazyPreviewRender()` (app-ui.js). 자리표시는 `.pv-pending`.
- **B. 병렬 렌더**: pdf.js는 디코드를 **자기 워커에서** 하므로 같은 바이트로 문서를 K개 띄우면
  거의 선형으로 빨라진다(실측 32시트: 1개 2,508 / 2개 1,339 / 3개 1,009 / 4개 847ms).
  → OffscreenCanvas·커스텀 워커 **불필요**. `previewPoolSize()`가 출력 용량·코어 수·작업량으로
  1~4개를 정한다(≤30MB 4, ≤80MB 3, ≤200MB 2, 그 이상 1 — 문서마다 바이트 사본을 갖기 때문).
- **D. 저장 경로 base 캐시**: `buildBaseOptimized`에 `_optBaseCache`(키 = `baseSignature()`).
  편집·임포징 옵션만 바꿔도 전 페이지를 다시 흑백변환하던 것을 없앴다.
- **E. 조작 중 갱신 보류**: 범위 슬라이더를 잡고 있는 동안에는 예약만 하고 놓을 때 1회 반영.

## 효과 (같은 문서로 변경 전/후 직접 비교)
**300쪽 · 84MB · 중철 150시트**
| | 변경 전 | 변경 후 |
|---|---|---|
| 첫 미리보기(체감) | **15,867ms** | **3,788ms** (4.2배) |
| 전부 채워지기까지 | 15,880ms | 9,343ms (1.7배) |
| 여백 1mm 변경(웜) | 약 12초 | **1,643ms** |
| 거터 1mm 변경 | 약 12초 | **921ms** |
| 저장 경로: 임포징 옵션 변경 | — | 855ms (base 재변환 0) |

**64쪽 · 18MB**: 여백 1mm 변경 2,961ms → **381ms (7.8배)**, 전부 채움 2,980 → 1,366ms.

## 검증
- **화질 무손실 확인**: 변경 전/후 같은 문서로 앞 6개 시트의 캔버스 픽셀 해시 **6개 모두 완전 일치**.
- 연속 조작 스트레스(여백 10→14→18→20을 120ms 간격): 최종 화면이 '한 번만 바꾼 결과'와 픽셀 일치,
  콘솔 오류 0, 남은 자리표시 0.
- 지문 충돌 점검: 여백 10/14/18/20mm 출력의 `bytesFingerprint` 4종 모두 상이(캐시 오적중 없음).
- `npm run smoke` + 회귀 94항목 통과.

## 함정
1. **교체 범위 안의 선언을 빠뜨리면 한쪽 경로만 죽는다.** `layoutSig` 선언을 지운 채로 두었더니
   임포징 경로는 그 변수를 안 써서 멀쩡했고 **1up 미리보기만** `ReferenceError`로 죽었다.
   임포징만 보고 "된다"고 판단하면 놓친다 — 두 경로를 다 확인할 것.
2. 미리보기 갱신은 디바운스·큐(`_liveQueued`)가 있어 **측정 시 큐가 빌 때까지 기다려야** 한다.
   안 기다리면 중간 단계 화면을 결과로 오인한다(실제로 "픽셀 불일치"로 잘못 판정했다).
3. pdf-lib 출력 바이트는 **실행할 때마다 다르다**(ID·타임스탬프). 결과 동일성은 바이트가 아니라
   **렌더된 픽셀**로 비교할 것.

---

# 📐 가로 페이지 자동 세로 맞춤 + 회전 임포징 재현 시도

## 1) 새 기능 — 가로 페이지 자동 방향 맞춤
세로 원고에 가로 원고가 섞여 있으면 임포징·인쇄에서 그 쪽만 눕는다. 아크로뱃에서 일일이 돌려
저장할 필요 없이 **앱이 방향이 다른 페이지만 90° 돌려 맞춘다.**

- 위치: ✏ 편집 사이드바 `📐 페이지 방향 맞춤`
  - `열 때 자동으로 방향 맞춤`(기본 켜짐) · 회전 방향 `↺ 왼쪽 90°`(기본) / `↻ 오른쪽 90°`
  - `📐 지금 방향 맞추기` 버튼 · 상단에 "방향이 다른 페이지 N쪽" 안내
- 동작: 문서의 **다수 방향을 기준**으로 삼고(동수면 세로), 다른 방향 페이지만 회전.
  정사각형에 가까운 페이지(오차 2% 안)는 건드리지 않는다.
- 회전은 기존 **페이지별 회전(`r.rotation`)** 을 그대로 쓴다 → 미리보기·적용·다운로드·임포징에
  자동으로 함께 반영되고 **Ctrl+Z로 되돌린다**. 새 파이프라인 단계를 만들지 않았다.
- 표시 방향 판정은 `pageWpt/pageHpt`(pdf.js 뷰포트 = **/Rotate가 반영된 크기**) 기준이라,
  아크로뱃에서 이미 돌려 둔 파일은 "모두 세로"로 보여 **아무것도 건드리지 않는다**.
- 작업 파일(.pdfw) 복원 중에는 끼어들지 않는다(`_openingWorkFile` 가드).

**검증**: `scripts/test/auto-orient.test.js` 19항목(회전 누적·정규화·다수결·빈페이지·폴백).
실제 앱 E2E: 세로 6 + 가로 2 문서를 열자 3·5쪽이 자동으로 왼쪽 90° 회전 → 전 페이지 세로 통일,
그 상태로 임포징한 시트를 Ghostscript로 렌더해 **가로였던 쪽이 세로 쪽과 같은 방향**으로 배치됨을 확인.
Ctrl+Z로 회전이 모두 0으로 복귀하는 것도 확인.

## 2) '아크로뱃 회전 후 임포징하면 90° 돌아간다' — 재현 실패(원인 미확정)
아크로뱃 '왼쪽으로 90도 회전'과 동일한 파일(가로 MediaBox + `/Rotate 270`)을 만들어 아래를 전부
확인했으나 **모두 정상**이었다.

| 조건 | 결과 |
|---|---|
| 편집 옵션 없이 임포징(중철) | 정상 — 아크로뱃에서 보이던 방향 그대로 |
| 규격화(A4)로 조립 워커를 태운 뒤 임포징 | 정상 |
| /Rotate 0·90·180·270 × 세로·가로 MediaBox 8종 | 배치·회전 모두 원본 표시와 일치 |
| 임포징 4모드(중철·정합·반복·복제 2부) | 정상 |

- `embedAllPages`(app-process.js)와 조립 워커 `drawFit`(worker-assemble.js) 모두 `/Rotate`를
  변환행렬로 굽고 있고, 실측에서도 어긋나지 않았다.
- `placeInSlot`은 슬롯에 맞추려고 페이지를 돌리지 않는다(비율만 맞춤) — 자동 회전 요소 없음.
- **참고**: `복제 2부` 모드는 오른쪽 벌을 **의도적으로 180° 회전**한다(Quite Imposing 방식).
  이걸 오작동으로 오해할 수 있다.

→ 사용자 원본 PDF(또는 문제가 나는 2~3쪽만 발췌)가 있어야 원인을 잡을 수 있다.
   자동 방향 맞춤이 들어왔으므로 **아크로뱃에서 돌리는 과정 자체를 생략**하면 이 경로를 피할 수 있다.

---

# 임포징 후 버튼 잠김 · 흑백변환 속도 · 작업 파일 복원 (4건)

## 1) 편집 모드에서 나오면 적용·다운로드가 한참 잠기던 문제

**원인**: `saveAndCloseWorkspace`가 **무조건 `applyChanges()`를 다시 돌렸다**.
편집 모드의 실시간 미리보기는 임포징 포함 상태에서 **표본이 아니라 전체 파이프라인**
(`buildBaseProcessed → 레이아웃 → 블리드 → 임포징`)을 이미 돌려 `processedPdfBytes`까지
만들어 둔다. 그런데 나오자마자 완전히 같은 조립을 처음부터 한 번 더 했고, 그동안
`applying=true`라 메인·좌측 사이드바·편집 패널의 적용·다운로드 버튼이 전부 비활성이었다.
"한참 뒤에 '미리보기 채우는 중'이 시작되고 그게 끝나갈 때 다운로드가 살아난다"의 정체.

**수정**
- 결과 바이트를 만들 때마다 그 시점의 파이프라인 지문 `_processedSig = optSignature()`를 남긴다
  (조립 **시작 시점**과 비교해 도중에 설정이 바뀌었으면 남기지 않는다 — 낡은 결과 재사용 방지).
- '💾 저장하고 닫기'는 지문이 현재 설정과 같고 라이브 렌더가 대기·진행 중이 아니면
  `adoptLivePreviewResult()`로 **그 결과를 그대로 적용 결과로 승격**한다(재조립 0초).
  흑백 확정(appliedBw)·선택 해제·작업내역 기록·다운로드 프리웜은 `applyChanges`와 동일하게 수행.
- 재조립이 필요한 경우에도 적용 버튼 3형제에 `btn-busy` 진행 스윕을 걸어
  '죽은 버튼'이 아니라 '만드는 중'으로 보이게 했다(`setApplyBusy`).
- `syncSidebarPanel`의 수정사항 판정에 **임포징·블리드·폰트 안전화가 빠져 있던 것**을 메인
  버튼(`updateDownloadBtn`)과 같은 조건으로 맞췄다 — 임포징만 켠 상태에서 좌측 '✔ 적용'만
  회색으로 남던 비대칭 해소.

## 2) 좌측 사이드바 버튼 색 — '지금 되는 것'만 노랑
`.sbp-apply:disabled`는 노랑을 벗고 회색으로, `.sbp-dl.btn-dim`(적용본 없음)도 회색 글자로.
📖 E-book 시안 버튼도 문서가 없으면 비활성(=회색)으로. 작업 중(`btn-busy`)일 때만 예외로
노랑을 유지한다.

## 3) 벡터 흑백변환 + 잉크 정규화 속도

세 가지를 고쳤다.

1. **원본 문서 공유 캐시(`getSourceDoc`)** — '적용' 한 번에 `PDFDocument.load(원본)`이
   3회(흑백변환·조립·다운로드 base) 일어나던 것을 1회로. `copyPages`가 소스를 바꾸지 않음을
   실제 pdf-lib로 검증(`scripts/test/srcdoc-share.test.js`, 바이트 단위 동일).
2. **흑백변환을 페이지별 독립 문서 → 한 배치 문서로** (`ensureBwConverted`).
   예전에는 쪽마다 새 문서에 `copyPages` → 여러 쪽이 같은 이미지를 쓰면 그 이미지를
   **쪽 수만큼** 복제하고 쪽 수만큼 다시 디코드·그레이화·재인코드했다. 한 문서에 모으면
   리소스가 공유돼 공용 이미지는 한 번만 변환된다(두 번째 쪽에서는 이미 DeviceGray라 건너뜀).
   캐시 값도 `{doc, idx}`로 바꾸고, 조립 시 같은 문서에서 온 쪽들은 `copyPages` 한 번으로 가져온다.
3. **프리웜 대상을 `isBwTarget`으로 통일** — 잉크 정규화 페이지뿐 아니라 사용자가 고른
   흑백 선택 페이지도 유휴 시간에 미리 굽는다(선택·옵션 변경 시 `scheduleBwPrewarm` 디바운스).
   '적용'을 누를 때는 대개 캐시가 이미 채워져 있다.

**실측**(오프스크린 Electron 하네스 + 실제 worker-gray.js, 40쪽·전 쪽이 600×800 컬러 이미지 공유)

| | 변환 | 조립 | 합계 | 결과 크기 |
|---|---|---|---|---|
| 예전 | 190ms | 25ms | 214ms | 1,996,563 B |
| 지금 | 127ms | 2ms | **129ms** | **71,274 B** |

리소스를 공유하지 않는 원고(쪽마다 다른 이미지 40쪽)에서도 173ms → 146ms로 손해가 없다.
결과 크기가 28배 줄어드는 것이 더 큰 소득 — 이후 미리보기 렌더·임포징·저장이 전부 가벼워진다.
출력 검증: Ghostscript ppmraw 렌더 480만 픽셀에서 **컬러 잔류 0픽셀**(최대 채도차 0).

## 4) 작업 파일(.pdfw)이 '적용 안 된 상태'로 열리던 문제

**원인**: 저장은 설정·문서 상태만 담고, 열 때도 그것만 되씌운 뒤 멈췄다. 사용자 입장에서는
매번 '✔ 적용'을 다시 눌러야 해서 새로 여는 것과 차이가 없었다.

**수정** — 작업 파일에 **최종 적용본 PDF 자체를 담는다**.
- `entries`에 `k:'result'`(적용 결과 PDF)와 필요 시 `k:'direct'`(블리드·아웃라인처럼 그대로
  저장해야 하는 외부 변환 결과)를 추가하고, `state.resultName`에 저장 파일명을 기록.
- 열 때는 그 블롭을 `processedPdfBytes`에 바로 넣고 화면에 띄운다 — **재계산 없음**.
  임포징 결과처럼 원본과 쪽 수가 달라도 저장 시점 화면 그대로 열린다.
- 구버전 작업 파일(적용 표식만 있고 적용본은 없음)은 예전처럼 `applyChanges()`로 한 번 만들어
  준다(호환 유지). 어느 경로든 중복 조립을 막으려 `scheduleLivePreview`는 예약하지 않는다.
- 대신 파일 크기는 원본 + 적용본만큼 커진다 — 저장 안내에 '적용본 포함'과 MB를 표시한다.
- `captureDocState`/`restoreDocState`에 **목차 페이지 표식(`isTocPage`)·북마크 제목(`tocTitle`)**
  추가 — 빠져 있어서 다시 열면 목차 페이지가 평범한 본문이 되고 로마자 번호·PDF 북마크가 사라졌다.
  (작업 파일과 '🕓 최근 작업 설정'이 같은 함수를 쓰므로 둘 다 함께 고쳐진다)

**검증**: `scripts/test/docstate.test.js`(실제 함수 추출, 18항목) + 오프스크린 앱 하네스에서
① `buildWorkFileBytes → unpackWorkFile` 왕복(applied·resultName·회전·목차표식·북마크제목·
흑백확정·선택·임포징 설정·원본 PDF·적용본이 **바이트 단위로 동일**),
② 실제 `openWorkFileBytes` 왕복 — 적용본이 그대로 복원되고(바이트 동일) **applyChanges가
한 번도 불리지 않으며**(재조립 0), 미리보기 셀 수가 원본 4쪽이 아니라 **적용본 2시트**,
다운로드 버튼이 활성·비흐림 상태임을 확인,
③ 구버전 파일(result 엔트리 제거)에서는 재적용 폴백이 동작하는 것까지 확인.
